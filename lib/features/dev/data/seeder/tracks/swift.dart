import '../seed_models.dart';

/// Seed content for the Swift track: modules (content + questions) and
/// flipcards. Consumed by the shared Firestore seeder via the combined
/// `seedTracks` catalog.
final SeedTrack swiftTrack = SeedTrack(
  id: 'SWIFT',
  name: 'Swift',
  description: 'Master Swift language fundamentals, concurrency, SwiftUI, memory management, networking, and data persistence for iOS development.',
  category: 'Mobile',
  color: 0xFFF05138,
  order: 1,
  modules: [
    // =========================================================================
    // Module 1: Swift Language Fundamentals
    // =========================================================================
    SeedModule(
      id: 'swift_language',
      title: 'Swift Language Fundamentals',
      description: 'Explore Swift\'s type system, optionals, control flow, value semantics, and protocols that form the backbone of iOS development.',
      level: 'BEGINNER',
      learningObjectives: [
        'Understand optionals and safely unwrap values using guard, if-let, and nil-coalescing.',
        'Distinguish between value types and reference types and know when to use structs versus classes.',
        'Write expressive code using enums, associated values, pattern matching, and switch statements.',
        'Leverage protocols and protocol-oriented programming to build flexible, testable architectures.',
        'Use generics to write reusable, type-safe code across different data types.',
      ],
      content: [
        {'type': 'heading', 'text': 'Optionals in Swift'},
        {
          'type': 'explanation',
          'text': 'Optionals are Swift\'s mechanism for handling the absence of a value. A variable of type String? can hold either a String or nil. This eliminates null pointer exceptions at compile time and forces developers to explicitly handle the nil case before using the value.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'var name: String? = "Alice"\nvar nothing: String? = nil\n\n// Force unwrap - dangerous if nil\nlet forced = name!\n\n// Safe unwrap with if-let\nif let safeName = name {\n    print("Hello, \\(safeName)")\n}\n\n// Guard unwrap for early exit\nfunc greet(_ name: String?) {\n    guard let name = name else {\n        print("Name is nil")\n        return\n    }\n    print("Hello, \\(name)")\n}',
        },
        {'type': 'heading', 'text': 'Nil-Coalescing and Optional Chaining'},
        {
          'type': 'explanation',
          'text': 'The nil-coalescing operator (??) provides a default value when an optional is nil. Optional chaining (?.) lets you call properties and methods on an optional without explicit unwrapping, returning nil if any link in the chain is nil.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'let userAge: Int? = nil\nlet displayAge = userAge ?? 0  // 0\n\nstruct Address {\n    let street: String\n}\nstruct Person {\n    var address: Address?\n}\nlet person = Person(address: nil)\nlet street = person.address?.street  // nil',
        },
        {'type': 'heading', 'text': 'Structs vs Classes'},
        {
          'type': 'explanation',
          'text': 'Swift favors value semantics. Structs are copied on assignment and passed by value, making them thread-safe by default. Classes use reference semantics and support inheritance. Use structs unless you specifically need inheritance, Objective-C interoperability, or reference identity.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'struct Point {\n    var x: Double\n    var y: Double\n}\nvar a = Point(x: 1, y: 2)\nvar b = a\nb.x = 10  // a.x is still 1\n\nclass Counter {\n    var count = 0\n    func increment() { count += 1 }\n}\nlet c1 = Counter()\nlet c2 = c1\nc2.increment()\nprint(c1.count)  // 1, both share the same instance',
        },
        {'type': 'heading', 'text': 'Enums with Associated Values'},
        {
          'type': 'explanation',
          'text': 'Swift enums are much more powerful than their C counterparts. They can have raw values, associated values, computed properties, and methods. Associated values let each case store different types of data.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'enum NetworkError {\n    case timeout\n    case serverError(code: Int, message: String)\n    case decodingError(Error)\n}\n\nlet error = NetworkError.serverError(code: 500, message: "Internal")\n\nswitch error {\ncase .timeout:\n    print("Timed out")\ncase .serverError(let code, let msg):\n    print("Server \\(code): \\(msg)")\ncase .decodingError(let err):\n    print("Decode fail: \\(err)")\n}',
        },
        {
          'type': 'heading',
          'text': 'Protocols and Protocol-Oriented Programming',
        },
        {
          'type': 'explanation',
          'text': 'Protocols define a blueprint of methods, properties, and other requirements. Swift\'s protocol-oriented approach lets you compose behavior through protocol extensions instead of class inheritance, enabling code reuse without the fragility of class hierarchies.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'protocol Describable {\n    var description: String { get }\n}\n\nextension Describable {\n    var description: String { "Default description" }\n}\n\nstruct User: Describable {\n    let name: String\n    var description: String { "User: \\(name)" }\n}',
        },
        {'type': 'heading', 'text': 'Generics'},
        {
          'type': 'explanation',
          'text': 'Generics allow you to write flexible, reusable functions and types that work with any type while maintaining type safety. They are used extensively in the Swift standard library (Array, Dictionary, Optional) and are essential for building generic data layers.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'func swapValues<T>(_ a: inout T, _ b: inout T) {\n    let temp = a\n    a = b\n    b = temp\n}\n\nstruct Stack<Element> {\n    private var items: [Element] = []\n    mutating func push(_ item: Element) { items.append(item) }\n    mutating func pop() -> Element? { items.popLast() }\n}\n\nvar intStack = Stack<Int>()\nintStack.push(1)\nintStack.push(2)\nlet top = intStack.pop()  // Optional(2)',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'swift_language_q1',
          question: 'What is the result of: var x: String? = nil; print(x ?? "default")?',
          options: ['nil', 'default', 'Runtime crash', 'Optional("default")'],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'swift_language_q2',
          question: 'Which keyword safely unwraps an optional and provides an early exit path?',
          options: ['as', 'guard let', 'defer', 'lazy'],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'swift_language_q3',
          question:
              'What happens when you assign one struct variable to another?',
          options: [
            'Both variables point to the same memory',
            'A copy of the value is created',
            'The original is deallocated',
            'A reference count is incremented',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'swift_language_q4',
          question: 'Which is true about Swift classes compared to structs?',
          options: [
            'Classes support inheritance and reference semantics',
            'Classes are copied on assignment',
            'Classes cannot have methods',
            'Classes are faster than structs',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'swift_language_q5',
          question: 'What does optional chaining return when any link in the chain is nil?',
          options: ['An error', 'An empty string', 'nil', 'Zero'],
          correctIndex: 2,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'swift_language_q6',
          question: 'What is a key advantage of protocol-oriented programming over class inheritance?',
          codeHeader: 'Protocol composition',
          codeLanguage: 'Swift',
          codeLines: [
            'protocol Flyable { func fly() }',
            'protocol Swimmable { func swim() }',
            'struct Duck: Flyable, Swimmable {',
            '    func fly() {}',
            '    func swim() {}',
            '}',
          ],
          options: [
            'Protocol extensions allow default implementations without inheritance',
            'Protocols are always faster',
            'Classes cannot use protocols',
            'Protocols enforce value semantics only',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_language_q7',
          question:
              'In the code below, what is the value of b.x after assignment?',
          codeHeader: 'Value semantics',
          codeLanguage: 'Swift',
          codeLines: [
            'struct Point { var x: Int; var y: Int }',
            'var a = Point(x: 5, y: 10)',
            'var b = a',
            'b.x = 20',
          ],
          options: ['5', '20', '10', 'Compilation error'],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'swift_language_q8',
          question: 'What does the following enum pattern matching print?',
          codeHeader: 'Enum associated values',
          codeLanguage: 'Swift',
          codeLines: [
            'enum Result {',
            '    case success(Int)',
            '    case failure(String)',
            '}',
            'let r = Result.success(42)',
            'if case .success(let val) = r {',
            '    print(val)',
            '}',
          ],
          options: ['success(42)', '42', 'Optional(42)', 'Compilation error'],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_language_q9',
          question:
              'Which of the following is a valid use of the defer statement?',
          options: [
            'Cleaning up resources at the end of a scope regardless of how the scope exits',
            'Deferring optional unwrapping until runtime',
            'Creating lazy properties',
            'Making a function asynchronous',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_language_q10',
          question: 'What does the generic constraint <T: Comparable> enforce?',
          options: [
            'T must be a class type',
            'T must conform to the Comparable protocol',
            'T must be an integer',
            'T can be any type',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
      ],
    ),
    // =========================================================================
    // Module 2: Concurrency, async/await & Actors
    // =========================================================================
    SeedModule(
      id: 'swift_concurrency',
      title: 'Concurrency, async/await & Actors',
      description: 'Master Swift structured concurrency with async/await, task groups, actors for data isolation, and Sendable protocol for thread safety.',
      level: 'INTERMEDIATE',
      learningObjectives: [
        'Use async/await to write readable asynchronous code that replaces completion handler patterns.',
        'Leverage Task and TaskGroup to run concurrent operations with structured lifecycle management.',
        'Understand actors and how they serialize access to mutable state without locks.',
        'Apply the Sendable protocol and global actor isolation to prevent data races at compile time.',
      ],
      content: [
        {'type': 'heading', 'text': 'async/await Fundamentals'},
        {
          'type': 'explanation',
          'text': 'Swift 5.5 introduced async/await, replacing callback-based asynchronous patterns. An async function can suspend execution at any await point, allowing the runtime to resume it when the result is available. This eliminates callback nesting and makes asynchronous code read like synchronous code.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'func fetchUser(id: Int) async throws -> User {\n    let url = URL(string: "https://api.example.com/users/\\(id)")!\n    let (data, _) = try await URLSession.shared.data(from: url)\n    return try JSONDecoder().decode(User.self, from: data)\n}\n\n// Calling an async function\nlet user = try await fetchUser(id: 42)',
        },
        {'type': 'heading', 'text': 'Task and TaskGroup'},
        {
          'type': 'explanation',
          'text': 'Tasks are the basic unit of concurrent work. A Task {} creates an unstructured task that runs concurrently. TaskGroup allows spawning multiple child tasks whose results you can collect. Structured concurrency ensures child tasks complete before their parent scope exits.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'await withTaskGroup(of: String.self) { group in\n    for url in urls {\n        group.addTask {\n            await self.download(url)\n        }\n    }\n    for await result in group {\n        print(result)\n    }\n}',
        },
        {'type': 'heading', 'text': 'Actors for Data Isolation'},
        {
          'type': 'explanation',
          'text': 'Actors protect their mutable state by ensuring only one task can access it at a time. This eliminates data races without requiring locks. Actor isolation means you must await when accessing an actor\'s properties or methods from outside.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'actor BankAccount {\n    var balance: Double\n    init(balance: Double) { self.balance = balance }\n    func deposit(_ amount: Double) { balance += amount }\n    func withdraw(_ amount: Double) -> Bool {\n        guard balance >= amount else { return false }\n        balance -= amount\n        return true\n    }\n}\n\nlet account = BankAccount(balance: 100)\nawait account.deposit(50)\nlet success = await account.withdraw(30)',
        },
        {'type': 'heading', 'text': 'Nonisolated and globalActor'},
        {
          'type': 'explanation',
          'text': 'The nonisolated keyword marks an actor method as not requiring isolation, meaning it can be called from anywhere without await. @MainActor is a global actor that ensures execution on the main thread, critical for UI updates.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': '@MainActor class ViewModel: ObservableObject {\n    @Published var items: [Item] = []\n    func load() async {\n        items = await service.fetchItems()\n    }\n}\n\nactor DataStore {\n    nonisolated var count: Int { items.count }\n    private var items: [String] = []\n    func add(_ item: String) { items.append(item) }\n}',
        },
        {'type': 'heading', 'text': 'Sendable Protocol'},
        {
          'type': 'explanation',
          'text': 'Sendable marks a type as safe to send across concurrency boundaries. Classes are not Sendable by default because they have reference semantics. Structs and enums with Sendable stored properties are automatically Sendable. This is enforced at compile time to prevent data races.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'struct UserData: Sendable {\n    let name: String\n    let age: Int\n}\n\nfunc process(_ data: UserData) async {\n    // Safe to pass across tasks\n}\n\n// Classes are NOT Sendable by default\nclass UnsafeCache: @unchecked Sendable {\n    var items: [String] = []\n}',
        },
        {'type': 'heading', 'text': 'Structured vs Unstructured Concurrency'},
        {
          'type': 'explanation',
          'text': 'Structured concurrency ties the lifetime of child tasks to their parent scope. When a scope exits, all child tasks are cancelled and awaited. Unstructured tasks (Task.detached) run independently and must be managed manually.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': '// Structured - child tasks are scoped\ndo {\n    try await withThrowingTaskGroup(of: Data.self) { group in\n        group.addTask { try await self.fetchA() }\n        group.addTask { try await self.fetchB() }\n    }\n} catch { print(error) }\n\n// Unstructured - must be managed manually\nlet task = Task.detached {\n    await self.longRunningWork()\n}\ntask.cancel()',
        },
        {'type': 'heading', 'text': 'Cancellation Handling'},
        {
          'type': 'explanation',
          'text': 'Tasks in a group are cancelled when the parent scope exits or when explicitly cancelled. Long-running tasks should check Task.isCancelled periodically. The withThrowingTaskGroup propagates cancellation as a CancellationError.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'func processItems(_ items: [Item]) async throws {\n    for item in items {\n        try Task.checkCancellation()\n        try await process(item)\n    }\n}\n\nlet task = Task {\n    try await processItems(items)\n}\ntask.cancel()',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'swift_concurrency_q1',
          question: 'What does the await keyword do in Swift concurrency?',
          options: [
            'Blocks the current thread until work completes',
            'Suspends the current task and allows the runtime to resume it later',
            'Creates a new thread',
            'Cancels the current task',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'swift_concurrency_q2',
          question: 'What happens to child tasks when a structured concurrency scope exits?',
          options: [
            'They continue running independently',
            'They are cancelled and awaited',
            'They throw an error',
            'They are paused',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_concurrency_q3',
          question:
              'Why must you use await when accessing an actor\'s property?',
          options: [
            'Actors are always asynchronous',
            'Actor isolation serializes access to prevent data races',
            'Actors run on a separate thread',
            'It is a compiler requirement only',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_concurrency_q4',
          question: 'What does @MainActor ensure?',
          codeHeader: 'Global actor isolation',
          codeLanguage: 'Swift',
          codeLines: [
            '@MainActor class HomeVM: ObservableObject {',
            '    @Published var count = 0',
            '    func increment() {',
            '        count += 1',
            '    }',
            '}',
          ],
          options: [
            'The class runs on a background thread',
            'All methods and properties are isolated to the main thread',
            'The class is Sendable',
            'The class uses a lock for thread safety',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_concurrency_q5',
          question: 'Which type is NOT automatically Sendable?',
          options: [
            'A struct with String and Int properties',
            'An enum with associated Int values',
            'A reference type class with mutable properties',
            'An actor',
          ],
          correctIndex: 2,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'swift_concurrency_q6',
          question: 'What is the purpose of Task.detached?',
          codeHeader: 'Unstructured concurrency',
          codeLanguage: 'Swift',
          codeLines: [
            'let task = Task.detached {',
            '    await self.heavyComputation()',
            '}',
          ],
          options: [
            'It creates a structured child task',
            'It creates an unstructured task not tied to the current scope',
            'It runs the task on the main thread',
            'It prevents the task from being cancelled',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'swift_concurrency_q7',
          question: 'What does withTaskGroup return?',
          options: [
            'A single Task object',
            'The result of the first child task',
            'An AsyncSequence that yields results as child tasks complete',
            'A DispatchQueue',
          ],
          correctIndex: 2,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'swift_concurrency_q8',
          question:
              'How do you check for cancellation inside a long-running task?',
          options: [
            'task.isCancelled',
            'Task.isCancelled',
            'try Task.checkCancellation()',
            'Both Task.isCancelled and try Task.checkCancellation()',
          ],
          correctIndex: 3,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_concurrency_q9',
          question: 'What is the result of calling an isolated actor method without await?',
          options: [
            'It compiles but crashes at runtime',
            'A compiler error',
            'The method runs concurrently',
            'The method is silently skipped',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'swift_concurrency_q10',
          question: 'What error does withThrowingTaskGroup throw when a child task is cancelled?',
          options: [
            'CancellationError',
            'RuntimeError',
            'TimeoutError',
            'NilError',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
        ),
      ],
    ),
    // =========================================================================
    // Module 3: SwiftUI
    // =========================================================================
    SeedModule(
      id: 'swiftui_basics',
      title: 'SwiftUI',
      description: 'Build declarative UIs with SwiftUI using state management, view composition, animations, and the modern layout system.',
      level: 'INTERMEDIATE',
      learningObjectives: [
        'Manage view state with @State, @Binding, @ObservedObject, @StateObject, and @EnvironmentObject.',
        'Compose complex layouts using stacks, grids, and custom layout containers.',
        'Implement animations using implicit and explicit animation techniques.',
        'Use navigation and modal presentation patterns in SwiftUI.',
        'Understand the SwiftUI view lifecycle and diffing algorithm.',
      ],
      content: [
        {'type': 'heading', 'text': 'State Management in SwiftUI'},
        {
          'type': 'explanation',
          'text': 'SwiftUI uses property wrappers to manage view state. @State holds local mutable state owned by the view. @Binding creates a two-way connection to a parent\'s @State. @StateObject creates an observable object owned by the view. @ObservedObject observes an externally provided object.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'struct CounterView: View {\n    @State private var count = 0\n    @Binding var title: String\n    @ObservedObject var viewModel: CounterViewModel\n\n    var body: some View {\n        VStack {\n            Text("\\(title): \\(count)")\n            Button("Increment") { count += 1 }\n        }\n    }\n}',
        },
        {'type': 'heading', 'text': 'View Composition and Building Blocks'},
        {
          'type': 'explanation',
          'text': 'SwiftUI views are structs, not classes. They are value types that are cheaply created and discarded. The framework diffs the view hierarchy and updates only the changed parts. Break complex views into small, focused components for readability and testability.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'struct UserRow: View {\n    let user: User\n    var body: some View {\n        HStack {\n            AvatarView(user: user)\n            VStack(alignment: .leading) {\n                Text(user.name).font(.headline)\n                Text(user.email).font(.subheadline).foregroundColor(.gray)\n            }\n        }\n        .padding()\n    }\n}\n\nstruct AvatarView: View {\n    let user: User\n    var body: some View {\n        AsyncImage(url: user.avatarURL)\n            .frame(width: 44, height: 44)\n            .clipShape(Circle())\n    }\n}',
        },
        {'type': 'heading', 'text': 'Layout System'},
        {
          'type': 'explanation',
          'text': 'SwiftUI layouts work by proposing sizes to child views. The parent proposes a size, the child returns its ideal size, and the parent places it. HStack, VStack, and ZStack distribute space along axes. LazyVGrid uses columns defined by GridItem.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'let columns = [\n    GridItem(.flexible()),\n    GridItem(.flexible()),\n    GridItem(.flexible())\n]\n\nScrollView {\n    LazyVGrid(columns: columns, spacing: 16) {\n        ForEach(items) { item in\n            ItemCard(item: item)\n        }\n    }\n    .padding()\n}',
        },
        {'type': 'heading', 'text': 'Implicit and Explicit Animations'},
        {
          'type': 'explanation',
          'text': 'The .animation modifier applies implicit animations when state changes. For explicit control, use withAnimation blocks. Transition modifiers (.transition(.slide)) animate views entering and leaving.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'struct AnimatedView: View {\n    @State private var show = false\n    var body: some View {\n        VStack {\n            if show {\n                RoundedRectangle(cornerRadius: 20)\n                    .fill(Color.blue)\n                    .frame(height: 200)\n                    .transition(.asymmetric(\n                        insertion: .slide.combined(with: .opacity),\n                        removal: .scale.combined(with: .opacity)\n                    ))\n            }\n            Button("Toggle") {\n                withAnimation(.spring(response: 0.6)) {\n                    show.toggle()\n                }\n            }\n        }\n    }\n}',
        },
        {'type': 'heading', 'text': 'Navigation Patterns'},
        {
          'type': 'explanation',
          'text': 'SwiftUI offers NavigationStack (iOS 16+) for stack-based navigation, NavigationSplitView for master-detail layouts, and .sheet / .fullScreenCover for modal presentation. NavigationPath allows programmatic navigation in a type-safe way.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'struct ContentView: View {\n    @State private var path = NavigationPath()\n    var body: some View {\n        NavigationStack(path: \$path) {\n            List(items) { item in\n                NavigationLink(value: item) {\n                    Text(item.name)\n                }\n            }\n            .navigationDestination(for: Item.self) { item in\n                DetailView(item: item)\n            }\n        }\n    }\n}',
        },
        {'type': 'heading', 'text': 'Environment and EnvironmentObject'},
        {
          'type': 'explanation',
          'text': 'The @Environment property wrapper reads system-provided values (color scheme, locale, size class). @EnvironmentObject injects shared observable objects down the view tree. Both propagate changes automatically when values update.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'struct SettingsView: View {\n    @Environment(\\.colorScheme) var colorScheme\n    @EnvironmentObject var settings: AppSettings\n    var body: some View {\n        VStack {\n            Text("Current theme: \\(colorScheme == .dark ? "Dark" : "Light")")\n            Toggle("Notifications", isOn: \$settings.notificationsEnabled)\n        }\n    }\n}',
        },
        {'type': 'heading', 'text': 'Custom View Modifiers and Previews'},
        {
          'type': 'explanation',
          'text': 'View modifiers encapsulate reusable styling and behavior. Create them by conforming to ViewModifier. SwiftUI Previews provide instant feedback in Xcode without running the full app.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'struct CardModifier: ViewModifier {\n    func body(content: Content) -> some View {\n        content\n            .padding()\n            .background(Color(.systemBackground))\n            .cornerRadius(12)\n            .shadow(radius: 4)\n    }\n}\n\nextension View {\n    func cardStyle() -> some View {\n        modifier(CardModifier())\n    }\n}\n\n#Preview {\n    Text("Hello").cardStyle()\n}',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'swiftui_basics_q1',
          question: 'What is the difference between @State and @StateObject?',
          options: [
            '@State is for value types, @StateObject is for reference types (ObservableObject)',
            'They are identical',
            '@State is for reference types, @StateObject is for value types',
            '@StateObject is deprecated in favor of @State',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'swiftui_basics_q2',
          question: 'When should you use @Binding instead of @State?',
          options: [
            'When the view needs to own and initialize the value',
            'When the view needs to read and write a value owned by a parent view',
            'When the value never changes',
            'When working with non-observable objects',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'swiftui_basics_q3',
          question: 'What does the .animation modifier do in SwiftUI?',
          options: [
            'Only animates the first state change',
            'Animates all animatable property changes that occur within a state update',
            'Creates a keyframe animation',
            'Plays a sound during animation',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swiftui_basics_q4',
          question: 'Which layout container distributes space horizontally?',
          options: ['VStack', 'ZStack', 'HStack', 'GeometryReader'],
          correctIndex: 2,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'swiftui_basics_q5',
          question: 'What is the purpose of GeometryReader?',
          options: [
            'It reads the size and position of its parent',
            'It provides device orientation',
            'It renders vector graphics',
            'It reads Core Motion data',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swiftui_basics_q6',
          question: 'How does NavigationPath enable type-safe navigation?',
          codeHeader: 'Navigation path',
          codeLanguage: 'Swift',
          codeLines: [
            '@State private var path = NavigationPath()',
            'NavigationStack(path: \$path) {',
            '    .navigationDestination(for: Item.self) { item in',
            '        DetailView(item: item)',
            '    }',
            '}',
          ],
          options: [
            'It uses string-based routing',
            'It uses Hashable values pushed onto a typed stack',
            'It uses enum cases as destinations',
            'It uses a Coordinator pattern',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'swiftui_basics_q7',
          question: 'What happens when an @ObservableObject property changes?',
          options: [
            'The entire parent view rebuilds',
            'Only the specific views reading the changed property rebuild',
            'Nothing unless manually triggered',
            'Only the view that owns the object rebuilds',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'swiftui_basics_q8',
          question: 'What is the correct way to inject an environment object?',
          options: [
            '@EnvironmentObject var settings: AppSettings',
            '@Injected var settings: AppSettings',
            '@Injectable var settings: AppSettings',
            '@Provides var settings: AppSettings',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'swiftui_basics_q9',
          question: 'Which modifier presents a modal sheet?',
          options: ['.popover', '.sheet', '.navigationLink', '.alert'],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'swiftui_basics_q10',
          question: 'Why are SwiftUI views structs rather than classes?',
          options: [
            'For reference semantics and inheritance',
            'For value semantics, performance, and deterministic diffing',
            'Because UIKit views are structs',
            'It has no practical reason',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
      ],
    ),
    // =========================================================================
    // Module 4: ARC & Memory Management
    // =========================================================================
    SeedModule(
      id: 'ios_memory_management',
      title: 'ARC & Memory Management',
      description: 'Understand Automatic Reference Counting, retain cycles, weak and unowned references, and tools for diagnosing memory issues.',
      level: 'ADVANCED',
      learningObjectives: [
        'Explain how Automatic Reference Counting tracks and deallocates objects.',
        'Identify and fix retain cycles using weak and unowned references.',
        'Use Instruments to detect memory leaks, zombies, and excessive allocations.',
        'Apply best practices for closures and delegate patterns to prevent memory issues.',
      ],
      content: [
        {'type': 'heading', 'text': 'How ARC Works'},
        {
          'type': 'explanation',
          'text': 'ARC (Automatic Reference Counting) is a compile-time memory management system for classes in Swift. Each strong reference to an object increments its reference count. When the reference count reaches zero, the object is deallocated immediately. Structs and enums do not use ARC because they are value types.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'class User {\n    let name: String\n    init(name: String) { self.name = name }\n    deinit { print("\\(name) deallocated") }\n}\n\nvar ref1: User? = User(name: "Alice")\nvar ref2 = ref1  // refCount = 2\nref1 = nil       // refCount = 1\nref2 = nil       // refCount = 0, deallocated',
        },
        {'type': 'heading', 'text': 'Retain Cycles'},
        {
          'type': 'explanation',
          'text': 'A retain cycle occurs when two objects hold strong references to each other, preventing deallocation. This is the most common memory leak pattern in iOS. Closures that capture self strongly create retain cycles.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'class ViewController: UIViewController {\n    var onSave: (() -> Void)?\n\n    override func viewDidLoad() {\n        super.viewDidLoad()\n        // RETAIN CYCLE: closure captures self strongly\n        onSave = {\n            self.save()\n        }\n    }\n\n    func save() { print("Saved") }\n    deinit { print("Deallocated") }\n}',
        },
        {'type': 'heading', 'text': 'Weak and Unowned References'},
        {
          'type': 'explanation',
          'text': 'Use weak references to break retain cycles. A weak reference does not increase the reference count and is automatically set to nil when the object is deallocated (hence it must be optional). Use unowned when you know the referenced object will always outlive the reference (non-optional, like a parent).',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'class ViewController: UIViewController {\n    var onSave: (() -> Void)?\n\n    override func viewDidLoad() {\n        super.viewDidLoad()\n        onSave = { [weak self] in\n            self?.save()\n        }\n    }\n\n    func save() { print("Saved") }\n}\n\n// Unowned: parent outlives child\nclass Parent {\n    var child: Child?\n    init() { child = Child(parent: self) }\n}\nclass Child {\n    unowned let parent: Parent\n    init(parent: Parent) { self.parent = parent }\n}',
        },
        {'type': 'heading', 'text': 'Delegate Pattern and Memory'},
        {
          'type': 'explanation',
          'text': 'The delegate pattern uses weak references to prevent retain cycles. A delegate property is declared as weak var delegate: SomeDelegate?, ensuring the child view does not retain the parent. This is a standard pattern in UIKit.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'protocol DataManagerDelegate: AnyObject {\n    func didFetchData(_ data: [Item])\n}\n\nclass DataManager {\n    weak var delegate: DataManagerDelegate?\n    func fetch() {\n        let data = [Item]()\n        delegate?.didFetchData(data)\n    }\n}',
        },
        {'type': 'heading', 'text': 'Closures and Capture Lists'},
        {
          'type': 'explanation',
          'text': 'Capture lists define how closures capture external values. [weak self] captures self as a weak optional. [unowned self] captures self as an unowned reference. You can capture other values by value using [let x = x].',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'class NetworkManager {\n    var session: URLSession!\n    func start() {\n        let url = URL(string: "https://api.example.com")!\n        session = URLSession(configuration: .default)\n        session.dataTask(with: url) { [weak self] data, _, _ in\n            guard let self = self else { return }\n            self.process(data)\n        }.resume()\n    }\n    func process(_ data: Data?) {}\n}',
        },
        {'type': 'heading', 'text': 'Detecting Memory Leaks with Instruments'},
        {
          'type': 'explanation',
          'text': 'Xcode Instruments provides the Leaks and Allocations tools to detect memory issues. The Leaks instrument tracks objects that were deallocated but still have references. The Memory Graph Debugger in Xcode visualizes object reference graphs to identify retain cycles at runtime.',
        },
        {'type': 'heading', 'text': 'Weak Self vs Unowned Self'},
        {
          'type': 'explanation',
          'text': 'Choose weak when the captured object may be deallocated before the closure executes (e.g., network callbacks). Choose unowned when you are certain the object outlives the closure (e.g., synchronous access within a known scope). Unowned access crashes if the object is nil.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'class ImageLoader {\n    func load(url: URL, completion: @escaping (UIImage?) -> Void) {\n        URLSession.shared.dataTask(with: url) { [weak self] data, _, _ in\n            guard let self = self else { return }\n            let image = self.decode(data)\n            DispatchQueue.main.async { completion(image) }\n        }.resume()\n    }\n    func decode(_ data: Data?) -> UIImage? {\n        guard let data = data else { return nil }\n        return UIImage(data: data)\n    }\n}',
        },
        {'type': 'heading', 'text': 'Common Memory Pitfalls'},
        {
          'type': 'explanation',
          'text': 'Retain cycles from closures are the most common leak. Strong reference cycles in delegate patterns, closures capturing self in view controllers, and notification observers not being removed are frequent issues. Use [weak self] in all escaping closures and remove observers in deinit.',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'ios_memory_management_q1',
          question: 'What happens when an object\'s reference count reaches zero under ARC?',
          options: [
            'It is moved to a deallocation queue',
            'It is immediately deallocated',
            'It waits for the next run loop cycle',
            'Nothing, ARC does not deallocate objects',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'ios_memory_management_q2',
          question: 'What is a retain cycle?',
          options: [
            'A cycle where an object is reused in a loop',
            'A situation where two objects hold strong references to each other, preventing deallocation',
            'A reference count overflow error',
            'A weak reference that is not set to nil',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'ios_memory_management_q3',
          question: 'When should you use weak vs unowned references?',
          options: [
            'Weak when the object may be deallocated first, unowned when it will always outlive the reference',
            'Weak for performance, unowned for safety',
            'Always use weak',
            'Always use unowned',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'ios_memory_management_q4',
          question: 'What is the result of this code?',
          codeHeader: 'Capture list behavior',
          codeLanguage: 'Swift',
          codeLines: [
            'class Foo {',
            '    var handler: (() -> Void)?',
            '    func setup() {',
            '        handler = { [weak self] in',
            '            print(self?.name ?? "nil")',
            '        }',
            '    }',
            '    var name = "Foo"',
            '}',
          ],
          options: [
            'Prints "Foo"',
            'Prints "nil" after Foo is deallocated',
            'Causes a retain cycle',
            'Compiler error',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'ios_memory_management_q5',
          question: 'Which instrument detects memory leaks in Xcode?',
          options: ['Time Profiler', 'Leaks', 'Network', 'Animation Hitches'],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'ios_memory_management_q6',
          question: 'What happens if you access an unowned reference after the object is deallocated?',
          options: [
            'It returns nil',
            'It crashes with a runtime error',
            'It returns a default value',
            'It silently does nothing',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'ios_memory_management_q7',
          question:
              'Why is the delegate pattern typically declared with weak var?',
          codeHeader: 'Delegate pattern',
          codeLanguage: 'Swift',
          codeLines: [
            'protocol DataManagerDelegate: AnyObject {}',
            'class DataManager {',
            '    weak var delegate: DataManagerDelegate?',
            '}',
          ],
          options: [
            'For performance optimization',
            'To prevent a retain cycle between parent and child',
            'Because delegates must always be optional',
            'To enable ARC for the delegate protocol',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'ios_memory_management_q8',
          question: 'What is a common cause of memory leaks in closures?',
          options: [
            'Using value types in the closure',
            'Capturing self strongly in an escaping closure',
            'Using guard let inside closures',
            'Returning a value from a closure',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'ios_memory_management_q9',
          question: 'What does the deinit method do?',
          options: [
            'Initializes the object',
            'Is called when the reference count reaches zero and the object is about to be deallocated',
            'Resets all properties to their default values',
            'Removes all observers and delegates',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'ios_memory_management_q10',
          question: 'Which of the following will NOT cause a retain cycle?',
          options: [
            'A closure capturing self without a capture list',
            'Two classes referencing each other with strong var',
            'A closure capturing self with [weak self]',
            'A view controller storing a strong reference to a child that references the parent',
          ],
          correctIndex: 2,
          level: 'ADVANCED',
        ),
      ],
    ),
    // =========================================================================
    // Module 5: Networking & Codable
    // =========================================================================
    SeedModule(
      id: 'ios_networking',
      title: 'Networking & Codable',
      description: 'Build robust networking layers using URLSession, async/await, and Swift\'s Codable protocol for JSON serialization.',
      level: 'INTERMEDIATE',
      learningObjectives: [
        'Use URLSession with async/await to make HTTP requests and handle responses.',
        'Implement Codable structs to encode and decode JSON data.',
        'Handle network errors gracefully with custom error types and retry logic.',
        'Build a reusable API client with interceptors, headers, and base URL configuration.',
      ],
      content: [
        {'type': 'heading', 'text': 'URLSession with async/await'},
        {
          'type': 'explanation',
          'text': 'URLSession provides a modern API for network requests. Using async/await, you can fetch data without completion handlers. The data(from:) method returns both the data and the URL response, allowing you to check status codes.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'func fetchUsers() async throws -> [User] {\n    let url = URL(string: "https://api.example.com/users")!\n    let (data, response) = try await URLSession.shared.data(from: url)\n    guard let httpResponse = response as? HTTPURLResponse,\n          (200...299).contains(httpResponse.statusCode) else {\n        throw APIError.invalidResponse\n    }\n    return try JSONDecoder().decode([User].self, from: data)\n}',
        },
        {'type': 'heading', 'text': 'Codable Protocol'},
        {
          'type': 'explanation',
          'text': 'Codable is a type alias for Encodable and Decodable. Swift can automatically synthesize conformance for structs with Codable properties. Custom CodingKeys enable mapping between property names and JSON keys.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'struct User: Codable {\n    let id: Int\n    let name: String\n    let email: String\n    let createdAt: Date\n\n    enum CodingKeys: String, CodingKey {\n        case id\n        case name\n        case email\n        case createdAt = "created_at"\n    }\n}\n\nlet decoder = JSONDecoder()\ndecoder.keyDecodingStrategy = .convertFromSnakeCase\ndecoder.dateDecodingStrategy = .iso8601',
        },
        {'type': 'heading', 'text': 'Custom Error Types'},
        {
          'type': 'explanation',
          'text': 'Define custom error types that conform to Error and LocalizedError for meaningful error messages. This helps in debugging and provides user-friendly error handling across your networking layer.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'enum APIError: Error, LocalizedError {\n    case invalidURL\n    case invalidResponse\n    case decodingFailed(Error)\n    case serverError(Int)\n    case networkUnavailable\n\n    var errorDescription: String? {\n        switch self {\n        case .invalidURL: return "Invalid URL"\n        case .invalidResponse: return "Invalid server response"\n        case .decodingFailed(let err): return "Decoding failed: \\(err.localizedDescription)"\n        case .serverError(let code): return "Server error \\(code)"\n        case .networkUnavailable: return "Network unavailable"\n        }\n    }\n}',
        },
        {'type': 'heading', 'text': 'POST Requests with JSON Body'},
        {
          'type': 'explanation',
          'text': 'POST and PUT requests encode the request body as JSON using Encodable. Create URLRequest, set the HTTP method and Content-Type header, then encode the body.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'func createUser(_ user: NewUser) async throws -> User {\n    var request = URLRequest(url: URL(string: "https://api.example.com/users")!)\n    request.httpMethod = "POST"\n    request.setValue("application/json", forHTTPHeaderField: "Content-Type")\n    request.httpBody = try JSONEncoder().encode(user)\n    let (data, _) = try await URLSession.shared.data(for: request)\n    return try JSONDecoder().decode(User.self, from: data)\n}',
        },
        {'type': 'heading', 'text': 'Custom URL Protocol and Interceptors'},
        {
          'type': 'explanation',
          'text': 'URLProtocol allows intercepting and modifying network requests. Use it for logging, caching, authentication token injection, and mocking in tests. Subclass URLProtocol and register it with URLSessionConfiguration.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'class AuthInterceptor: URLProtocol {\n    override class func canInit(with request: URLRequest) -> Bool {\n        request.url?.host == "api.example.com"\n    }\n    override class func canonicalRequest(for request: URLRequest) -> URLRequest {\n        var modified = request\n        modified.setValue("Bearer \\(token)", forHTTPHeaderField: "Authorization")\n        return modified\n    }\n}',
        },
        {'type': 'heading', 'text': 'JSON Encoder Configuration'},
        {
          'type': 'explanation',
          'text': 'JSONEncoder supports various strategies for encoding dates, keys, and nested containers. Configure these on the encoder instance before encoding to match your API\'s expected format.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'let encoder = JSONEncoder()\nencoder.dateEncodingStrategy = .formatted(dateFormatter)\nencoder.keyEncodingStrategy = .convertToSnakeCase\nencoder.outputFormatting = [.prettyPrinted, .sortedKeys]\nlet data = try encoder.encode(user)\nlet jsonString = String(data: data, encoding: .utf8)',
        },
        {'type': 'heading', 'text': 'Network Reachability and Retry'},
        {
          'type': 'explanation',
          'text': 'Handle network failures with retry logic using exponential backoff. NWPathMonitor provides real-time network status updates. Combining both gives your app resilience against transient network issues.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'import Network\n\nlet monitor = NWPathMonitor()\nmonitor.pathUpdateHandler = { path in\n    if path.status == .satisfied {\n        print("Online")\n    } else {\n        print("Offline")\n    }\n}\nmonitor.start(queue: .global())\n\nfunc retry<T>(_ attempts: Int, operation: @escaping () async throws -> T) async throws -> T {\n    for attempt in 0..<attempts {\n        do {\n            return try await operation()\n        } catch {\n            if attempt == attempts - 1 { throw error }\n            try await Task.sleep(for: .seconds(pow(2.0, Double(attempt))))\n        }\n    }\n    fatalError("Unreachable")\n}',
        },
        {'type': 'heading', 'text': 'Decoding Flexible JSON'},
        {
          'type': 'explanation',
          'text': 'Real-world APIs often have inconsistent JSON shapes. Use custom init(from:) decoders to handle optional fields, nested responses, and polymorphic types gracefully.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'struct ApiResponse<T: Codable>: Codable {\n    let data: T\n    let meta: Meta?\n\n    struct Meta: Codable {\n        let page: Int\n        let totalPages: Int\n    }\n}\n\nlet response = try JSONDecoder().decode(ApiResponse<[User]>.self, from: data)\nlet users = response.data',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'ios_networking_q1',
          question: 'What does URLSession.shared.data(from:) return?',
          options: [
            'Only the response body as Data',
            'A tuple of (Data, URLResponse)',
            'A decoded object',
            'An optional Data',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'ios_networking_q2',
          question: 'What must a type conform to for automatic JSON decoding?',
          options: ['Codable', 'Equatable', 'Hashable', 'Identifiable'],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'ios_networking_q3',
          question: 'What CodingKeys case maps a Swift property to a snake_case JSON key?',
          codeHeader: 'CodingKeys',
          codeLanguage: 'Swift',
          codeLines: [
            'struct User: Codable {',
            '    let firstName: String',
            '    enum CodingKeys: String, CodingKey {',
            '        case firstName = "first_name"',
            '    }',
            '}',
          ],
          options: [
            'It maps firstName to "first_name"',
            'It maps firstName to "firstName"',
            'It maps firstName to "first-name"',
            'No mapping needed',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'ios_networking_q4',
          question: 'How do you set the HTTP method on a URLRequest?',
          options: [
            'request.method = "POST"',
            'request.httpMethod = "POST"',
            'request.setMethod(.post)',
            'request.type = "POST"',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'ios_networking_q5',
          question: 'What strategy should you use for decoding snake_case JSON keys automatically?',
          options: [
            'decoder.keyDecodingStrategy = .convertFromSnakeCase',
            'decoder.keyDecodingStrategy = .useDefaultKeys',
            'Set CodingKeys for every property',
            'Use a custom JSON parser',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'ios_networking_q6',
          question: 'What is the purpose of URLProtocol?',
          options: [
            'To make HTTP requests only',
            'To intercept and modify network requests for logging, caching, or mocking',
            'To parse JSON responses',
            'To manage background downloads',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'ios_networking_q7',
          question: 'What does NWPathMonitor provide?',
          options: [
            'SSL certificate validation',
            'Real-time network reachability status',
            'Automatic request retries',
            'URL session configuration',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'ios_networking_q8',
          question: 'What is exponential backoff in retry logic?',
          options: [
            'Retrying immediately without delay',
            'Doubling the wait time between each retry attempt',
            'Reducing the retry count',
            'Using a random delay each time',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'ios_networking_q9',
          question: 'How do you handle a custom JSON key mapping for dates?',
          options: [
            'Use CodingKeys to map to a Date directly',
            'Configure decoder.dateDecodingStrategy with a DateFormatter',
            'Decode dates as strings and convert manually',
            'Dates are handled automatically',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'ios_networking_q10',
          question: 'What is the benefit of wrapping API responses in a generic ApiResponse type?',
          options: [
            'It prevents nil values',
            'It standardizes response handling for data, metadata, and pagination across all endpoints',
            'It automatically caches responses',
            'It handles authentication',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
      ],
    ),
    // =========================================================================
    // Module 6: Persistence & Storage
    // =========================================================================
    SeedModule(
      id: 'ios_data_persistence',
      title: 'Persistence & Storage',
      description: 'Choose and implement the right persistence strategy for iOS apps: UserDefaults, FileManager, CoreData, SwiftData, Keychain, and SQLite.',
      level: 'ADVANCED',
      learningObjectives: [
        'Compare persistence options and select the right one based on data size, complexity, and query needs.',
        'Use SwiftData to persist object graphs with modern declarative APIs.',
        'Implement CoreData for complex relational data with migrations and batch operations.',
        'Secure sensitive data using Keychain and encrypt files at rest.',
      ],
      content: [
        {'type': 'heading', 'text': 'UserDefaults for Lightweight Storage'},
        {
          'type': 'explanation',
          'text': 'UserDefaults stores small amounts of data like preferences, settings, and flags. It is backed by a plist file and is not suitable for large or structured data. Keep stored values to primitives, strings, Data, and simple collections.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'let defaults = UserDefaults.standard\ndefaults.set("dark", forKey: "theme")\ndefaults.set(true, forKey: "notifications_enabled")\ndefaults.integer(forKey: "launch_count")\n\ndefaults.register(defaults: [\n    "theme": "light",\n    "notifications_enabled": true,\n    "launch_count": 0\n])',
        },
        {'type': 'heading', 'text': 'SwiftData Overview'},
        {
          'type': 'explanation',
          'text': 'SwiftData is Apple\'s modern persistence framework introduced in iOS 17. It uses the @Model macro to define persistent types and integrates seamlessly with SwiftUI. It handles migrations, queries, and relationship management with a declarative API.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': '@Model\nclass Note {\n    var title: String\n    var body: String\n    var createdAt: Date\n    @Relationship(deleteRule: .cascade) var tags: [Tag]\n\n    init(title: String, body: String) {\n        self.title = title\n        self.body = body\n        self.createdAt = Date()\n        self.tags = []\n    }\n}\n\n@Model\nclass Tag {\n    var name: String\n    init(name: String) { self.name = name }\n}',
        },
        {'type': 'heading', 'text': 'SwiftData Queries and Filtering'},
        {
          'type': 'explanation',
          'text': 'SwiftData uses @Query to fetch data declaratively in SwiftUI. You can apply predicates and sort descriptors to filter results. For non-SwiftUI contexts, use ModelContext.fetch() with FetchDescriptor.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'struct NoteListView: View {\n    @Query(sort: \\Note.createdAt, order: .reverse) var notes: [Note]\n    @Environment(\\.modelContext) var context\n\n    var body: some View {\n        List(notes) { note in\n            Text(note.title)\n        }\n    }\n}\n\n// Programmatic fetch\nlet descriptor = FetchDescriptor<Note>(\n    predicate: #Predicate { \$0.title.contains("Swift") },\n    sortBy: [SortDescriptor(\\Note.createdAt)]\n)\nlet results = try context.fetch(descriptor)',
        },
        {'type': 'heading', 'text': 'CoreData Fundamentals'},
        {
          'type': 'explanation',
          'text': 'CoreData is Apple\'s mature persistence framework with full relational database capabilities. It uses NSManagedObject subclasses, NSManagedObjectContext for transactions, and NSPersistentContainer for the store stack. It supports complex migrations and batch operations.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'let container = NSPersistentContainer(name: "Model")\ncontainer.loadPersistentStores { description, error in\n    if let error = error {\n        fatalError("Core Data failed to load: \\(error)")\n    }\n}\nlet context = container.viewContext\n\nlet fetchRequest: NSFetchRequest<Person> = Person.fetchRequest()\nfetchRequest.predicate = NSPredicate(format: "age > %d", 18)\nfetchRequest.sortDescriptors = [NSSortDescriptor(keyPath: \\Person.name, ascending: true)]\nlet adults = try context.fetch(fetchRequest)',
        },
        {'type': 'heading', 'text': 'CoreData vs SwiftData'},
        {
          'type': 'explanation',
          'text': 'SwiftData is the modern choice for new projects targeting iOS 17+. It offers type-safe queries, better SwiftUI integration, and automatic migrations for simple schema changes. CoreData remains necessary for complex relational models, heavy batch operations, and backward compatibility with older iOS versions.',
        },
        {'type': 'heading', 'text': 'Keychain for Secure Storage'},
        {
          'type': 'explanation',
          'text': 'Keychain Services stores sensitive data like tokens, passwords, and encryption keys in an encrypted database. Data persists across app uninstalls unless explicitly deleted. Use the Security framework or a wrapper library for access.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'import Security\n\nfunc saveToKeychain(key: String, value: String) {\n    let data = value.data(using: .utf8)!\n    let query: [String: Any] = [\n        kSecClass as String: kSecClassGenericPassword,\n        kSecAttrAccount as String: key,\n        kSecValueData as String: data\n    ]\n    SecItemAdd(query as CFDictionary, nil)\n}\n\nfunc loadFromKeychain(key: String) -> String? {\n    let query: [String: Any] = [\n        kSecClass as String: kSecClassGenericPassword,\n        kSecAttrAccount as String: key,\n        kSecReturnData as String: true\n    ]\n    var result: AnyObject?\n    SecItemCopyMatching(query as CFDictionary, &result)\n    guard let data = result as? Data else { return nil }\n    return String(data: data, encoding: .utf8)\n}',
        },
        {'type': 'heading', 'text': 'FileManager and Documents Directory'},
        {
          'type': 'explanation',
          'text': 'The Documents directory is the standard location for user-generated content. Use FileManager to read and write files there. Files in the Documents directory are backed up to iCloud automatically.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'let fileManager = FileManager.default\nlet documents = fileManager.urls(for: .documentDirectory, in: .userDomainMask).first!\nlet fileURL = documents.appendingPathComponent("data.json")\n\n// Write\nlet data = try JSONEncoder().encode(items)\ntry data.write(to: fileURL)\n\n// Read\nlet savedData = try Data(contentsOf: fileURL)\nlet items = try JSONDecoder().decode([Item].self, from: savedData)',
        },
        {'type': 'heading', 'text': 'Choosing the Right Persistence Layer'},
        {
          'type': 'explanation',
          'text': 'Use UserDefaults for preferences and settings. Use Keychain for secrets. Use SwiftData for object graphs in new iOS 17+ projects. Use CoreData for complex relational models or backward compatibility. Use FileManager for files, images, and large blobs. Use SQLite directly only when you need fine-grained SQL control.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': '// Persistence decision guide:\n// - Simple key-value: UserDefaults\n// - Sensitive data: Keychain\n// - Object graph (iOS 17+): SwiftData\n// - Complex relations (all iOS): CoreData\n// - Files/images: FileManager\n// - Raw SQL control: GRDB or SQLite.swift',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'ios_data_persistence_q1',
          question: 'Which is the best use case for UserDefaults?',
          options: [
            'Storing large JSON files',
            'Storing user preferences and app settings',
            'Storing user authentication tokens securely',
            'Storing Core Data migrations',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'ios_data_persistence_q2',
          question: 'What macro defines a persistent type in SwiftData?',
          options: ['@Persist', '@Model', '@Entity', '@Storable'],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'ios_data_persistence_q3',
          question: 'Where should user-generated content be stored for automatic iCloud backup?',
          options: [
            'The tmp directory',
            'The Library directory',
            'The Documents directory',
            'The Caches directory',
          ],
          correctIndex: 2,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'ios_data_persistence_q4',
          question: 'What framework stores data in an encrypted system database on iOS?',
          options: [
            'UserDefaults',
            'CoreData',
            'Keychain Services',
            'FileManager',
          ],
          correctIndex: 2,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'ios_data_persistence_q5',
          question: 'What is a key advantage of SwiftData over CoreData?',
          options: [
            'SwiftData supports iOS 13 and later',
            'SwiftData offers type-safe queries and better SwiftUI integration',
            'SwiftData uses SQL directly',
            'SwiftData has no migration support',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'ios_data_persistence_q6',
          question: 'What does the @Relationship(deleteRule: .cascade) macro do in SwiftData?',
          options: [
            'Prevents deletion of the parent object',
            'Deletes all related child objects when the parent is deleted',
            'Ignores the relationship during deletion',
            'Moves related objects to a trash container',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'ios_data_persistence_q7',
          question: 'What is an NSManagedObjectContext in CoreData?',
          options: [
            'The database file itself',
            'An in-memory scratchpad for creating, fetching, and managing managed objects',
            'The persistence store coordinator',
            'A migration manager',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'ios_data_persistence_q8',
          question: 'How does Keychain data persist across app uninstalls?',
          options: [
            'It does not persist',
            'It persists unless explicitly deleted by the user or developer',
            'It persists only on jailbroken devices',
            'It requires iCloud backup to persist',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'ios_data_persistence_q9',
          question: 'What is the correct way to fetch data with a predicate in SwiftData?',
          codeHeader: 'SwiftData fetch',
          codeLanguage: 'Swift',
          codeLines: [
            'let descriptor = FetchDescriptor<Note>(',
            '    predicate: #Predicate { \$0.title.contains("Swift") },',
            '    sortBy: [SortDescriptor(\\Note.createdAt)]',
            ')',
            'let results = try context.fetch(descriptor)',
          ],
          options: [
            'Use NSPredicate with format strings',
            'Use #Predicate for type-safe, compile-time checked predicates',
            'Use raw SQL strings',
            'Predicates are not supported in SwiftData',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'ios_data_persistence_q10',
          question: 'When would you choose CoreData over SwiftData for a new project?',
          options: [
            'When targeting only iOS 17+',
            'When you need complex relational queries, batch operations, or backward compatibility below iOS 17',
            'When you want simpler migration handling',
            'When you prefer declarative APIs',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
      ],
    ),
    // =========================================================================
    // Module 7: The Value Semantics
    // =========================================================================
    SeedModule(
      id: 'swift_the_value_semantics',
      title: 'Swift Value Semantics & Copy-on-Write',
      description: 'Understand how Swift value types guarantee copy behavior, how the standard library uses Copy-on-Write for efficiency, and when to choose value types over reference types.',
      level: 'BEGINNER',
      learningObjectives: [
        'Explain what value semantics means and how structs, enums, and tuples behave when assigned or passed.',
        'Analyze the Copy-on-Write optimization used by Array, Dictionary, Set, and String.',
        'Implement a custom Copy-on-Write type using isKnownUniquelyReferenced.',
        'Understand how value types eliminate shared mutable state and improve thread safety.',
        'Choose between value types and reference types based on identity, inheritance, and performance needs.',
      ],
      content: [
        {'type': 'heading', 'text': 'Value Semantics Fundamentals'},
        {
          'type': 'explanation',
          'text': 'In Swift, value types (structs, enums, and tuples) are copied when assigned to another variable or passed into a function. Each variable holds its own independent copy of the data, so mutating one copy never affects another. This is the opposite of reference semantics, where classes and actors share a single instance through multiple references. Value semantics make code predictable and eliminate a whole class of shared-mutation bugs.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'struct Point {\n    var x: Double\n    var y: Double\n}\n\nvar p1 = Point(x: 1, y: 2)\nvar p2 = p1          // independent copy\np2.x = 10            // only p2 changes\nprint(p1.x)          // 1.0\nprint(p2.x)          // 10.0\n\nfunc moveByX(_ point: Point, _ delta: Double) -> Point {\n    var copy = point   // parameter is a copy too\n    copy.x += delta\n    return copy\n}',
        },
        {
          'type': 'heading',
          'text': 'Copy-on-Write (COW) in the Standard Library',
        },
        {
          'type': 'explanation',
          'text': 'Copying a large array on every assignment would be expensive. Apple\'s standard library therefore implements Copy-on-Write: Array, Dictionary, Set, and String share their underlying storage buffer across copies and only allocate a fresh buffer when you mutate one of the copies. As long as you only read, all copies reference the same buffer.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'var arrayA = [1, 2, 3, 4, 5]\nvar arrayB = arrayA     // shares the same buffer, no copy yet\narrayB.append(6)        // COW: a new buffer is allocated for arrayB\nprint(arrayA.count)     // 5 - unaffected\nprint(arrayB.count)     // 6\n\n// Reading does not trigger a copy:\nlet sum = arrayA.reduce(0, +)   // shares storage with arrayA',
        },
        {'type': 'heading', 'text': 'Implementing Custom Copy-on-Write'},
        {
          'type': 'explanation',
          'text': 'For custom value types that wrap a reference, you can implement COW manually. Store the object in an internal reference box, then check isKnownUniquelyReferenced before mutating: if the reference is held uniquely, mutate in place; otherwise, clone the storage first. This gives your type value semantics while deferring expensive copying.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'final class Storage<T> {\n    var value: T\n    init(_ value: T) { self.value = value }\n}\n\nstruct MyArray {\n    private var storage = Storage([Int]())\n\n    mutating func append(_ element: Int) {\n        if !isKnownUniquelyReferenced(&storage) {\n            storage = Storage(storage.value)\n        }\n        storage.value.append(element)\n    }\n\n    var count: Int { storage.value.count }\n}\n\nvar box1 = MyArray()\nvar box2 = box1     // shares storage\nbox2.append(42)     // box2 now owns a unique copy\nprint(box1.count)   // 0',
        },
        {'type': 'heading', 'text': 'Struct Mutation and mutating Methods'},
        {
          'type': 'explanation',
          'text': 'When you declare a struct with let, none of its properties can change. When you declare it with var, its properties can be mutated. Methods that change a struct\'s stored properties must be marked mutating. The compiler enforces value-semantics constraints at compile time, so accidental mutation is a compile error rather than a runtime bug.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'struct Counter {\n    var value = 0\n\n    // Must be mutating because it changes a stored property\n    mutating func increment() {\n        value += 1\n    }\n}\n\nvar counter = Counter()\ncounter.increment()\n\nlet frozen = Counter()\nfrozen.increment()   // compile error: cannot use mutating member\n                     // on immutable value',
        },
        {'type': 'heading', 'text': 'Value Types and Concurrency Safety'},
        {
          'type': 'explanation',
          'text': 'Because every thread that receives a value gets its own independent copy, value types are inherently safe for concurrent use. There is no shared mutable state to protect with locks, which is why structs conforming to Sendable are approved by the compiler for crossing concurrency boundaries. This is a key reason Swift encourages value semantics: they remove data races by construction.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'struct UserProfile: Sendable {\n    var name: String\n    var age: Int\n}\n\nfunc loadProfiles() async -> [UserProfile] {\n    await withTaskGroup(of: UserProfile.self) { group in\n        for username in ["alice", "bob", "carol"] {\n            group.addTask {\n                // Each task produces its own independent UserProfile.\n                await fetchProfile(username)\n            }\n        }\n        var profiles: [UserProfile] = []\n        for await profile in group {\n            profiles.append(profile)\n        }\n        return profiles\n    }\n}',
        },
        {'type': 'heading', 'text': 'Choosing Value Types vs Reference Types'},
        {
          'type': 'explanation',
          'text': 'Swift API design guidelines recommend using value types by default. Reach for a class only when you need inheritance, Objective-C interoperability, or reference identity (where two variables must refer to the same instance). Reach for an actor when you need a reference type whose state is protected from data races. Keep data in structs for predictable, testable code.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': '// Prefer a struct for data\nstruct Order {\n    let id: Int\n    let items: [String]\n    var status: String\n}\n\n// Use a class only when identity/inheritance is required\nclass AbstractRenderer {\n    func render() {}\n}\nfinal class MetalRenderer: AbstractRenderer {\n    override func render() { /* GPU work */ }\n}\n\n// Use an actor for mutable shared state that must be thread-safe\nactor OrderStore {\n    private var orders: [Int: Order] = [:]\n    func add(_ order: Order) { orders[order.id] = order }\n}',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'swift_the_value_semantics_q1',
          question: 'What does value semantics mean in Swift?',
          options: [
            'Values are copied when assigned or passed to another scope',
            'Values are shared by reference between variables',
            'Values are always stored on the heap',
            'Values cannot be declared with let',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'swift_the_value_semantics_q2',
          question: 'What does this code print?',
          codeHeader: 'Struct assignment',
          codeLanguage: 'Swift',
          codeLines: [
            'struct Point { var x: Int; var y: Int }',
            'var a = Point(x: 1, y: 2)',
            'var b = a',
            'b.x = 10',
            'print(a.x)',
          ],
          options: ['10', '1', '0', 'Compilation error'],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'swift_the_value_semantics_q3',
          question:
              'When does Copy-on-Write actually perform a copy for an Array?',
          options: [
            'Immediately when one array is assigned to another variable',
            'Every time an element is read from the array',
            'Only when a mutable copy changes and the buffer is shared with another variable',
            'Never - arrays copy eagerly like all other types',
          ],
          correctIndex: 2,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_the_value_semantics_q4',
          question: 'Which of these is a value type in Swift?',
          options: ['class', 'actor', 'struct', 'closure'],
          correctIndex: 2,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'swift_the_value_semantics_q5',
          question: 'Why is Array still memory-efficient despite its value semantics?',
          options: [
            'It uses reference counting internally and only copies when needed',
            'It implements Copy-on-Write, sharing storage until a mutation occurs',
            'It permanently stores elements on the stack',
            'It compresses its element data in memory',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_the_value_semantics_q6',
          question: 'What is the result of this code?',
          codeHeader: 'Class reference behavior',
          codeLanguage: 'Swift',
          codeLines: [
            'class Box { var value = 0 }',
            'let first = Box()',
            'let second = first',
            'second.value = 99',
            'print(first.value)',
          ],
          options: ['0', '99', 'nil', 'Compilation error'],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_the_value_semantics_q7',
          question: 'What does isKnownUniquelyReferenced(_:) return?',
          options: [
            'True when the object has exactly one strong reference to it',
            'True when the object has any number of live references',
            'True for every value type',
            'False for all class instances',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'swift_the_value_semantics_q8',
          question: 'Why are value types inherently safer for concurrent programming?',
          options: [
            'They always execute on the main thread',
            'Each thread works with its own independent copy, so there is no shared mutable state',
            'They automatically acquire a mutex when accessed',
            'They cannot be accessed from background threads',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_the_value_semantics_q9',
          question:
              'In what situation does an Array actually allocate new storage?',
          codeHeader: 'COW mutation',
          codeLanguage: 'Swift',
          codeLines: [
            'var a = [1, 2, 3, 4, 5]',
            'let b = a',
            'let c = a',
            'c.forEach { print(\$0) }',
            'a.append(6)  // <-- here',
          ],
          options: [
            'When b and c are created',
            'When forEach reads the values',
            'Only when a appends, because a still shares its buffer with b and c',
            'Never, because arrays always keep unique storage',
          ],
          correctIndex: 2,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'swift_the_value_semantics_q10',
          question:
              'Which scenario justifies using a struct instead of a class?',
          options: [
            'When you need reference identity between variables',
            'When you need Objective-C interoperability',
            'When you need inheritance from a shared base type',
            'When an instance should behave as an independent value that gets copied',
          ],
          correctIndex: 3,
          level: 'INTERMEDIATE',
        ),
      ],
    ),
    // =========================================================================
    // Module 8: Protocols & Generics
    // =========================================================================
    SeedModule(
      id: 'swift_protocols_generics',
      title: 'Protocols, Generics & Type Erasure',
      description: 'Master protocol-oriented programming with associated types, protocol composition, generic constraints, and type erasure to build flexible, reusable Swift APIs.',
      level: 'INTERMEDIATE',
      learningObjectives: [
        'Design protocols with associated types and constrain them to build generic APIs.',
        'Compose behavior using multiple protocol conformances and protocol inheritance.',
        'Apply generic constraints and where clauses to write type-safe reusable functions.',
        'Understand existential types (any Protocol) and when protocols with associated types require type erasure.',
        'Use protocol extensions with default and conditional implementations for retroactive conformance.',
      ],
      content: [
        {'type': 'heading', 'text': 'Protocol-Oriented Programming'},
        {
          'type': 'explanation',
          'text': 'Protocol-oriented programming treats protocols as the primary abstraction instead of class hierarchies. Protocols declare requirements, and protocol extensions provide default implementations. Types then conform and inherit behavior by composition, avoiding the rigidity of deep class inheritance trees.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'protocol Vehicle {\n    var name: String { get }\n    func startEngine()\n}\n\nextension Vehicle {\n    func startEngine() {\n        print("\\(name) engine started")\n    }\n}\n\nstruct Car: Vehicle {\n    let name: String\n}\n\nstruct Truck: Vehicle {\n    let name: String\n}\n\nlet car = Car(name: "Sedan")\ncar.startEngine()  // "Sedan engine started"',
        },
        {'type': 'heading', 'text': 'Associated Types in Protocols'},
        {
          'type': 'explanation',
          'text': 'An associated type is a placeholder type inside a protocol. Conforming types choose a concrete type for each associated type via a typealias or by inference. This lets protocols express relationships over kinds of data without being a concrete type themselves. IteratorProtocol, for example, uses an associated Element.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'protocol Storage {\n    associatedtype Item\n    var count: Int { get }\n    mutating func push(_ item: Item)\n    mutating func pop() -> Item?\n}\n\nstruct IntStack: Storage {\n    typealias Item = Int\n    private var items: [Int] = []\n    var count: Int { items.count }\n    mutating func push(_ item: Int) { items.append(item) }\n    mutating func pop() -> Int? { items.popLast() }\n}\n\nstruct StringStack: Storage {\n    private var items: [String] = []\n    var count: Int { items.count }\n    mutating func push(_ item: String) { items.append(item) }\n    mutating func pop() -> String? { items.popLast() }\n}',
        },
        {'type': 'heading', 'text': 'Protocol Composition'},
        {
          'type': 'explanation',
          'text': 'Protocol composition combines multiple protocols into a single constraint using the & operator. It expresses that a value conforms to several protocols at once without defining a new protocol. This is ideal for generic function parameters and for opaque return types where you need multiple capabilities.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'import CoreGraphics\n\nprotocol Drawable {\n    func draw()\n}\nprotocol Resizable {\n    func resize(to point: CGSize)\n}\n\n// Composition: the value must conform to BOTH protocols\nfunc render<T>(_ item: T) where T: Drawable & Resizable {\n    item.draw()\n    item.resize(to: CGSize(width: 100, height: 100))\n}\n\nstruct ShapeCard: Drawable, Resizable {\n    func draw() {}\n    func resize(to point: CGSize) {}\n}\n\nrender(ShapeCard())',
        },
        {'type': 'heading', 'text': 'Generic Constraints and Where Clauses'},
        {
          'type': 'explanation',
          'text': 'Generic constraints restrict which types the compiler accepts for a type parameter. The <T: Protocol> shorthand constrains a single type. where clauses express more complex constraints, such as relating two type parameters or requiring Equatable/Comparable to perform operations inside the body.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': '// Single generic constrained to Comparable\nfunc largest<T: Comparable>(_ items: [T]) -> T? {\n    return items.max()\n}\n\n// where clause relating two type parameters\nfunc merge<A, B>(_ a: A, into b: inout B)\n    where A: Sequence, B: RangeReplaceableCollection, A.Element == B.Element {\n    b.append(contentsOf: a)\n}\n\nvar target: [Int] = []\nmerge([1, 2, 3], into: &target)\nprint(target)  // [1, 2, 3]',
        },
        {'type': 'heading', 'text': 'Existential Types (any Protocol)'},
        {
          'type': 'explanation',
          'text': 'An existential type (any Animal) can store any concrete value that conforms to the protocol. Because the concrete type is hidden, calls dispatch dynamically at runtime with a small performance cost. Protocols without associated types can be used directly as existentials; protocols with associated types cannot and instead use generics or type erasure.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'protocol Speaker {\n    func speak()\n}\n\nstruct Dog: Speaker { func speak() { print("Woof") } }\nstruct Person: Speaker { func speak() { print("Hello") } }\n\n// Existential: any value conforming to Speaker\nlet speakers: [any Speaker] = [Dog(), Person()]\nfor speaker in speakers {\n    speaker.speak()\n}',
        },
        {'type': 'heading', 'text': 'Type Erasure'},
        {
          'type': 'explanation',
          'text': 'Type erasure wraps a protocol that has an associated type inside a concrete box, hiding implementation details behind a simple API. Swift does this in the standard library with AnySequence, AnyCollection, and AnyIterator. You can build the same pattern for your own protocols whenever you need to pass them around as plain values.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'protocol DataSource {\n    associatedtype Element\n    func element(at index: Int) -> Element?\n}\n\n// Type eraser: AnyDataSource hides the associated type\nstruct AnyDataSource<Element>: DataSource {\n    private let _elementAt: (Int) -> Element?\n\n    init<Source: DataSource>(_ source: Source) where Source.Element == Element {\n        _elementAt = source.element(at:)\n    }\n\n    func element(at index: Int) -> Element? {\n        _elementAt(index)\n    }\n}\n\nstruct ArrayDataSource: DataSource {\n    let values: [Int]\n    func element(at index: Int) -> Int? {\n        index < values.count ? values[index] : nil\n    }\n}\n\nlet source = AnyDataSource(ArrayDataSource(values: [1, 2, 3]))\nprint(source.element(at: 0) ?? -1)  // 1',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'swift_protocols_generics_q1',
          question: 'What is an associated type in a protocol?',
          options: [
            'A placeholder type that conforming types specify concretely',
            'A type that stores metadata about the protocol',
            'A default value declared inside the protocol',
            'A private helper type of the protocol',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'swift_protocols_generics_q2',
          question: 'What does this code print?',
          codeHeader: 'Protocol default implementation',
          codeLanguage: 'Swift',
          codeLines: [
            'protocol Greeter {',
            '    func greet() -> String',
            '}',
            'extension Greeter {',
            '    func greet() -> String { return "Hi" }',
            '}',
            'struct User: Greeter {}',
            'print(User().greet())',
          ],
          options: ['Compilation error', 'Hi', 'nil', 'Runtime crash'],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'swift_protocols_generics_q3',
          question: 'What constraint does `func f<T>(_ x: T) where T: Numeric & Hashable` impose?',
          options: [
            'T must be a class type',
            'T must conform to both Numeric and Hashable',
            'T must be either Numeric or Hashable',
            'T must be a String type',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_protocols_generics_q4',
          question: 'Why can a protocol with an associated type NOT be used directly as a standalone value type?',
          options: [
            'The compiler cannot know the concrete type, so it refuses to use it as a standalone value',
            'It is deprecated since Swift 5',
            'Associated types are only allowed on classes',
            'It causes infinite recursion at runtime',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'swift_protocols_generics_q5',
          question: 'How do you restrict an associated type in a protocol declaration?',
          options: [
            'protocol P { associatedtype T: Equatable }',
            'protocol P<T: Equatable> { }',
            'protocol P: Equatable { }',
            'protocol P requires T: Equatable { }',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_protocols_generics_q6',
          question: 'What is an existential type like `any Animal`?',
          options: [
            'A type that statically knows the concrete conforming type',
            'A box storing any value conforming to Animal, with dynamic dispatch',
            'A type that only works with classes',
            'A syntactic shortcut for generics',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'swift_protocols_generics_q7',
          question: 'What does the `where A.Element == B.Element` clause do in a generic function?',
          options: [
            'It makes A and B the same type',
            'It requires the element types of the two generic parameters to be equal',
            'It deletes elements from both collections',
            'It forces both parameters to be arrays',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_protocols_generics_q8',
          question: 'What does `func largest<T: Comparable>(_ items: [T]) -> T?` allow?',
          codeHeader: 'Generic constraint',
          codeLanguage: 'Swift',
          codeLines: [
            'func largest<T: Comparable>(_ items: [T]) -> T? {',
            '    return items.max()',
            '}',
          ],
          options: [
            'Only integers can be passed',
            'Any type that conforms to Comparable, such as Int and String',
            'Any type at all, including non-Comparable ones',
            'Only user-defined types',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_protocols_generics_q9',
          question: 'When does a protocol extension\'s default implementation get overridden?',
          options: [
            'Never - extensions always win',
            'When the conforming type provides its own implementation',
            'Only when the type is a class',
            'Only for computed properties, not methods',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_protocols_generics_q10',
          question: 'What is the purpose of a type eraser like AnySequence?',
          options: [
            'To make protocols with associated types usable as values by hiding the associated type',
            'To erase the class hierarchy for faster runtime dispatch',
            'To remove unused generic parameters from a protocol',
            'To enforce Sendable conformance at compile time',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
        ),
      ],
    ),
    // =========================================================================
    // Module 9: GCD & OperationQueue
    // =========================================================================
    SeedModule(
      id: 'swift_gcd_operations',
      title: 'Grand Central Dispatch & OperationQueue',
      description: 'Manage concurrent code with DispatchQueue, quality-of-service classes, dispatch groups, and OperationQueue dependencies for robust, responsive iOS apps.',
      level: 'INTERMEDIATE',
      learningObjectives: [
        'Use DispatchQueue to schedule serial and concurrent work on the main thread or background threads.',
        'Apply QoS classes to communicate urgency and resource priority to the system.',
        'Coordinate parallel tasks with DispatchGroup and synchronize access with barriers and semaphores.',
        'Model multi-step work with Operation subclasses and OperationQueue dependencies.',
        'Avoid common pitfalls like main-thread blocking, deadlock, and data races.',
      ],
      content: [
        {'type': 'heading', 'text': 'DispatchQueue Fundamentals'},
        {
          'type': 'explanation',
          'text': 'Grand Central Dispatch (GCD) manages a pool of worker threads and schedules blocks of work onto them. A serial queue runs one task at a time in FIFO order. A concurrent queue runs multiple tasks at once. The main queue is a serial queue that must always be used for UIKit and SwiftUI updates. Never block the main thread with heavy synchronous work.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'let serialQueue = DispatchQueue(label: "com.example.serial")\nlet concurrentQueue = DispatchQueue(label: "com.example.concurrent",\n                                    attributes: .concurrent)\n\n// Dispatch asynchronously\nserialQueue.async {\n    print("Task 1")\n}\nserialQueue.async {\n    print("Task 2 - always runs after Task 1")\n}\n\n// Main thread UI update\nDispatchQueue.main.async {\n    label.text = loadedText\n}',
        },
        {'type': 'heading', 'text': 'Quality of Service (QoS)'},
        {
          'type': 'explanation',
          'text': 'QoS classes tell the system how important your work is. The system uses this to schedule threads, throttle background work, and conserve energy. userInteractive is for UI updates and immediate responses; userInitiated for work the user is waiting on; utility for long-running tasks like importing data; and background for not-user-visible maintenance work.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'DispatchQueue.global(qos: .userInteractive).async {\n    updateUI()   // High priority, urgent\n}\n\nDispatchQueue.global(qos: .userInitiated).async {\n    loadSharedData()   // The user explicitly requested this\n}\n\nDispatchQueue.global(qos: .utility).async {\n    importLargeArchive()   // Long running, interruptible\n}\n\nDispatchQueue.global(qos: .background).async {\n    cleanUpCache()   // User does not see this\n}',
        },
        {'type': 'heading', 'text': 'DispatchGroup for Coordination'},
        {
          'type': 'explanation',
          'text': 'A DispatchGroup tracks multiple asynchronous tasks. Use enter() before each task, leave() when it finishes, and notify() to run a completion block once the count reaches zero. Use wait() sparingly because it blocks the calling thread. Groups are ideal for fan-out: download several resources in parallel, then refresh the UI once.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'let group = DispatchGroup()\nlet queue = DispatchQueue.global(qos: .userInitiated)\n\nfor url in imageURLs {\n    group.enter()\n    queue.async {\n        downloadImage(from: url)\n        group.leave()\n    }\n}\n\ngroup.notify(queue: .main) {\n    print("All images downloaded")\n    collectionView.reloadData()\n}',
        },
        {'type': 'heading', 'text': 'OperationQueue and Operation'},
        {
          'type': 'explanation',
          'text': 'OperationQueue is a higher-level layer on top of GCD. Operation encapsulates a unit of work with state (isReady, isExecuting, isFinished), cancellation support, and dependency tracking. Configure maxConcurrentOperationCount to limit parallelism. This is ideal when work needs user-visible cancellation and progress.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'final class DownloadOperation: Operation {\n    let url: URL\n    init(url: URL) { self.url = url }\n\n    override func main() {\n        guard !isCancelled else { return }\n        Thread.sleep(forTimeInterval: 1)\n        if isCancelled { return }\n        print("Downloaded \\(url.lastPathComponent)")\n    }\n}\n\nlet operationQueue = OperationQueue()\noperationQueue.maxConcurrentOperationCount = 2\n\nlet op1 = DownloadOperation(url: url1)\nlet op2 = DownloadOperation(url: url2)\noperationQueue.addOperations([op1, op2], waitUntilFinished: false)',
        },
        {'type': 'heading', 'text': 'Operation Dependencies'},
        {
          'type': 'explanation',
          'text': 'Dependencies enforce execution order: an operation does not start until all operations it depends on finish. If an operation is cancelled, its dependents are also cancelled automatically. This makes pipeline-style processing straightforward: fetch data, decode it, then present it.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'let fetchOperation = FetchDataOperation()\nlet decodeOperation = DecodeDataOperation()\nlet renderOperation = RenderDataOperation()\n\ndecodeOperation.addDependency(fetchOperation)\nrenderOperation.addDependency(decodeOperation)\n\nlet queue = OperationQueue()\nqueue.addOperations([fetchOperation, decodeOperation, renderOperation],\n                    waitUntilFinished: false)\n\n// renderOperation runs only after fetch AND decode finish.',
        },
        {'type': 'heading', 'text': 'Thread-Safety Patterns with GCD'},
        {
          'type': 'explanation',
          'text': 'GCD offers several ways to protect shared state. A serial queue serializes all mutations without locks. A concurrent queue combined with a barrier block gives concurrent reads and exclusive writes. A DispatchSemaphore limits access to a finite resource. These patterns keep state consistent and avoid data races.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'let stateQueue = DispatchQueue(label: "com.example.state",\n                                attributes: .concurrent)\nvar cachedUsers: [Int: User] = [:]\n\n// Exclusive writes with a barrier\nstateQueue.async(flags: .barrier) {\n    cachedUsers[id] = user\n}\n\n// Concurrent reads\nstateQueue.async {\n    let snapshot = cachedUsers\n    print(snapshot.count)\n}\n\n// Semaphore: allow at most 3 concurrent workers\nlet semaphore = DispatchSemaphore(value: 3)\nlet workQueue = DispatchQueue.global()\nworkQueue.async {\n    semaphore.wait()\n    process(index: 0)\n    semaphore.signal()\n}',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'swift_gcd_operations_q1',
          question: 'Which DispatchQueue must be used for UI updates?',
          options: [
            'Any concurrent queue',
            'DispatchQueue.main',
            'DispatchQueue.global(qos: .background)',
            'A custom serial queue',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'swift_gcd_operations_q2',
          question: 'What is a serial DispatchQueue?',
          options: [
            'A queue that executes one task at a time in FIFO order',
            'A queue that runs each task on a separate thread',
            'A queue that only accepts blocking calls',
            'A queue reserved for the main thread',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'swift_gcd_operations_q3',
          question: 'What does notify(queue:) on a DispatchGroup do?',
          options: [
            'Blocks the current thread until the group finishes',
            'Schedules a block to run when all tasks in the group have finished',
            'Cancels all pending tasks in the group',
            'Runs immediately when the group is created',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_gcd_operations_q4',
          question: 'What is the key difference between sync and async on a DispatchQueue?',
          codeHeader: 'Sync vs async',
          codeLanguage: 'Swift',
          codeLines: [
            'let queue = DispatchQueue(label: "com.example.work")',
            'queue.sync { print("a") }',
            'queue.async { print("b") }',
          ],
          options: [
            'sync blocks the calling thread until the block runs; async schedules and returns immediately',
            'async blocks the calling thread until the block runs; sync returns immediately',
            'Both always block until completion',
            'sync runs on the main thread; async runs in the background',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_gcd_operations_q5',
          question: 'What happens if you call DispatchQueue.main.sync from the main thread?',
          options: [
            'It runs the block on the main thread',
            'The app deadlocks because the main thread waits for itself',
            'It moves the block to a background queue',
            'It throws a compile-time error',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'swift_gcd_operations_q6',
          question: 'How do you add a dependency so opB runs only after opA finishes?',
          options: [
            'opA.dependency = opB',
            'opB.addDependency(opA)',
            'queue.addDependency(opA, to: opB)',
            'OperationQueue.link(opB, after: opA)',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_gcd_operations_q7',
          question: 'Which QoS class has the HIGHEST scheduling priority?',
          options: ['background', 'utility', 'userInteractive', 'default'],
          correctIndex: 2,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'swift_gcd_operations_q8',
          question: 'Why use OperationQueue over plain GCD?',
          options: [
            'GCD always blocks the main thread',
            'OperationQueue adds dependencies, cancellation, and max-concurrency control',
            'GCD cannot dispatch to background queues',
            'OperationQueue runs only on the main thread',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_gcd_operations_q9',
          question:
              'What does a barrier block on a concurrent queue guarantee?',
          options: [
            'It runs read blocks first, then write blocks',
            'It runs exclusively: blocks submitted earlier finish first, and blocks submitted later wait',
            'It executes the block twice for safety',
            'It promotes the queue to a serial queue permanently',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'swift_gcd_operations_q10',
          question: 'What is DispatchSemaphore used for?',
          options: [
            'Limiting access to a finite number of resources',
            'Creating new threads',
            'Scheduling work on the main queue',
            'Encrypting data in transit',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
      ],
    ),
    // =========================================================================
    // Module 10: SwiftUI Architecture
    // =========================================================================
    SeedModule(
      id: 'swift_ui_architecture',
      title: 'SwiftUI App Architecture',
      description: 'Design scalable SwiftUI apps with ObservableObject, the @Observable macro, MVVM, Coordinator-based navigation, and dependency injection patterns.',
      level: 'ADVANCED',
      learningObjectives: [
        'Compare ObservableObject with the @Observable macro and choose based on your minimum iOS target.',
        'Structure apps with MVVM separating view state, business logic, and data models.',
        'Manage complex navigation with Coordinator objects and NavigationPath.',
        'Inject dependencies through initializers and the SwiftUI environment to keep views testable.',
        'Apply performance and identity best practices to keep SwiftUI re-rendering minimal and correct.',
      ],
      content: [
        {'type': 'heading', 'text': 'SwiftUI App Lifecycle'},
        {
          'type': 'explanation',
          'text': 'SwiftUI replaces UIApplicationDelegate with the @main App protocol. The Scene (usually WindowGroup) declares the root view tree. Lifecycle modifiers such as .onAppear and task { } hook into appearance. The environment carries system values like colorScheme and locale, plus any custom values you inject. Keep app setup minimal and push initialization into view models.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': '@main\nstruct MyApp: App {\n    var body: some Scene {\n        WindowGroup {\n            AppRootView()\n                .environment(\\.colorScheme, .light)\n        }\n    }\n}\n\nstruct AppRootView: View {\n    @StateObject private var appModel = AppModel()\n\n    var body: some View {\n        ContentView()\n            .environmentObject(appModel)\n            .task {\n                await appModel.bootstrap()\n            }\n    }\n}',
        },
        {'type': 'heading', 'text': 'ObservableObject and @Published'},
        {
          'type': 'explanation',
          'text': 'ObservableObject is a Combine-backed protocol for reference-type view models. Properties marked @Published emit change notifications. Views subscribe with @StateObject (owns the object) or @ObservedObject (does not own it). This has been the standard MVVM mechanism since iOS 13, though it re-renders a view whenever any @Published property changes, even if that property is not read by the view.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'final class HomeViewModel: ObservableObject {\n    @Published var items: [Item] = []\n    @Published var isLoading = false\n    @Published var errorMessage: String?\n\n    private let service: ItemService\n\n    init(service: ItemService) {\n        self.service = service\n    }\n\n    func load() async {\n        isLoading = true\n        defer { isLoading = false }\n        do {\n            items = try await service.fetchItems()\n        } catch {\n            errorMessage = error.localizedDescription\n        }\n    }\n}',
        },
        {'type': 'heading', 'text': 'The @Observable Macro (Swift 5.9+)'},
        {
          'type': 'explanation',
          'text': 'Introduced with iOS 17 and Swift 5.9, the @Observable macro tracks property access instead of whole-object subscriptions. Views only re-render when the properties they actually read change, removing the @Published boilerplate and cutting unnecessary view updates. In the view, declare a plain var viewModel: MyModel instead of @StateObject/@ObservedObject wrappers.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'import Observation\n\n@Observable\nfinal class LibraryModel {\n    var books: [Book] = []\n    var selectedBookID: Int?\n    var isLoading = false\n}\n\nstruct LibraryView: View {\n    var model: LibraryModel\n\n    var body: some View {\n        List(model.books) { book in\n            Text(book.title)\n            // This view only subscribes to the properties it reads\n        }\n    }\n}',
        },
        {'type': 'heading', 'text': 'MVVM in SwiftUI'},
        {
          'type': 'explanation',
          'text': 'MVVM splits the app into three layers. Model holds data. ViewModel holds the state and business rules the UI needs, exposing it through published/observable properties. View renders declaratively and forwards user actions to the ViewModel. This separation keeps views thin, makes business logic unit-testable without UI, and matches SwiftUI\'s observation model naturally.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'struct Note: Identifiable {\n    let id: UUID\n    var title: String\n}\n\n@Observable\nfinal class NotesViewModel {\n    var notes: [Note] = []\n    var searchText = ""\n\n    var filteredNotes: [Note] {\n        searchText.isEmpty ? notes : notes.filter { \$0.title.localizedCaseInsensitiveContains(searchText) }\n    }\n}\n\nstruct NotesView: View {\n    var viewModel: NotesViewModel\n\n    var body: some View {\n        List(viewModel.filteredNotes) { note in\n            Text(note.title)\n        }\n        .searchable(text: \$viewModel.searchText)\n    }\n}',
        },
        {'type': 'heading', 'text': 'The Coordinator Pattern'},
        {
          'type': 'explanation',
          'text': 'Coordinators own navigation state and present destination views programmatically. By holding the NavigationPath, sheets, and alerts, a coordinator moves navigation concerns out of the view body. This makes deep links, complex flows, and testing far easier than scattering navigationDestination modifiers through views.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'enum AppRoute: Hashable {\n    case detail(Item)\n    case settings\n}\n\n@Observable\nfinal class AppCoordinator {\n    var path = NavigationPath()\n\n    func pushDetail(_ item: Item) {\n        path.append(AppRoute.detail(item))\n    }\n    func presentSettings() {\n        path.append(AppRoute.settings)\n    }\n    func popToRoot() {\n        path.removeLast(path.count)\n    }\n}\n\nstruct CoordinatorView: View {\n    var coordinator: AppCoordinator\n\n    var body: some View {\n        NavigationStack(path: \$coordinator.path) {\n            ItemListView(coordinator: coordinator)\n                .navigationDestination(for: AppRoute.self) { route in\n                    switch route {\n                    case .detail(let item):\n                        DetailView(item: item)\n                    case .settings:\n                        SettingsView()\n                    }\n                }\n        }\n        .environment(coordinator)\n    }\n}',
        },
        {'type': 'heading', 'text': 'Dependency Injection in SwiftUI'},
        {
          'type': 'explanation',
          'text': 'Inject dependencies through initializer parameters or the environment so views never reach into singletons. Constructor injection is explicit and great for testability. Environment objects and custom EnvironmentKey values propagate shared services down the tree, matching SwiftUI\'s declarative flow. Avoid hidden global state that makes views impossible to test.',
        },
        {
          'type': 'code',
          'language': 'Swift',
          'code': 'protocol ItemService {\n    func fetchItems() async throws -> [Item]\n}\n\nstruct ItemListView: View {\n    @Environment(ItemService.self) private var service\n\n    var body: some View {\n        Text("Items")\n    }\n}\n\n// Provide the dependency at the root:\n#Preview {\n    ItemListView()\n        .environment(MockItemService())\n}',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'swift_ui_architecture_q1',
          question: 'What is the difference between @StateObject and @ObservedObject?',
          options: [
            '@StateObject owns the observable object; @ObservedObject does not take ownership',
            '@ObservedObject owns the object; @StateObject does not',
            'They are identical in behavior',
            '@StateObject works only with UIKit',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'swift_ui_architecture_q2',
          question: 'What does the @Observable macro (iOS 17+) improve over ObservableObject?',
          options: [
            'It supports iOS 13 and later',
            'It tracks the properties a view reads so views re-render without @Published',
            'It makes classes into value types',
            'It removes the need for the @main App struct',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_ui_architecture_q3',
          question: 'In MVVM, which layer should contain networking and business logic?',
          options: ['View', 'ViewModel', 'Model', 'SceneDelegate'],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'swift_ui_architecture_q4',
          question:
              'Which type holds navigation state in the Coordinator pattern?',
          options: [
            'A navigationDestination modifier',
            'An @Observable coordinator that owns a NavigationPath',
            'The AppDelegate',
            'An extension on UIView',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_ui_architecture_q5',
          question: 'What does the .environment(key) modifier do?',
          codeHeader: 'Environment injection',
          codeLanguage: 'Swift',
          codeLines: [
            'ItemListView()',
            '    .environment(ItemService.self, MockItemService())',
          ],
          options: [
            'It sets a default property on the view struct',
            'It injects a value into the SwiftUI environment so descendant views can read it',
            'It configures the app\'s Info.plist',
            'It changes the view\'s identity',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_ui_architecture_q6',
          question: 'What triggers a SwiftUI view body to be re-evaluated?',
          options: [
            'Any change to the device hardware',
            'A change in state the view reads, through @State, observable objects, or the environment',
            'A change to the view\'s color scheme only',
            'Every run loop iteration regardless of state',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'swift_ui_architecture_q7',
          question: 'Which pattern best separates navigation from view rendering in a large SwiftUI app?',
          options: [
            'Putting all navigation links inside one big ContentView',
            'Using a Coordinator that owns NavigationPath and pushes routes programmatically',
            'Always using UIStoryboard segues',
            'Triggering navigation from the ViewModel with NotificationCenter',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_ui_architecture_q8',
          question: 'Why should heavy computation be avoided inside `body`?',
          options: [
            'SwiftUI forbids methods inside body',
            'body runs on the main thread and is re-evaluated frequently, so heavy work freezes the UI',
            'It makes the app use too much battery while idle',
            'It prevents the view from conforming to View',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'swift_ui_architecture_q9',
          question:
              'How does the @Observable macro notify SwiftUI about changes?',
          options: [
            'It uses KVO notifications',
            'The Observation framework tracks which properties are accessed during body evaluation',
            'It posts NotificationCenter events once per second',
            'It re-renders the entire WindowGroup on every change',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'swift_ui_architecture_q10',
          question: 'Why do teams prefer constructor injection over singletons in SwiftUI?',
          options: [
            'Singleton injection makes tests impossible to isolate',
            'Constructor injection makes dependencies explicit and testable',
            'Constructor injection is faster at runtime',
            'Singletons work only in UIKit',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
      ],
    ),
  ],
  flipcards: [
    SeedFlipcard(
      id: 'swift_flip_optionals',
      term: 'Optionals',
      definition: 'A type that can hold either a value or nil. Declared with a ? suffix. Swift forces safe unwrapping via if-let, guard, nil-coalescing, or force unwrap (!). This eliminates null pointer exceptions at compile time.',
      codeHeader: 'Optional types',
      codeLanguage: 'Swift',
      codeLines: [
        'var name: String? = "Alice"',
        'var nothing: String? = nil',
        'let safe = name ?? "default"',
      ],
    ),
    SeedFlipcard(
      id: 'swift_flip_guard',
      term: 'guard let',
      definition: 'Unwraps an optional and requires an early exit (return, break, continue, throw) if nil. Unlike if-let, the unwrapped value is available for the rest of the enclosing scope, making it ideal for preconditions and validation.',
      codeHeader: 'Guard statement',
      codeLanguage: 'Swift',
      codeLines: [
        'func process(_ input: String?) {',
        '    guard let input = input else { return }',
        '    print(input)',
        '}',
      ],
    ),
    SeedFlipcard(
      id: 'swift_flip_actor',
      term: 'Actor',
      definition: 'A reference type that protects mutable state by ensuring only one task accesses it at a time. Actor methods are isolated by default, requiring await from external callers. This replaces locks for thread-safe state management.',
      codeHeader: 'Actor',
      codeLanguage: 'Swift',
      codeLines: [
        'actor Counter {',
        '    var value = 0',
        '    func increment() { value += 1 }',
        '}',
      ],
    ),
    SeedFlipcard(
      id: 'swift_flip_asyncawait',
      term: 'async/await',
      definition: 'Swift concurrency syntax for asynchronous code. An async function can suspend at await points, allowing the runtime to resume when ready. Replaces completion handlers with linear, readable code flow.',
      codeHeader: 'Async function',
      codeLanguage: 'Swift',
      codeLines: [
        'func fetch() async throws -> Data {',
        '    let (data, _) = try await URLSession.shared.data(from: url)',
        '    return data',
        '}',
      ],
    ),
    SeedFlipcard(
      id: 'swift_flip_sendable',
      term: 'Sendable',
      definition: 'A marker protocol indicating a type is safe to send across concurrency boundaries. Structs and enums with Sendable properties conform automatically. Classes must explicitly opt in with @unchecked Sendable. Enforced at compile time to prevent data races.',
    ),
    SeedFlipcard(
      id: 'swift_flip_arc',
      term: 'ARC (Automatic Reference Counting)',
      definition: 'Compile-time memory management for classes in Swift. Each strong reference increments the reference count. When the count reaches zero, the object is immediately deallocated. Does not apply to structs or enums which are value types.',
      codeHeader: 'Reference counting',
      codeLanguage: 'Swift',
      codeLines: [
        'class Foo {',
        '    deinit { print("deallocated") }',
        '}',
        'var ref: Foo? = Foo()',
        'ref = nil  // deallocated',
      ],
    ),
    SeedFlipcard(
      id: 'swift_flip_weak_unowned',
      term: 'weak and unowned references',
      definition: 'Weak references are optional and automatically set to nil when the object is deallocated. Use them to break retain cycles. Unowned references are non-optional and crash if the object is nil. Use when the reference always outlives the target.',
      codeHeader: 'Weak reference',
      codeLanguage: 'Swift',
      codeLines: [
        'class Parent {',
        '    var child: Child?',
        '}',
        'class Child {',
        '    weak var parent: Parent?',
        '}',
      ],
    ),
    SeedFlipcard(
      id: 'swift_flip_struct_class',
      term: 'Struct vs Class',
      definition: 'Structs are value types (copied on assignment), support no inheritance, and are thread-safe by default. Classes are reference types (shared reference), support inheritance, and require ARC for memory management. Swift prefers structs unless you need reference semantics.',
      codeHeader: 'Value vs reference',
      codeLanguage: 'Swift',
      codeLines: [
        'struct Point { var x: Int }',
        'var a = Point(x: 1)',
        'var b = a  // copy',
        'b.x = 2   // a.x still 1',
      ],
    ),
    SeedFlipcard(
      id: 'swift_flip_codable',
      term: 'Codable',
      definition: 'A type alias for Encodable & Decodable. Swift synthesizes conformance automatically for structs with Codable properties. Use CodingKeys to map property names to different JSON keys. Supports custom date and key decoding strategies.',
      codeHeader: 'Codable conformance',
      codeLanguage: 'Swift',
      codeLines: [
        'struct User: Codable {',
        '    let id: Int',
        '    let name: String',
        '}',
      ],
    ),
    SeedFlipcard(
      id: 'swift_flip_swiftui_state',
      term: '@State in SwiftUI',
      definition: 'A property wrapper that stores view-owned local state. When @State changes, SwiftUI re-renders the view. It is value-type semantics: the view creates and owns the storage. Combine with @Binding for child views to modify parent state.',
      codeHeader: 'SwiftUI state',
      codeLanguage: 'Swift',
      codeLines: [
        'struct Counter: View {',
        '    @State private var count = 0',
        '    var body: some View {',
        '        Button("Count: \\(count)") { count += 1 }',
        '    }',
        '}',
      ],
    ),
    SeedFlipcard(
      id: 'swift_flip_mvvm',
      term: 'MVVM (Model-View-ViewModel)',
      definition: 'An architectural pattern where the View observes a ViewModel (ObservableObject) that exposes state. The View binds to @Published properties. The ViewModel handles business logic and data fetching, keeping Views thin. Works well with SwiftUI\'s declarative paradigm.',
      codeHeader: 'MVVM pattern',
      codeLanguage: 'Swift',
      codeLines: [
        'class HomeViewModel: ObservableObject {',
        '    @Published var items: [Item] = []',
        '    func load() async {',
        '        items = await service.fetch()',
        '    }',
        '}',
      ],
    ),
    SeedFlipcard(
      id: 'swift_flip_retain_cycle',
      term: 'Retain cycle',
      definition: 'A memory leak where two objects hold strong references to each other, preventing ARC from deallocating either. Common in closures capturing self strongly or circular delegate relationships. Fix with [weak self] or [unowned self] capture lists.',
      codeHeader: 'Retain cycle fix',
      codeLanguage: 'Swift',
      codeLines: [
        'class VC: UIViewController {',
        '    var handler: (() -> Void)?',
        '    func setup() {',
        '        handler = { [weak self] in',
        '            self?.update()',
        '        }',
        '    }',
        '}',
      ],
    ),
    SeedFlipcard(
      id: 'swift_flip_cow',
      term: 'Copy-on-Write (COW)',
      definition: 'An optimization where a value type shares its underlying storage buffer across copies until one of them is mutated. Used by Array, Dictionary, Set, and String so value semantics come without eager copying cost. On mutation, a fresh buffer is allocated for the writing copy.',
      codeHeader: 'COW in action',
      codeLanguage: 'Swift',
      codeLines: [
        'var a = [1, 2, 3]',
        'var b = a           // shares buffer',
        'b.append(4)         // new buffer for b',
        'print(a.count)      // 3',
      ],
    ),
    SeedFlipcard(
      id: 'swift_flip_protocol_associatedtype',
      term: 'associatedtype',
      definition: 'A placeholder type declared inside a protocol that conforming types fill in with a concrete type, either via a typealias or by inference. Enables protocols to be generic over data without being a concrete type. Example: IteratorProtocol\'s associated Element.',
      codeHeader: 'Associated type',
      codeLanguage: 'Swift',
      codeLines: [
        'protocol Container {',
        '    associatedtype Item',
        '    func add(_ item: Item)',
        '}',
      ],
    ),
    SeedFlipcard(
      id: 'swift_flip_dispatchqos',
      term: 'DispatchQueue QoS',
      definition: 'A Quality of Service class that tells the system the importance of a task so it can prioritize scheduling and energy use. The classes, from highest to lowest priority: userInteractive, userInitiated, default, utility, and background.',
      codeHeader: 'QoS classes',
      codeLanguage: 'Swift',
      codeLines: [
        'DispatchQueue.global(qos: .userInteractive).async { updateUI() }',
        'DispatchQueue.global(qos: .utility).async { importData() }',
      ],
    ),
    SeedFlipcard(
      id: 'swift_flip_operationqueue',
      term: 'OperationQueue',
      definition: 'A higher-level concurrency API built on GCD. Executes Operation objects with features like cancellation, dependencies, priority, and maxConcurrentOperationCount. Choose it over raw DispatchQueue when work is cancellable or has ordering requirements.',
      codeHeader: 'Operation dependencies',
      codeLanguage: 'Swift',
      codeLines: [
        'let decode = DecodeOperation()',
        'decode.addDependency(fetchOperation)',
        'queue.addOperation(decode)',
      ],
    ),
    SeedFlipcard(
      id: 'swift_flip_mainactor',
      term: '@MainActor',
      definition: 'A global actor that isolates all of its methods and properties to the main thread. Marking a class @MainActor guarantees UI-facing state is accessed and updated on the main thread, eliminating many UI concurrency bugs at compile time.',
      codeHeader: 'Main actor',
      codeLanguage: 'Swift',
      codeLines: [
        '@MainActor',
        'final class HomeViewModel {',
        '    var items: [Item] = []',
        '}',
      ],
    ),
    SeedFlipcard(
      id: 'swift_flip_taskgroups',
      term: 'Task Group',
      definition: 'A structured concurrency mechanism that launches multiple child tasks and collects their results. Child tasks are tied to the group scope: when the scope exits they are awaited and cancelled automatically. Ideal for fan-out work like parallel downloads.',
      codeHeader: 'Task group',
      codeLanguage: 'Swift',
      codeLines: [
        'await withTaskGroup(of: Data.self) { group in',
        '    for url in urls {',
        '        group.addTask { try await self.download(url) }',
        '    }',
        '}',
      ],
    ),
    SeedFlipcard(
      id: 'swift_flip_keypaths',
      term: 'KeyPath',
      definition: 'A type-safe reference to a property usable across different instances, written as \\Type.property. KeyPaths enable generic programming over properties, sort descriptors like SortDescriptor(\\Note.createdAt), and Kotlin-style property observation without stringly-typed keys.',
      codeHeader: 'KeyPath',
      codeLanguage: 'Swift',
      codeLines: [
        'let keyPath = \\CGRect.size.width',
        'let width = rect[keyPath: keyPath]',
        'SortDescriptor(\\Note.createdAt)',
      ],
    ),
    SeedFlipcard(
      id: 'swift_flip_result_builders',
      term: 'Result Builder',
      definition: 'A feature (introduced with Swift 5.4) that converts a sequence of expressions into a composite value. @ViewBuilder, @SceneBuilder, and custom @resultBuilder types build declarative trees from lists of clauses, enabling the if/switch syntax of SwiftUI bodies.',
      codeHeader: 'Result builder',
      codeLanguage: 'Swift',
      codeLines: [
        '@ViewBuilder var body: some View {',
        '    if items.isEmpty {',
        '        EmptyView()',
        '    } else {',
        '        Text("\\(items.count) items")',
        '    }',
        '}',
      ],
    ),
    SeedFlipcard(
      id: 'swift_flip_viewbuilder',
      term: '@ViewBuilder',
      definition: 'A result builder applied to SwiftUI body and container closures. It upgrades to BuildPartialBlock (Swift 5.9) and composes up to ten view expressions into a single value. This is what lets SwiftUI bodies contain if/else and switch statements.',
      codeHeader: 'ViewBuilder',
      codeLanguage: 'Swift',
      codeLines: [
        '@ViewBuilder',
        'func header() -> some View {',
        '    Text("Title").font(.title)',
        '    Divider()',
        '}',
      ],
    ),
    SeedFlipcard(
      id: 'swift_flip_property_wrappers',
      term: 'Property Wrapper',
      definition: 'A wrapper type (declared with @propertyWrapper) that adds storage behavior to a property. SwiftUI relies on them for @State, @Binding, @Published, and @Environment. The wrappedValue getter/setter transform reads and writes before reaching backing storage.',
      codeHeader: 'Property wrapper',
      codeLanguage: 'Swift',
      codeLines: [
        '@propertyWrapper',
        'struct Trimmed {',
        '    private var value: String = ""',
        '    var wrappedValue: String {',
        '        get { value }',
        '        set { value = newValue.trimmingCharacters(in: .whitespaces) }',
        '    }',
        '}',
      ],
    ),
    SeedFlipcard(
      id: 'swift_flip_some_return',
      term: 'some (opaque return type)',
      definition: 'An opaque return type that hides a function\'s concrete type while fixing it to one specific type at compile time. Used everywhere in SwiftUI (body -> some View). Unlike `any`, the underlying type is known to the compiler, enabling fast static dispatch and preserving type identity.',
      codeHeader: 'Opaque return',
      codeLanguage: 'Swift',
      codeLines: [
        'func makeContent() -> some View {',
        '    Text("Hello")',
        '}',
      ],
    ),
    SeedFlipcard(
      id: 'swift_flip_dispatchgroup',
      term: 'DispatchGroup',
      definition: 'A GCD mechanism that tracks a set of tasks and executes a completion block when they all finish. Call enter() before starting each task and leave() in its completion callback. notify(queue:) runs a block once the group is empty without blocking.',
      codeHeader: 'Dispatch group',
      codeLanguage: 'Swift',
      codeLines: [
        'let group = DispatchGroup()',
        'group.enter()',
        'queue.async {',
        '    download()',
        '    group.leave()',
        '}',
        'group.notify(queue: .main) { refresh() }',
      ],
    ),
  ],
);
