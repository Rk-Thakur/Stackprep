import '../seed_models.dart';

final SeedTrack kotlinTrack = SeedTrack(
  id: 'KOTLIN',
  name: 'Kotlin',
  description: 'Master Kotlin and modern Android development: language fundamentals, coroutines and structured concurrency, functional collections, Jetpack Compose, MVVM architecture, and production-grade testing with Gradle.',
  category: 'Mobile',
  color: 0xFF8B5CF6,
  order: 0,
  modules: [
    SeedModule(
      id: 'kotlin_coroutines',
      title: 'Kotlin Coroutines & Structured Concurrency',
      description: 'Learn how Kotlin coroutines provide lightweight concurrency, structured lifecycle management, and reactive data streams through Flow, StateFlow, and SharedFlow.',
      level: 'INTERMEDIATE',
      learningObjectives: [
        'Understand the difference between coroutines and threads',
        'Use launch and async builders to start concurrent work',
        'Apply Dispatchers.Main, Dispatchers.IO, and Dispatchers.Default correctly',
        'Implement structured concurrency with CoroutineScope and SupervisorJob',
        'Build reactive streams using Flow, StateFlow, and SharedFlow',
      ],
      content: [
        {'type': 'heading', 'text': 'Coroutines vs Threads'},
        {
          'type': 'explanation',
          'text': 'Threads are heavyweight OS-level constructs, each consuming roughly 1 MB of stack memory. Kotlin coroutines are cooperative tasks multiplexed over a small pool of threads by the Kotlin runtime. A single thread can host thousands of coroutines, and because coroutines suspend rather than block, they are ideal for high-concurrency I/O without the cost of thread creation or expensive context switching.',
        },
        {'type': 'heading', 'text': 'Launching Coroutines'},
        {
          'type': 'explanation',
          'text': 'The launch builder starts a fire-and-forget coroutine and returns a Job. The async builder returns a Deferred (which extends Job) that produces a value you retrieve with await(). Both require a CoroutineScope that owns the lifecycle. When the scope is cancelled, every coroutine launched inside it is cancelled as well.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'viewModelScope.launch {\n    val users = repository.getUsers()\n    _uiState.value = UiState.Success(users)\n}\n\n// async returns a Deferred result\nval result = scope.async { computeExpensiveValue() }\nval value = result.await()',
        },
        {'type': 'heading', 'text': 'Dispatchers'},
        {
          'type': 'explanation',
          'text': 'A Dispatcher controls which thread pool a coroutine runs on. Dispatchers.Main maps to the Android main thread and is used for UI work. Dispatchers.IO is a cached pool for blocking disk and network operations. Dispatchers.Default is sized to the CPU core count and suits CPU-bound work. Use withContext to hop between dispatchers without blocking the caller.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'suspend fun fetchUser(): User {\n    return withContext(Dispatchers.IO) {\n        api.getUser()  // network call on IO threads\n    }\n}\n\nviewModelScope.launch {          // Main dispatcher\n    val user = fetchUser()       // suspends, resumes on Main\n    _user.value = user\n}',
        },
        {'type': 'heading', 'text': 'Structured Concurrency'},
        {
          'type': 'explanation',
          'text': 'Structured concurrency binds every coroutine to the scope that created it. Cancelling the scope cancels all children. By default a failing child cancels its parent and all siblings. A SupervisorJob isolates failures so one child crashing does not cancel its siblings. viewModelScope already installs a SupervisorJob, so children failures do not cancel the scope itself.',
        },
        {'type': 'heading', 'text': 'Exception Handling'},
        {
          'type': 'explanation',
          'text': 'Unhandled exceptions in launched coroutines crash the application unless a CoroutineExceptionHandler is installed at the scope level. For async builders, exceptions are deferred and surface when you call await(). Because structured concurrency propagates failures to parents, install handlers on the outermost scope and let child failures bubble up predictably.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'val handler = CoroutineExceptionHandler { _, exception ->\n    Log.e("Coroutine", "Caught: \${exception.message}")\n}\n\nviewModelScope.launch(handler) {\n    throw RuntimeException("Something failed")\n}',
        },
        {'type': 'heading', 'text': 'Flows and StateFlow'},
        {
          'type': 'explanation',
          'text': 'A Flow is a cold asynchronous stream that emits multiple values sequentially. StateFlow is a hot Flow that always holds a current value and replays it to new collectors, which makes it perfect for observable UI state in MVVM. SharedFlow is a hot broadcaster with configurable replay. Operators such as debounce, mapLatest, and combine enable powerful reactive pipelines.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'val results: StateFlow<List<Item>> = _query\n    .debounce(300)\n    .flatMapLatest { q -> searchRepository.search(q) }\n    .stateIn(\n        scope = viewModelScope,\n        started = SharingStarted.WhileSubscribed(5000),\n        initialValue = emptyList()\n    )',
        },
        {'type': 'heading', 'text': 'Cooperative Cancellation'},
        {
          'type': 'explanation',
          'text': 'Cancelling a Job is cooperative: the coroutine stops at the next suspension point such as delay() or yield(). CPU-bound loops that never suspend will not be interrupted, so call ensureActive() or check isActive inside long-running loops. This guarantees clean, prompt cancellation without leaking work.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'viewModelScope.launch {\n    val job = launch(Dispatchers.Default) {\n        (0 until 1_000_000).forEach { index ->\n            ensureActive()\n            processItem(index)\n        }\n    }\n    delay(5000)\n    job.cancel()  // cooperative cancellation\n}',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'kotlin_coroutines_q1',
          question: 'What is the primary advantage of coroutines over threads in Kotlin?',
          options: [
            'Coroutines always run on separate CPU cores for true parallelism',
            'Coroutines are lightweight and multiplexed over threads with minimal memory overhead',
            'Coroutines eliminate the need for any synchronization primitives',
            'Coroutines run only on the main thread for simplicity',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'kotlin_coroutines_q2',
          question: 'Which coroutine builder returns a Deferred that produces a result?',
          options: ['launch', 'runBlocking', 'async', 'withContext'],
          correctIndex: 2,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'kotlin_coroutines_q3',
          question: 'Which Dispatcher should you use for a CPU-intensive computation?',
          options: [
            'Dispatchers.Main',
            'Dispatchers.IO',
            'Dispatchers.Default',
            'Dispatchers.Unconfined',
          ],
          correctIndex: 2,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_coroutines_q4',
          question: 'What is the key behavior of a StateFlow compared to a plain Flow?',
          options: [
            'StateFlow is cold and lazy, emitting only when subscribed',
            'StateFlow always holds a current value and replays the latest value to new collectors',
            'StateFlow is stateless and cannot hold a value',
            'StateFlow requires manual collection on the IO dispatcher',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_coroutines_q5',
          question:
              'What does a SupervisorJob accomplish in structured concurrency?',
          options: [
            'It forces all child coroutines to run on the main thread',
            'It isolates child failures so one failing child does not cancel its siblings',
            'It makes coroutines run in parallel across multiple threads',
            'It automatically retries failed coroutines',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_coroutines_q6',
          question: 'When is an exception from an async builder surfaced to the caller?',
          options: [
            'Immediately when the coroutine throws',
            'At the scope creation time',
            'Only when the Deferred result is awaited',
            'Never, because async swallows exceptions',
          ],
          correctIndex: 2,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_coroutines_q7',
          question: 'What is the purpose of the CoroutineExceptionHandler?',
          options: [
            'To catch exceptions thrown inside a try-catch block',
            'To handle uncaught exceptions at the scope level for launched coroutines',
            'To convert exceptions into sealed Error classes',
            'To retry failed network requests automatically',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'kotlin_coroutines_q8',
          question:
              'Why must CPU-bound loops call ensureActive() or check isActive?',
          options: [
            'Because coroutines can only be cancelled at suspension points',
            'Because it increases the thread priority',
            'Because it enables parallelism across cores',
            'Because it prevents memory leaks automatically',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'kotlin_coroutines_q9',
          question: 'Which Flow operator would you call before a search to avoid launching a request on every keystroke?',
          options: ['map', 'filter', 'debounce', 'catch'],
          correctIndex: 2,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_coroutines_q10',
          question: 'Assuming Dispatchers.Default, on how many threads will a coroutine run at any single point in time?',
          options: [
            'Exactly one thread',
            'One thread per suspension point',
            'Two threads concurrently while suspended',
            'All cores simultaneously during delay()',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
          codeHeader: '[CALLSITE]',
          codeLanguage: 'Kotlin',
          codeLines: [
            'scope.launch(Dispatchers.Default) {',
            '    val data = loadData()',
            '    process(data)',
            '}',
          ],
        ),
      ],
    ),
    SeedModule(
      id: 'kotlin_language',
      title: 'Kotlin Language Essentials',
      description: 'Build a solid foundation in the Kotlin language: null safety, data classes, sealed classes, scope functions, type inference, and idiomatic constructs.',
      level: 'BEGINNER',
      learningObjectives: [
        'Use null safety features such as ?. !!. and the safe-call operators',
        'Define and use data classes, sealed classes, and enums',
        'Apply the five scope functions let, run, apply, also, and with',
        'Understand type inference, val vs var, and type systems',
        'Write idiomatic Kotlin using string templates and smart casts',
      ],
      content: [
        {'type': 'heading', 'text': 'Val and Var'},
        {
          'type': 'explanation',
          'text': 'The val keyword declares an immutable reference whose value cannot be reassigned after initialization, similar to a final field in Java. The var keyword allows reassignment. Prefer val by default. The compiler enforces this at compile time, which greatly reduces accidental state mutation and makes the code easier to reason about.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'val name: String = "Kotlin"  // immutable\nvar counter: Int = 0          // mutable\ncounter++                     // allowed\n// name = "Java"              // compile error',
        },
        {'type': 'heading', 'text': 'Null Safety'},
        {
          'type': 'explanation',
          'text': 'Kotlin distinguishes nullable and non-nullable types at compile time. A type like String can never be null, while String? explicitly allows null. The safe call ?. returns null if the receiver is null, the elvis operator ?: supplies a default, and !! asserts a non-null value. Together these features shift null checks from runtime to compile time.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'val length: Int? = text?.length   // safe call\nval len: Int = text?.length ?: 0   // elvis default\nval name = user?.name ?: "Unknown"\n// !! is a deliberate non-null assertion\nval upper: String = text!!.uppercase()',
        },
        {'type': 'heading', 'text': 'Data Classes and equals'},
        {
          'type': 'explanation',
          'text': 'A data class automatically generates equals, hashCode, toString, copy, and componentN functions from the properties declared in the primary constructor. This makes value objects trivial to model. Data classes require at least one primary-constructor parameter and cannot be open or abstract.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'data class User(val id: Long, val email: String)\n\nval a = User(1, "a@b.com")\nval b = a.copy(email = "c@d.com") // copy with change\n// equals, hashCode, toString auto-generated',
        },
        {'type': 'heading', 'text': 'Sealed Classes'},
        {
          'type': 'explanation',
          'text': 'A sealed class restricts the inheritance hierarchy to a fixed set of subclasses declared in the same module. When you exhaustively when over a sealed class, the compiler knows all possible branches and can verify that you have handled every case, which forces compile-time safety when modelling states or results.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'sealed class UiState {\n    data object Loading : UiState()\n    data class Success(val data: List<Item>) : UiState()\n    data class Error(val message: String) : UiState()\n}\n\nfun render(state: UiState) = when (state) {\n    is UiState.Loading -> spinner()\n    is UiState.Success -> list(state.data)\n    is UiState.Error -> errorText(state.message)\n}',
        },
        {'type': 'heading', 'text': 'Scope Functions'},
        {
          'type': 'explanation',
          'text': 'Kotlin provides five scope functions that execute a block in the context of an object. let and run return the lambda result; apply and also return the receiver. let uses it as the receiver, apply and run use this, and also and with differ by argument. Choose the function that expresses the intent: apply to configure, let to transform, also for side effects.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'val user = User().apply {\n    name = "Alex"      // configure using this\n    email = "a@b.com"\n}\n\nval length: Int = name?.let { it.length } ?: 0\n\nrun { println("computing") }  // return block result\nname.also { println("logging \$it") } // side effects',
        },
        {'type': 'heading', 'text': 'Type Inference and Any'},
        {
          'type': 'explanation',
          'text': 'Kotlin infers types when omitted, but you can annotate explicitly when clarity matters. The top of the type hierarchy is Any, the counterpart of java.lang.Object, while Nothing is a subtype of every type that represents an expression that never completes, such as a throw or an infinite loop.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'val count = 42          // Int inferred\nval message = "Hi"     // String inferred\nval anything: Any = "any value"\n\nfun fail(): Nothing = throw IllegalStateException("boom")',
        },
        {'type': 'heading', 'text': 'String Templates'},
        {
          'type': 'explanation',
          'text': 'Kotlin strings support interpolation with a dollar sign followed by a variable name or a braced expression. This produces concise, readable string construction without explicit concatenation. Note that in real code the dollar sign is not escaped; it only appears escaped in this seed data because Dart interprets dollar signs as interpolation.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'val name = "Kotlin"\nval version = 2.0\nprintln("Welcome to \$name version \${version + 1}")\nprintln("Product: \${"a" + "b"}")  // nested expression',
        },
        {'type': 'heading', 'text': 'Smart Casts'},
        {
          'type': 'explanation',
          'text': 'The Kotlin compiler performs smart casts: after a type check such as is String, it automatically treats the variable as that type within the branch without an explicit cast. A smart cast is possible only when the variable is immutable (val) or a local var that is not captured by a closure, so the compiler can prove the value does not change.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'fun process(value: Any) {\n    if (value is String) {\n        println(value.length)  // smart-cast to String\n    }\n    if (value is Int && value > 0) {\n        println(value + 1)\n    }\n}',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'kotlin_language_q1',
          question: 'What is the effect of declaring a property with val instead of var?',
          options: [
            'The property is allocated on the heap only',
            'The property cannot be reassigned after initialization',
            'The property is automatically synchronized across threads',
            'The property becomes nullable',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'kotlin_language_q2',
          question: 'What does the elvis operator ?: do when the left operand is null?',
          options: [
            'It throws a NullPointerException',
            'It returns the value on the right-hand side',
            'It skips the entire expression',
            'It converts the value to a non-null type',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'kotlin_language_q3',
          question: 'Which functions does a data class automatically generate?',
          options: [
            'equals, hashCode, toString, copy, and componentN',
            'compareTo, clone, finalize, and finalizeAndAwait',
            'map, filter, reduce, and fold',
            'onCreate, onStart, onResume, and onDestroy',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'kotlin_language_q4',
          question: 'Why is a sealed class useful when modelling UI state?',
          options: [
            'It allows unlimited dynamic subclasses at runtime',
            'The compiler can verify that a when expression is exhaustive over all cases',
            'Sealed classes are always serializable to JSON',
            'It guarantees thread safety without synchronization',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_language_q5',
          question: 'Which scope function returns the receiver object and is best suited for configuring an object?',
          options: ['let', 'run', 'apply', 'with'],
          correctIndex: 2,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_language_q6',
          question: 'Which type is a subtype of every other type in Kotlin?',
          options: ['Any', 'Nothing', 'Object', 'Unit'],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'kotlin_language_q7',
          question: 'When is a smart cast possible?',
          options: [
            'Only inside a try-catch block',
            'When the variable is immutable and the compiler can prove it does not change',
            'Only for nullable String types',
            'Only when the variable is declared inside a companion object',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_language_q8',
          question: 'What does the safe call operator ?. return if the receiver is null?',
          options: [
            'It throws a NullPointerException',
            'It returns the value of the right-hand expression',
            'It returns null',
            'It returns zero',
          ],
          correctIndex: 2,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'kotlin_language_q9',
          question: 'Which function requires the receiver as an argument rather than a receiver on the calling object?',
          options: ['apply', 'let', 'with', 'also'],
          correctIndex: 2,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_language_q10',
          question: 'What restrictions exist for declaring a data class?',
          options: [
            'It must declare at least one property in the primary constructor',
            'It cannot have any methods',
            'It must extend a ViewModel',
            'It can only contain primitive properties',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
      ],
    ),
    SeedModule(
      id: 'kotlin_collections',
      title: 'Collections & Functional Programming',
      description: 'Master Kotlin collections, functional transformations, sequence laziness, extension functions, and higher-order functions for concise and expressive code.',
      level: 'INTERMEDIATE',
      learningObjectives: [
        'Use transformation operators map, filter, flatMap, and fold',
        'Distinguish eager lists from lazy sequences',
        'Write and reason about extension functions and higher-order functions',
        'Understand immutability of List vs MutableList',
        'Combine collections idiomatically with grouping and partitioning',
      ],
      content: [
        {'type': 'heading', 'text': 'Read-Only vs Mutable Collections'},
        {
          'type': 'explanation',
          'text': 'Kotlin distinguishes a read-only List interface from a MutableList. A List exposes only read operations, while MutableList adds add, remove, and set. Read-only does not guarantee immutability underneath, but it communicates intent at the API boundary. Prefer exposing read-only collections from your public interfaces.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'val items: List<Int> = listOf(1, 2, 3)  // read-only view\nval mutable: MutableList<Int> = mutableListOf(1, 2, 3)\nmutable.add(4)        // allowed\n// items.add(4)      // compile error',
        },
        {'type': 'heading', 'text': 'Transformations'},
        {
          'type': 'explanation',
          'text': 'Functional operators build new collections from existing ones. map applies a transform to each element, filter keeps elements matching a predicate, flatMap flattens nested collections, and fold reduces to a single accumulated value. These compose into readable pipelines that avoid imperative loops.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'val numbers = listOf(1, 2, 3, 4)\n\nval doubled = numbers.map { it * 2 }          // [2, 4, 6, 8]\nval evens = numbers.filter { it % 2 == 0 }    // [2, 4]\nval flat = numbers.flatMap { listOf(it, it) } // [1,1,2,2,3,3,4,4]\nval sum = numbers.fold(0) { acc, n -> acc + n }  // 10',
        },
        {'type': 'heading', 'text': 'Sequences vs List'},
        {
          'type': 'explanation',
          'text': 'Collection operators like map and filter are eager: each operator allocates a new list. A Sequence defers operations and processes elements one at a time, which avoids intermediate allocations and short-circuits when only a few results are needed. Use sequences for large or infinite data sets or when chaining many operations would allocate many intermediate lists.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'val result = (1..1_000_000).asSequence()\n    .filter { it % 2 == 0 }\n    .map { it * it }\n    .take(5)          // processes only until 5 results\n    .toList()\n// eager version would allocate the full filtered list first',
        },
        {'type': 'heading', 'text': 'Extension Functions'},
        {
          'type': 'explanation',
          'text': 'Extension functions let you add new behavior to existing classes without inheritance. They are resolved statically based on the declared type of the receiver, and the extension can access only public members of the class. This keeps related utilities near the type while respecting encapsulation.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'fun String.isEmail(): Boolean =\n    contains("@") && contains(".")\n\nfun Int.isEven(): Boolean = this % 2 == 0\n\nprintln("a@b.com".isEmail())  // true\nprintln(4.isEven())           // true',
        },
        {'type': 'heading', 'text': 'Higher-Order Functions and Lambdas'},
        {
          'type': 'explanation',
          'text': 'Kotlin treats functions as first-class: higher-order functions accept function parameters or return functions. A lambda is a lightweight function literal, and an inline function can avoid the allocation overhead of a lambda by inlining its body at the call site. Standard library functions such as forEach, filter, and map are higher-order.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'inline fun <T> List<T>.forEachNonZero(block: (T) -> Unit) {\n    for (item in this) block(item)\n}\n\nfun applyTwice(x: Int, op: (Int) -> Int): Int = op(op(x))\nval result = applyTwice(2) { it * 3 }  // 18',
        },
        {'type': 'heading', 'text': 'Grouping and Partitioning'},
        {
          'type': 'explanation',
          'text': 'The groupBy operator organizes a collection into a Map keyed by a selector, while partition splits a single collection into a Pair of lists that satisfy and fail a predicate. These operators replace verbose manual grouping loops and express intent clearly.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'data class Item(val category: String, val price: Int)\n\nval items = listOf(Item("book", 10), Item("toy", 20), Item("book", 15))\n\nval byCategory = items.groupBy { it.category }\nval (cheap, expensive) = items.partition { it.price < 15 }',
        },
        {'type': 'heading', 'text': 'Associate and Zip'},
        {
          'type': 'explanation',
          'text': 'The associate family builds maps from collections, allowing you to derive keys and values. The zip function pairs elements from two collections element-wise, producing a list of Pairs. These are convenient for building lookup tables and combining related sequences.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'val names = listOf("A", "B", "C")\nval scores = listOf(90, 80, 70)\n\nval lookup = names.associateWith { index -> scores[index] }\nval paired = names.zip(scores) // [(A,90),(B,80),(C,70)]',
        },
        {'type': 'heading', 'text': 'Sorted and Distinct'},
        {
          'type': 'explanation',
          'text': 'Kotlin offers sorted, sortedBy with a selector, and sortedWith with a comparator. The distinct and distinctBy operators remove duplicates to produce a new list. These pure operators never mutate the original collection, preserving functional discipline.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'val people = listOf(\n    Person("Bob", 30), Person("Alice", 25)\n)\nval byAge = people.sortedBy { it.age }\nval unique = listOf(1, 1, 2, 3, 3).distinct()  // [1,2,3]',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'kotlin_collections_q1',
          question: 'What is the key difference between eager collection operators and a Sequence?',
          options: [
            'Sequences allocate a new list for every operator',
            'Sequences are lazy and process elements one at a time, avoiding intermediate allocations',
            'Sequence operators can only filter, never map',
            'Sequences are always backed by the database',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_collections_q2',
          question: 'What does the fold function do?',
          options: [
            'It duplicates each element of a collection',
            'It reduces the collection to a single accumulated value using an initial accumulator',
            'It groups elements into a Map',
            'It removes duplicate elements in place',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
          codeHeader: '[CALLSITE]',
          codeLanguage: 'Kotlin',
          codeLines: [
            'val nums = listOf(1, 2, 3, 4)',
            'val total = nums.fold(0) { acc, n ->',
            '    acc + n',
            '}',
          ],
        ),
        SeedQuestion(
          id: 'kotlin_collections_q3',
          question: 'When is it preferable to use a Sequence instead of eager list operators?',
          options: [
            'When the collection has fewer than three elements',
            'When processing very large data with many chained operations to avoid intermediate lists',
            'When you need to mutate the source collection',
            'When you need element indexing by position',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'kotlin_collections_q4',
          question: 'What is the defining characteristic of an extension function in Kotlin?',
          options: [
            'It can override methods of the receiver class',
            'It adds behavior to an existing type without inheritance and is resolved statically',
            'It can access private members of the receiver class',
            'It must be declared inside the receiver class body',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_collections_q5',
          question: 'Which operator splits a collection into two lists based on a predicate?',
          options: ['partition', 'chunked', 'windowed', 'zip'],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'kotlin_collections_q6',
          question: 'What does the groupBy operator return?',
          options: [
            'A single reduced value',
            'A Map keyed by the result of a selector function',
            'A flat list of elements',
            'A sorted array',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'kotlin_collections_q7',
          question: 'What is the purpose of marking a higher-order function with the inline keyword?',
          options: [
            'To make it run on a background thread',
            'To avoid lambda allocation by inlining the function body at the call site',
            'To make the function final and non-overridable',
            'To restrict it to val parameters only',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'kotlin_collections_q8',
          question: 'What does the zip function produce from two collections?',
          options: [
            'A single concatenated list',
            'A list of Pairs pairing elements element-wise',
            'A map from the second collection to the first',
            'A set of unique common elements',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'kotlin_collections_q9',
          question: 'Which is true about a read-only List exposed from an API?',
          options: [
            'It is guaranteed to be immutable',
            'It only exposes read operations but may be backed by a mutable list',
            'It cannot be converted back to a MutableList',
            'It always uses the most memory-efficient representation',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_collections_q10',
          question:
              'What is the result of listOf("a", "b").map { it.uppercase() }?',
          options: [
            'A list containing ["A", "B"]',
            'A list containing ["a", "b"]',
            'An empty list',
            'A single string "AB"',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
          codeHeader: '[REPL]',
          codeLanguage: 'Kotlin',
          codeLines: ['listOf("a", "b").map { it.uppercase() }'],
        ),
      ],
    ),
    SeedModule(
      id: 'jetpack_compose',
      title: 'Jetpack Compose',
      description: 'Build modern declarative UIs with Jetpack Compose: recomposition, state hoisting, remember and derivedStateOf, and lifecycle-aware collectAsState.',
      level: 'INTERMEDIATE',
      learningObjectives: [
        'Explain recomposition and when a composable updates',
        'Use remember, mutableStateOf, and derivedStateOf to manage state',
        'Hoist state and expose events from composables',
        'Consume Flow state with collectAsStateLifecycleAware',
        'Compose layouts with Row, Column, Box, and LazyColumn',
      ],
      content: [
        {'type': 'heading', 'text': 'Declarative UI Model'},
        {
          'type': 'explanation',
          'text': 'Jetpack Compose replaces the imperative view hierarchy with a declarative model. You describe the UI as a function of state, and when the state changes Compose recomposes only the affected parts. Composable functions take state as input, emit UI, and are annotated with @Composable so the compiler can manage their lifecycle.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': '@Composable\nfun Greeting(name: String) {\n    Text(text = "Hello \$name")\n}\n\n@Composable\nfun Counter() {\n    var count by remember { mutableStateOf(0) }\n    Button(onClick = { count++ }) {\n        Text("Count: \$count")\n    }\n}',
        },
        {'type': 'heading', 'text': 'remember and mutableStateOf'},
        {
          'type': 'explanation',
          'text': 'mutableStateOf creates observable state, and remember keeps a value across recompositions, resetting it when the composable leaves composition. Reading a mutable state in composition subscribes the composable to that state, so writes trigger recomposition. Prefer rememberSaveable for state that must survive activity and process recreation.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'var text by rememberSaveable { mutableStateOf("") }\nTextField(\n    value = text,\n    onValueChange = { text = it },\n    label = { Text("Search") }\n)',
        },
        {'type': 'heading', 'text': 'State Hoisting'},
        {
          'type': 'explanation',
          'text': 'State hoisting moves state out of a stateless composable by passing the value and an event lambda as parameters. This makes the composable reusable and testable, with a single source of truth owned by a higher composable or the ViewModel. The pattern is value plus onEvent for unidirectional data flow.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': '@Composable\nfun Counter(value: Int, onIncrement: () -> Unit) {\n    Row {\n        Text("Count: \$value")\n        Button(onClick = onIncrement) { Text("+1") }\n    }\n}\n\n@Composable\nfun Screen() {\n    var count by remember { mutableStateOf(0) }\n    Counter(value = count, onIncrement = { count++ })\n}',
        },
        {'type': 'heading', 'text': 'derivedStateOf'},
        {
          'type': 'explanation',
          'text': 'derivedStateOf computes a derived value from other state and only recomputes when its inputs change, avoiding unnecessary recomposition. Use it when a value must be computed from frequently changing state but the derived result changes less often, such as showing a button only when a list becomes non-empty after filtering.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'val filtered = remember(items) {\n    items.filter { it.matches(query) }\n}\n\nval isEmpty by remember {\n    derivedStateOf { filtered.isEmpty() }\n}\nif (isEmpty) { Text("No results") }',
        },
        {'type': 'heading', 'text': 'collectAsState'},
        {
          'type': 'explanation',
          'text': 'The collectAsStateWithLifecycle extension collects a StateFlow as Compose state, only collecting while the lifecycle is at least STARTED. This stops collection when the app is backgrounded, saving resources, and converts the latest value into state that triggers recomposition. For StateFlow, collectAsState is also common.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': '@Composable\nfun UserScreen(viewModel: UserViewModel) {\n    val uiState by viewModel.uiState.collectAsStateWithLifecycle()\n    when (val s = uiState) {\n        is UiState.Loading -> CircularProgressIndicator()\n        is UiState.Success -> UserList(s.users)\n        is UiState.Error -> Text(s.message)\n    }\n}',
        },
        {'type': 'heading', 'text': 'Layouts and LazyColumn'},
        {
          'type': 'explanation',
          'text': 'Row arranges children horizontally, Column vertically, and Box stacks children. LazyColumn and LazyRow build only the visible items, making them suitable for long lists. Lazy lists use key to preserve item identity and contentPadding for inset handling.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'LazyColumn(\n    contentPadding = PaddingValues(16.dp),\n    verticalArrangement = Arrangement.spacedBy(8.dp)\n) {\n    items(items = users, key = { it.id }) { user ->\n        Text(user.name, style = MaterialTheme.typography.bodyLarge)\n    }\n}',
        },
        {'type': 'heading', 'text': 'Theming and Modifier'},
        {
          'type': 'explanation',
          'text': 'Modifiers let you decorate and configure a composable with layout, padding, background, and click behavior in a chain. The MaterialTheme provides color, typography, and shapes that adapt to dark and light themes, enabling consistent theming across the app.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'Column(\n    modifier = Modifier\n        .fillMaxWidth()\n        .padding(16.dp)\n) {\n    Text(\n        text = "Title",\n        style = MaterialTheme.typography.titleLarge,\n        modifier = Modifier.clickable { handleTap() }\n    )\n}',
        },
        {'type': 'heading', 'text': 'Lifecycle and Recomposition'},
        {
          'type': 'explanation',
          'text': 'A composable can enter composition, recompose on state change, and leave composition. Side effects are managed with APIs such as LaunchedEffect for coroutines tied to composition and DisposableEffect for cleanup. Understanding recomposition scope helps you avoid unnecessary work by reading only the smallest state needed.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'LaunchedEffect(Unit) {\n    // runs once when this composable enters composition\n    viewModel.loadData()\n}\n\nDisposableEffect(Unit) {\n    val observer = registerObserver()\n    onDispose { observer.unregister() }\n}',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'jetpack_compose_q1',
          question: 'What does the remember function do in a composable?',
          options: [
            'It stores a value only in a ViewModel',
            'It keeps a value across recompositions, resetting when the composable leaves composition',
            'It persists data to the database',
            'It syncs state with a remote server',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'jetpack_compose_q2',
          question:
              'What happens when mutable state read in a composable changes?',
          options: [
            'The whole activity recreates',
            'The composable recomposes to reflect the new value',
            'Nothing, state is one-way only',
            'The composable is removed from composition',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'jetpack_compose_q3',
          question: 'What does state hoisting mean?',
          options: [
            'Storing state as a static global singleton',
            'Moving state out of a stateless composable, passing value and event as parameters',
            'Storing state on the network layer',
            'Keeping state inside every child composable',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'jetpack_compose_q4',
          question: 'What is the purpose of derivedStateOf?',
          options: [
            'To compute a derived value that only recomputes when its inputs change',
            'To cache network responses on disk',
            'To replace all ViewModel state',
            'To hoist events to a parent composable',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'jetpack_compose_q5',
          question: 'What does collectAsStateWithLifecycle do that plain collectAsState does not?',
          options: [
            'It enables parallel collection',
            'It only collects while the lifecycle is at least STARTED, saving resources',
            'It collects the flow into a database',
            'It converts a cold flow into a hot flow',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
          codeHeader: '[CALLSITE]',
          codeLanguage: 'Kotlin',
          codeLines: [
            'val state by viewModel.uiState',
            '    .collectAsStateWithLifecycle()',
          ],
        ),
        SeedQuestion(
          id: 'jetpack_compose_q6',
          question: 'When should you use LazyColumn instead of a Column?',
          options: [
            'When the list has only one item',
            'When displaying a long list to build only visible items for performance',
            'When you need to stack children on top of each other',
            'When children must be arranged in a grid',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'jetpack_compose_q7',
          question: 'What is the recommended way to persist simple UI state across process recreation?',
          options: [
            'rememberMutableState',
            'rememberSaveable',
            'mutableStateListOf',
            'derivedStateOf',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'jetpack_compose_q8',
          question: 'Which composable effect API is used to run a coroutine tied to composition lifecycle?',
          options: [
            'SideEffect',
            'LaunchedEffect',
            'DisposableEffect',
            'rememberUpdate',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'jetpack_compose_q9',
          question: 'How is a click action typically attached to a composable?',
          options: [
            'Through the Modifier.clickable chain',
            'Through a separate XML attribute',
            'Through setOnClickListener on the parent',
            'Through an Intent filter',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'jetpack_compose_q10',
          question: 'What is the recommended state holder location for complex state shared across the UI?',
          options: [
            'A static companion object',
            'A ViewModel that exposes state as StateFlow',
            'The AndroidManifest file',
            'A repository singleton only',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
      ],
    ),
    SeedModule(
      id: 'android_architecture',
      title: 'Modern Android Architecture',
      description: 'Apply recommended Android architecture: MVVM with UiState, unidirectional data flow, ViewModels, dependency injection with Hilt, and Room persistence.',
      level: 'ADVANCED',
      learningObjectives: [
        'Implement MVVM with a single source of truth',
        'Model UI state with immutable UiState and sealed classes',
        'Use unidirectional data flow with events and one-time effects',
        'Set up constructor injection with Hilt',
        'Persist data with Room and expose it through flows',
      ],
      content: [
        {'type': 'heading', 'text': 'MVVM and Single Source of Truth'},
        {
          'type': 'explanation',
          'text': 'The Model-View-ViewModel architecture separates UI from business logic. The ViewModel holds observable state as StateFlow, the View (Compose) renders that state, and the Model (repository and data sources) supplies data. A single source of truth means one authoritative owner of each piece of data, typically the repository, which the UI observes reactively.',
        },
        {'type': 'heading', 'text': 'UiState and Unidirectional Data Flow'},
        {
          'type': 'explanation',
          'text': 'UI state is exposed as an immutable value representing everything the screen needs. The View emits events, the ViewModel processes them, and new state flows back down. This unidirectional flow makes state transitions predictable and testable. Events such as navigation are separated from state, often via a Channel, so they are consumed only once.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'data class HomeUiState(\n    val isLoading: Boolean = false,\n    val articles: List<Article> = emptyList(),\n    val error: String? = null\n)\n\nclass HomeViewModel(\n    private val repo: ArticleRepository\n) : ViewModel() {\n    private val _uiState = MutableStateFlow(HomeUiState())\n    val uiState: StateFlow<HomeUiState> = _uiState.asStateFlow()\n}',
        },
        {'type': 'heading', 'text': 'ViewModels and the Lifecycle'},
        {
          'type': 'explanation',
          'text': 'A ViewModel survives configuration changes such as rotation because it outlives the activity or fragment. It should never hold references to views or activity contexts; use the AndroidViewModel or an application context when needed. The viewModelScope cancels coroutines automatically when the ViewModel clears.',
        },
        {'type': 'heading', 'text': 'Dependency Injection with Hilt'},
        {
          'type': 'explanation',
          'text': 'Hilt builds on Dagger to provide compile-time dependency injection. Annotate application with @HiltAndroidApp, activities and fragments with @AndroidEntryPoint, and inject dependencies into constructors with @Inject. Modules using @Module and @Provides supply bindings for interfaces such as repositories or Retrofit instances. This decouples classes and simplifies testing.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': '@Module\n@InstallIn(SingletonComponent::class)\nobject NetworkModule {\n    @Provides\n    @Singleton\n    fun provideRetrofit(): Retrofit =\n        Retrofit.Builder()\n            .baseUrl(BASE_URL)\n            .addConverterFactory(GsonConverterFactory.create())\n            .build()\n}',
        },
        {'type': 'heading', 'text': 'Repository Pattern'},
        {
          'type': 'explanation',
          'text': 'The repository is the single access point for data, deciding whether to fetch from the network or read from a local cache like Room. It survives the UI lifecycle, and because it hides the data source, the rest of the app does not care whether data comes from an API or a database.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'class ArticleRepository @Inject constructor(\n    private val api: ArticleApi,\n    private val dao: ArticleDao\n) {\n    fun observeArticles(): Flow<List<Article>> =\n        dao.observeAll().mapLatest { cached ->\n            if (cached.isEmpty()) refreshFromNetwork()\n            cached\n        }\n}',
        },
        {'type': 'heading', 'text': 'Room and Flows'},
        {
          'type': 'explanation',
          'text': 'Room is an ORM that persists data in SQLite and exposes reactive queries. A @Dao with a Flow return type emits the query result whenever the underlying table changes, so the UI always reflects the database. This combines reactive UI with offline persistence and decouples the network from presentation.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': '@Entity(tableName = "articles")\ndata class ArticleEntity(\n    @PrimaryKey val id: Int,\n    val title: String,\n    val body: String\n)\n\n@Dao\ninterface ArticleDao {\n    @Query("SELECT * FROM articles")\n    fun observeAll(): Flow<List<ArticleEntity>>\n}',
        },
        {'type': 'heading', 'text': 'One-Time Events'},
        {
          'type': 'explanation',
          'text': 'Navigation and snackbar messages are one-time events that must be consumed exactly once, unlike state which survives recomposition. A Channel with a buffer of one is commonly used, or a SharedFlow with a replay of zero. This prevents events from being re-shown after rotation or recomposition.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'private val _events = Channel<UiEvent>(Channel.BUFFERED)\nval events = _events.receiveAsFlow()\n\nfun openDetail(id: Int) {\n    viewModelScope.launch {\n        _events.send(UiEvent.NavigateToDetail(id))\n    }\n}',
        },
        {'type': 'heading', 'text': 'Modularization'},
        {
          'type': 'explanation',
          'text': 'Large apps are split into modules such as :app, :data, :domain, and :feature tiers. Modularization enforces dependency direction, improves build parallelism, and enables independent testing and feature development. The domain layer commonly defines models and use cases without Android dependencies for easy unit testing.',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'android_architecture_q1',
          question: 'Why does the UI state often use an immutable data class or sealed class?',
          options: [
            'To guarantee thread safety at runtime only',
            'To make state transitions predictable and easier to reason about',
            'Because Android requires immutable state by law',
            'To reduce the number of files in the project',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'android_architecture_q2',
          question: 'What is the recommended repository responsibility?',
          options: [
            'Rendering composables',
            'Being the single access point for data, deciding between network and cache',
            'Managing the activity lifecycle',
            'Collecting user input gestures',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'android_architecture_q3',
          question: 'When is a ViewModel cleared by the framework?',
          options: [
            'Immediately after every recomposition',
            'When the owning activity or fragment is permanently destroyed',
            'On every configuration change',
            'When any coroutine finishes',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'android_architecture_q4',
          question: 'Why are navigation messages modelled as one-time events rather than UI state?',
          options: [
            'They must be consumed exactly once to avoid re-triggering after recomposition',
            'They are stored in the database permanently',
            'They never change over time',
            'They must be rendered on every screen',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'android_architecture_q5',
          question: 'What are the Hilt annotations for the application class and for injection entry points?',
          options: [
            '@HiltAndroidApp and @AndroidEntryPoint',
            '@AppComponent and @InjectView',
            '@MainActivity and @Provide',
            '@Module and @InstallIn',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
          codeHeader: '[ANNOTATION]',
          codeLanguage: 'Kotlin',
          codeLines: ['@HiltAndroidApp', 'class MyApplication : Application()'],
        ),
        SeedQuestion(
          id: 'android_architecture_q6',
          question: 'What does a Room Flow query emit?',
          options: [
            'A single immutable result at subscription time',
            'The query result whenever the underlying table changes',
            'A callback per row only',
            'Nothing unless the network is queried',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'android_architecture_q7',
          question:
              'What is the main benefit of a repository pattern in Android?',
          options: [
            'It hides the data source and survives the UI lifecycle',
            'It replaces the ViewModel entirely',
            'It removes the need for dependency injection',
            'It makes all data operations synchronous',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'android_architecture_q8',
          question: 'Why is the ViewModel discouraged from holding a reference to an Activity context?',
          options: [
            'It causes network failures',
            'It can leak the activity because the ViewModel survives destruction',
            'It is too slow to access',
            'It prevents dependency injection',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'android_architecture_q9',
          question: 'What channel-backed pattern is recommended for one-time side effects?',
          options: [
            'A MutableStateFlow with an initial value',
            'A Channel that is consumed once in a flow',
            'A shared preference value',
            'A static global singleton',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'android_architecture_q10',
          question: 'What is the direction of data flow in unidirectional data flow with MVVM?',
          options: [
            'View emits events, ViewModel updates state, UI renders it',
            'ViewModel renders UI and the View fetches data',
            'Repository drives the View directly',
            'State flows from network to database only',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
      ],
    ),
    SeedModule(
      id: 'kotlin_testing',
      title: 'Testing & Build Tooling',
      description: 'Write reliable tests and understand the build system: unit tests with JUnit and MockK, coroutine testing with the test dispatcher, UI testing with Compose, and Gradle configuration.',
      level: 'ADVANCED',
      learningObjectives: [
        'Write unit tests for ViewModels and business logic',
        'Mock dependencies with MockK and use fakes',
        'Test coroutines and views with the StandardTestDispatcher',
        'Write Compose UI tests and assertions',
        'Configure the Gradle build for flavors, variants, and versions',
      ],
      content: [
        {'type': 'heading', 'text': 'Unit Testing with JUnit'},
        {
          'type': 'explanation',
          'text': 'Unit tests verify business logic in isolation using the JUnit framework. Test methods are annotated with @Test, and JUnit manages lifecycle with @Before and @BeforeEach. Pure domain logic that does not reference Android classes runs fast on the host JVM, which is why business rules are separated from the framework-dependent UI layer.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'class PriceCalculatorTest {\n    @Test\n    fun `applies discount to total`() {\n        val result = PriceCalculator.calculate(100.0, 0.1)\n        assertEquals(90.0, result)\n    }\n}',
        },
        {'type': 'heading', 'text': 'Mocking with MockK'},
        {
          'type': 'explanation',
          'text': 'MockK is a mocking library for Kotlin that supports relaxed mocks, verifying interactions, and mocking higher-order functions and coroutines. It lets you replace dependencies such as repositories with controlled responses so a unit test can focus on the class under test and its behavior.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'class HomeViewModelTest {\n    private val repo = mockk<ArticleRepository>()\n\n    @Test\n    fun `sets loading then success`() = runTest {\n        coEvery { repo.fetch() } returns listOf(article())\n        val vm = HomeViewModel(repo)\n        vm.load()\n        assertTrue(vm.uiState.value.isLoading)\n    }\n}',
        },
        {'type': 'heading', 'text': 'Testing Coroutines'},
        {
          'type': 'explanation',
          'text': 'Coroutines in tests are controlled with the StandardTestDispatcher or runTest from the kotlinx-coroutines-test artifact. The test dispatcher is virtual, so delay() advances instantly and you control when tasks run. This makes timing-dependent coroutine tests deterministic and fast instead of flaky.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': '@Test\nfun `debounce works`() = runTest {\n    var emissions = 0\n    val flow = MutableStateFlow(0)\n\n    val job = launch { flow.collect { emissions++ } }\n    flow.value = 1\n    advanceTimeBy(400)\n    runCurrent()\n\n    assertEquals(2, emissions)\n    job.cancel()\n}',
        },
        {'type': 'heading', 'text': 'Compose UI Testing'},
        {
          'type': 'explanation',
          'text': 'Compose UI tests use the createComposeRule with assertions such as onNodeWithText and onNodeWithContentDescription. Actions like performClick simulate user input, and Robolectric or an instrumented device executes them. UI tests verify behavior end to end, such as clicking a button updating displayed state.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': '@get:Rule\nval composeRule = createComposeRule()\n\n@Test\nfun counterIncrements() {\n    composeRule.setContent { CounterScreen() }\n    composeRule.onNodeWithText("Count: 0").assertExists()\n    composeRule.onNodeWithText("+1").performClick()\n    composeRule.onNodeWithText("Count: 1").assertExists()\n}',
        },
        {'type': 'heading', 'text': 'Testing with Fakes'},
        {
          'type': 'explanation',
          'text': 'Fakes are lightweight in-memory implementations of interfaces used in tests. Unlike mocks, they contain real behavior. Using fakes for repositories in unit tests validates integration between ViewModels and data logic without touching the network or database, and they are easier to maintain than deeply configured mocks.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'class FakeArticleRepository : ArticleRepository {\n    private val store = mutableListOf<Article>()\n\n    override suspend fun fetch(): List<Article> = store\n    override suspend fun save(article: Article) {\n        store += article\n    }\n}',
        },
        {'type': 'heading', 'text': 'Gradle Build Configuration'},
        {
          'type': 'explanation',
          'text': 'Gradle declares dependencies, build types, flavors, and versions for the project. The Gradle version catalog in gradle/libs.versions.toml centralizes dependency coordinates, and the Android Gradle Plugin (AGP) configures application and library modules in build.gradle.kts. Build variants combine build types with product flavors.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'android {\n    namespace = "com.example.app"\n    compileSdk = 35\n\n    buildTypes {\n        release {\n            isMinifyEnabled = true\n        }\n    }\n    productFlavors {\n        create("free") { applicationId = "com.example.app.free" }\n        create("pro")  { applicationId = "com.example.app.pro" }\n    }\n}\n\ndependencies {\n    implementation(libs.retrofit)\n    testImplementation(libs.junit)\n}',
        },
        {'type': 'heading', 'text': 'Instrumented Tests'},
        {
          'type': 'explanation',
          'text': 'Instrumented tests run on a device or emulator and validate Android integrations such as Room migrations, broadcasts, and fragments. They are placed in the androidTest source set and executed with connectedAndroidTest. Because they exercise the real framework, they catch issues that pure JVM tests cannot, at the cost of slower execution.',
        },
        {'type': 'heading', 'text': 'Coverage and Quality Gates'},
        {
          'type': 'explanation',
          'text': 'Continuous integration runs unit tests, lint checks, and build validation on every change. Tools like Jacoco measure code coverage, while the lint step flags unused resources, accessibility, and performance issues. A quality gate fails the build when coverage or lint thresholds are not met, keeping the codebase healthy over time.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': '// Gradle task to verify and lint every build\n./gradlew testDebugUnitTest\n./gradlew lintDebug\n./gradlew assembleDebug\n// Results are published by CI and gate merging',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'kotlin_testing_q1',
          question: 'Why are coroutine tests typically run with the StandardTestDispatcher?',
          options: [
            'It makes delay() advance virtually for deterministic fast tests',
            'It runs tests on real network sockets',
            'It disables all exception handling',
            'It forces tests to run on the main thread only',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
          codeHeader: '[TEST]',
          codeLanguage: 'Kotlin',
          codeLines: [
            '@Test',
            'fun example() = runTest {',
            '    delay(5000)',
            '    assertTrue(true)',
            '}',
          ],
        ),
        SeedQuestion(
          id: 'kotlin_testing_q2',
          question:
              'What is the difference between a mock and a fake in testing?',
          options: [
            'A mock has real behavior, a fake only records calls',
            'A fake is an in-memory implementation with real behavior, a mock stubs and verifies calls',
            'They are exactly the same thing',
            'A fake runs on device, a mock runs on the JVM',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_testing_q3',
          question: 'Which library and annotation define a unit test method in Kotlin?',
          options: [
            'MockK with @Mock',
            'JUnit with @Test',
            'Espresso with @Run',
            'Room with @Query',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'kotlin_testing_q4',
          question:
              'How would you simulate a click on a Compose node in a UI test?',
          options: [
            'onNodeWithText("+1").performClick()',
            'onNodeWithText("+1").runClick()',
            'performGesture(Click.+"+1")',
            'onNodeWithTag("+1").execute()',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_testing_q5',
          question: 'Where do instrumented tests that run on a device live?',
          options: [
            'The test source set',
            'The androidTest source set',
            'The main source set',
            'The benchmark source set',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'kotlin_testing_q6',
          question: 'What is the purpose of coEvery in MockK?',
          options: [
            'It configures a mock for suspend functions such as repositories',
            'It runs every test concurrently',
            'It verifies that a ViewModel clears properly',
            'It generates coverage reports',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'kotlin_testing_q7',
          question:
              'What do product flavors combine with to form build variants?',
          options: [
            'Source sets only',
            'Build types',
            'Modules',
            'Version catalogs',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_testing_q8',
          question: 'Why is domain business logic kept free of Android framework dependencies?',
          options: [
            'To run on the JVM quickly in unit tests without an emulator',
            'To make it slower to test',
            'To force using instrumented tests',
            'To increase APK size',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_testing_q9',
          question: 'What does the Gradle lint step primarily detect?',
          options: [
            'Runtime crashes only',
            'Code style, unused resources, and accessibility issues',
            'Network latency',
            'Database query performance',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'kotlin_testing_q10',
          question: 'Which function composes two coroutine-based computations and is often tested with runTest?',
          options: ['runBlocking', 'coroutineScope { }', 'launch', 'async'],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
      ],
    ),
    SeedModule(
      id: 'kotlin_async_flow',
      title: 'Advanced Flows: StateFlow, SharedFlow & Operators',
      description: 'Go deep on kotlinx.coroutines streams: cold versus hot flows, the StateFlow and SharedFlow APIs, selection operators, error handling, and backpressure for large production pipelines.',
      level: 'ADVANCED',
      learningObjectives: [
        'Explain the difference between cold Flows and hot StateFlow and SharedFlow',
        'Convert cold streams to hot ones with stateIn and shareIn',
        'Chain operators such as debounce, mapLatest, flatMapLatest, and combine',
        'Handle errors with catch and retryWhen in a flow pipeline',
        'Configure buffering, replay, and onBufferOverflow for slow consumers',
      ],
      content: [
        {'type': 'heading', 'text': 'Cold Flows vs Hot Flows'},
        {
          'type': 'explanation',
          'text': 'A plain Flow is cold: no work happens until a collector subscribes, and every collector gets its own independent emission sequence. StateFlow and SharedFlow are hot: they are active regardless of collectors, and a single emission is delivered to every active subscriber simultaneously. StateFlow additionally retains its latest value and replays it to any new collector, while SharedFlow only replays values you explicitly configure. Use the stateIn and shareIn converters to turn a cold producer into a hot stream shared by many UI consumers.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'val cold: Flow<LocationUpdate> = locationProvider.updates()\n\nval hot: StateFlow<LocationUpdate?> = cold.stateIn(\n    scope = viewModelScope,\n    started = SharingStarted.WhileSubscribed(5000),\n    initialValue = null,\n)',
        },
        {'type': 'heading', 'text': 'Operators For Interactive Pipelines'},
        {
          'type': 'explanation',
          'text': 'Interactive features lean on a few operators. debounce waits until emissions pause before forwarding, so a keystroke stream settles before a search fires. flatMapLatest cancels the previous inner flow as soon as a new value arrives, which is the standard search-as-you-type pattern. mapLatest does the same for suspend transforms. combine merges several flows and recomputes whenever any input changes, while conflate drops intermediate values a slow collector cannot keep up with.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'val results: StateFlow<List<Result>> = query\n    .debounce(300)\n    .flatMapLatest { q -> repository.search(q) }\n    .stateIn(\n        scope = viewModelScope,\n        started = SharingStarted.Lazily,\n        initialValue = emptyList(),\n    )\n\nval canSubmit: Flow<Boolean> = combine(username, password) { u, p ->\n    u.isNotBlank() && p.length >= 8\n}',
        },
        {'type': 'heading', 'text': 'Errors, Retries, and Backpressure'},
        {
          'type': 'explanation',
          'text': 'The catch operator intercepts only upstream exceptions (from blocks before it) and can emit recovery values instead of aborting the stream; to catch collector exceptions, place the catch above the collector. retryWhen resubscribes to the upstream flow when its lambda returns true, so pairing it with attempt counts and delays produces backoff. For fast producers and slow consumers, buffer plus onBufferOverflow chooses between suspending and dropping, and MutableSharedFlow exposes replay and extraBufferCapacity for the same trade-off on hot streams.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'val newsStream: Flow<News> = newsSource.stream()\n    .retryWhen { cause, attempt ->\n        cause is IOException && attempt < 3\n    }\n    .catch { emit(News.empty()) }\n\nval notifications = MutableSharedFlow<UiEvent>(\n    replay = 0,\n    extraBufferCapacity = 16,\n    onBufferOverflow = BufferOverflow.DROP_OLDEST,\n)\nval events: SharedFlow<UiEvent> = notifications.asSharedFlow()',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'kotlin_async_flow_q1',
          question: 'What is the defining difference between a cold Flow and a hot StateFlow?',
          options: [
            'A cold Flow emits independently to each collector; StateFlow shares emissions and holds a current value that new collectors immediately receive',
            'A cold Flow always runs on the main thread',
            'A StateFlow cannot be collected more than once',
            'A cold Flow produces at most one value per subscription',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'kotlin_async_flow_q2',
          question: 'Which call exposes the read-only public view of a MutableStateFlow?',
          options: [
            'asStateFlow()',
            'asSharedFlow()',
            'stateIn()',
            'shareIn()',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
          codeHeader: '[REPL]',
          codeLanguage: 'Kotlin',
          codeLines: [
            'private val _name = MutableStateFlow("")',
            'val name: StateFlow<String> = _name.asStateFlow()',
          ],
        ),
        SeedQuestion(
          id: 'kotlin_async_flow_q3',
          question: 'What does the conflate operator do in a Flow pipeline?',
          options: [
            'It drops intermediate values, delivering only the latest to a slow collector',
            'It duplicates each emission for every subscriber',
            'It converts a hot flow into a cold one',
            'It adds a fixed delay between every pair of emissions',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_async_flow_q4',
          question: 'What behavior does flatMapLatest guarantee?',
          options: [
            'It runs all inner flows in parallel and merges their results',
            'It cancels the previous inner flow as soon as a new upstream value arrives',
            'It waits for the previous inner flow to complete before subscribing to the next',
            'It only allows flat mapping over StateFlow, never plain Flow',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
          codeHeader: '[CALLSITE]',
          codeLanguage: 'Kotlin',
          codeLines: [
            'val results: StateFlow<List<Result>> = query',
            '    .debounce(300)',
            '    .flatMapLatest { q -> repository.search(q) }',
            '    .stateIn(viewModelScope, SharingStarted.Lazily, emptyList())',
          ],
        ),
        SeedQuestion(
          id: 'kotlin_async_flow_q5',
          question: 'When would you use the combine operator over merge?',
          options: [
            'When you need to pair the latest values of multiple flows and compute a combined result when any of them changes',
            'When you need to concatenate flows one after another',
            'When you only want the first flow that emits',
            'When you need to cancel all flows',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_async_flow_q6',
          question: 'Which constructor argument controls how many previously emitted values a new SharedFlow collector immediately receives?',
          options: [
            'replay',
            'extraBufferCapacity',
            'onBufferOverflow',
            'bufferSize',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
          codeHeader: '[REPL]',
          codeLanguage: 'Kotlin',
          codeLines: [
            'val notifications = MutableSharedFlow<String>(',
            '    replay = 1,',
            ')',
            'val events: SharedFlow<String> = notifications.asSharedFlow()',
          ],
        ),
        SeedQuestion(
          id: 'kotlin_async_flow_q7',
          question: 'Which elements of a Flow pipeline does the catch operator intercept?',
          options: [
            'Only exceptions thrown in the upstream blocks placed before the catch',
            'Exceptions thrown by the collector placed after the catch',
            'Every exception anywhere in the application',
            'Only cancellation exceptions',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'kotlin_async_flow_q8',
          question: 'What does SharingStarted.WhileSubscribed(5000) do in a stateIn reactor?',
          options: [
            'It keeps the upstream flow active while collectors exist and stops it 5 seconds after the last collector leaves',
            'It forces the upstream flow to run forever regardless of collectors',
            'It emits exactly once and completes',
            'It caches values on disk for 5 seconds',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'kotlin_async_flow_q9',
          question:
              'Which of the following is a cold stream in kotlinx.coroutines?',
          options: ['Flow', 'StateFlow', 'SharedFlow', 'Channel'],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'kotlin_async_flow_q10',
          question: 'What does BufferOverflow.DROP_OLDEST do when a flow buffer is full?',
          options: [
            'It discards the oldest buffered value to make room for the newest emission',
            'It discards the newest emission and keeps the previous ones',
            'It suspends the producer until the collector catches up',
            'It throws an exception',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
      ],
    ),
    SeedModule(
      id: 'kotlin_networking',
      title: 'Networking: Retrofit, OkHttp & Serialization',
      description: 'Build a production networking layer: type-safe Retrofit endpoints with suspend functions, OkHttp interceptors and caching, JSON serialization, and resilient error handling.',
      level: 'INTERMEDIATE',
      learningObjectives: [
        'Model REST endpoints with Retrofit suspend functions and Response types',
        'Configure OkHttp interceptors for logging and request modification',
        'Enable disk caching that satisfies Cache-Control headers',
        'Serialize request and response models with kotlinx.serialization',
        'Convert failures into sealed Result types for the UI layer',
      ],
      content: [
        {'type': 'heading', 'text': 'Modeling Endpoints with Retrofit'},
        {
          'type': 'explanation',
          'text': 'Retrofit turns an annotated interface into a working HTTP client at runtime. A suspend method returns the deserialized body directly on success and throws retrofit2.HttpException on non-2xx responses; declaring Response<T> instead exposes the status code, headers, and raw error body for graceful handling. Configure a converter factory for JSON and the shared OkHttpClient on the Builder so certificates, timeouts, and caching stay consistent.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'interface AuthApi {\n    @GET("users/{id}")\n    suspend fun getUser(@Path("id") id: Long): User\n\n    @POST("auth/login")\n    suspend fun login(@Body credentials: Credentials): Response<Session>\n}\n\nval api: AuthApi = Retrofit.Builder()\n    .baseUrl("https://api.example.com/")\n    .client(okHttpClient)\n    .addConverterFactory(json.asConverterFactory("application/json".toMediaType()))\n    .build()\n    .create(AuthApi::class.java)',
        },
        {'type': 'heading', 'text': 'OkHttp Interceptors and Caching'},
        {
          'type': 'explanation',
          'text': 'Interceptors sit on the request chain. Application interceptors modify the logical request, adding auth headers, rewriting URLs, or injecting a User-Agent; network interceptors observe the actual bytes after redirects and retries. HttpLoggingInterceptor at BODY level is invaluable in debug builds but must stay out of release builds so tokens do not leak into logs. A Cache directory combined with server Cache-Control headers enables offline reads and fewer round-trips.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'val clientBuilder = OkHttpClient.Builder()\n    .cache(Cache(cacheDir, 50L * 1024 * 1024))\n    .addInterceptor { chain ->\n        val request = chain.request().newBuilder()\n            .header("Authorization", "Bearer \$token")\n            .build()\n        chain.proceed(request)\n    }\nif (BuildConfig.DEBUG) {\n    clientBuilder.addInterceptor(HttpLoggingInterceptor().setLevel(Level.BODY))\n}\nval client = clientBuilder.build()',
        },
        {'type': 'heading', 'text': 'Serialization and Error Handling'},
        {
          'type': 'explanation',
          'text': 'kotlinx.serialization generates JSON codecs at compile time for @Serializable classes, matching fields to Kotlin property names; @SerialName handles renamed fields such as snake_case APIs. On the failure side, wrap the call site so Response failures and IO exceptions become a sealed Result the UI can switch over, instead of throwing across suspend boundaries where the ViewModel would need brittle try-catch chains.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': '@Serializable\ndata class User(\n    val id: Long,\n    val login: String,\n    @SerialName("avatar_url") val avatarUrl: String,\n)\n\nsealed interface Result<out T> {\n    data class Success<T>(val value: T) : Result<T>\n    data class Failure(val code: Int?, val cause: Throwable) : Result<Nothing>\n}\n\nsuspend fun fetchUser(): Result<User> = try {\n    Result.Success(api.getUser(id))\n} catch (e: HttpException) {\n    Result.Failure(e.code(), e)\n} catch (e: IOException) {\n    Result.Failure(null, e)\n}',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'kotlin_networking_q1',
          question: 'Which HTTP client does Retrofit delegate to for executing requests?',
          options: ['OkHttp', 'URLConnection', 'Volley', 'Apache HttpClient'],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'kotlin_networking_q2',
          question: 'How do you expose a Retrofit endpoint as a coroutine-friendly call?',
          options: [
            'Declare the interface method as suspend',
            'Annotate the method with @Synchronized',
            'Return java.util.concurrent.Future',
            'Wrap the call in a BroadcastReceiver',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'kotlin_networking_q3',
          question: 'Which annotation interpolates a value into the URL path of a Retrofit request?',
          options: ['@Path', '@Query', '@Field', '@Header'],
          correctIndex: 0,
          level: 'INTERMEDIATE',
          codeHeader: '[CALLSITE]',
          codeLanguage: 'Kotlin',
          codeLines: [
            '@GET("users/{id}")',
            'suspend fun getUser(@Path("id") id: Long): User',
          ],
        ),
        SeedQuestion(
          id: 'kotlin_networking_q4',
          question: 'What can an OkHttp interceptor do?',
          options: [
            'Observe and modify requests and responses as they pass through the request chain',
            'Replace the Room database schema at runtime',
            'Change the layout of an Activity',
            'Compress the final APK',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_networking_q5',
          question: 'Which annotation marks a Kotlin class for compile-time JSON serialization with kotlinx.serialization?',
          options: [
            '@Serializable',
            '@Parcelize',
            '@JsonObject',
            '@SerializedName',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_networking_q6',
          question: 'What is the practical difference between returning User and Response<User> from a Retrofit suspend method?',
          options: [
            'Response exposes the status code, headers, and any non-2xx error body; User yields the body or throws HttpException',
            'There is no practical difference',
            'Response performs the request twice',
            'User cannot ever be null',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_networking_q7',
          question: 'Why is HttpLoggingInterceptor usually added only to debug builds?',
          options: [
            'Because it logs request and response bodies, which may contain tokens and credentials that would be a risk in release',
            'Because it does not run on release devices',
            'Because it requires a connected debugger',
            'Because it only works with a test server',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
          codeHeader: '[BUILD.GRADLE.KTS]',
          codeLanguage: 'Kotlin',
          codeLines: [
            'debugImplementation("com.squareup.okhttp3:logging-interceptor:4.12.0")',
          ],
        ),
        SeedQuestion(
          id: 'kotlin_networking_q8',
          question: 'Which HTTP header primarily determines whether an OkHttp-cached response can be reused?',
          options: [
            'Cache-Control',
            'Content-Type',
            'Accept-Encoding',
            'User-Agent',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
          codeHeader: '[CALLSITE]',
          codeLanguage: 'Kotlin',
          codeLines: [
            'val cache = Cache(cacheDir, 10L * 1024 * 1024)',
            'val client = OkHttpClient.Builder()',
            '    .cache(cache)',
            '    .build()',
          ],
        ),
        SeedQuestion(
          id: 'kotlin_networking_q9',
          question: 'Which OkHttp mechanism transparently retries a request once after a 401 by producing a fresh credential?',
          options: [
            'An Authenticator attached to the OkHttpClient',
            'A BroadcastReceiver listening for network changes',
            'A Room migration',
            'A custom Compose modifier',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'kotlin_networking_q10',
          question: 'Which component translates JSON into Kotlin objects for Retrofit?',
          options: [
            'A converter factory such as kotlinx.serialization or Moshi',
            'The OkHttpDispatcher',
            'The Gradle version catalog',
            'The Android Profiler',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
      ],
    ),
    SeedModule(
      id: 'kotlin_dependency_injection',
      title: 'Dependency Injection: Hilt & Koin',
      description: 'Master dependency injection on Android: inversion of control, Hilt components and scopes, qualifiers, context injection, and the runtime-based Koin alternative.',
      level: 'ADVANCED',
      learningObjectives: [
        'Explain inversion of control and why constructor injection is preferred',
        'Set up @HiltAndroidApp, @AndroidEntryPoint, and @Inject constructors',
        'Provide bindings with @Binds and @Provides across scoped components',
        'Disambiguate same-type bindings with qualifiers',
        'Declare and resolve modules with the Koin service locator',
      ],
      content: [
        {'type': 'heading', 'text': 'Why Dependency Injection'},
        {
          'type': 'explanation',
          'text': 'Dependency injection inverts control: a class receives its collaborators instead of constructing them. Hard-coded NewsApi() calls couple layers and make components impossible to test, while injecting an interface means a test can substitute a fake. DI frameworks such as Hilt add graph ownership, configuration, and lifecycle scoping on top of plain constructor parameters.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'interface ArticleRepository {\n    suspend fun fetch(): List<Article>\n}\n\nclass NewsApiBasedRepository(private val api: NewsApi) : ArticleRepository {\n    override suspend fun fetch(): List<Article> = api.fetchArticles()\n}\n\nclass NewsViewModel(private val repository: ArticleRepository) : ViewModel() {\n    // consume the injected repository, never construct one directly\n}',
        },
        {'type': 'heading', 'text': 'Hilt Components and Scopes'},
        {
          'type': 'explanation',
          'text': '@HiltAndroidApp on the Application triggers generation of the DI graph; @AndroidEntryPoint enables injection into activities and fragments; @HiltViewModel makes ViewModels resolvable by the framework with constructor parameters. Bindings live in modules installed into a component such as SingletonComponent, either as @Provides factories or as abstract @Binds mappings from an interface to its implementation. Scopes such as @Singleton and @ActivityScoped reuse one instance for the matching component lifetime.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': '@HiltAndroidApp\nclass App : Application()\n\n@Module\n@InstallIn(SingletonComponent::class)\nabstract class RepositoryModule {\n    @Binds\n    abstract fun bindRepository(\n        impl: NewsApiBasedRepository,\n    ): ArticleRepository\n}\n\n@HiltViewModel\nclass NewsViewModel @Inject constructor(\n    private val repository: ArticleRepository,\n) : ViewModel()',
        },
        {'type': 'heading', 'text': 'Qualifiers, Context, and Koin'},
        {
          'type': 'explanation',
          'text': 'When two bindings share a type, a custom @Qualifier annotation disambiguates at the injection point; @Named is the string-based variant. Hilt provides Application, Activity, and Fragment types out of the box, so @Provides functions can take an Application parameter (or @ApplicationContext) to build context-dependent objects such as an OkHttp cache. Koin is the main runtime alternative: modules registered with single, factory, and viewModel are resolved by a service locator at runtime, trading compile-time validation for zero code generation. Tests swap production bindings with a test module.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': '@Qualifier\n@Retention(AnnotationRetention.BINARY)\nannotation class AuthOkHttp\n\n@Module\n@InstallIn(SingletonComponent::class)\nobject NetworkModule {\n    @Provides\n    @Singleton\n    fun provideCache(@ApplicationContext context: Context): Cache =\n        Cache(context.cacheDir, 10L * 1024 * 1024)\n\n    @Provides\n    @AuthOkHttp\n    fun provideAuthClient(cache: Cache): OkHttpClient =\n        OkHttpClient.Builder().cache(cache).build()\n}\n\n@TestInstallIn(\n    components = [SingletonComponent::class],\n    replaces = [RepositoryModule::class],\n)\n@Module\nobject FakeRepositoryModule {\n    @Provides\n    fun provideRepo(): ArticleRepository = FakeArticleRepository()\n}',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'kotlin_dependency_injection_q1',
          question: 'What does dependency injection primarily achieve?',
          options: [
            'It decouples a class from constructing its dependencies by passing them in instead',
            'It makes every class a singleton',
            'It removes the need for interfaces',
            'It makes all dependencies lazy by default',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'kotlin_dependency_injection_q2',
          question: 'Which Hilt annotation marks the Application class to enable code generation?',
          options: [
            '@HiltAndroidApp',
            '@AndroidEntryPoint',
            '@Singleton',
            '@Module',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'kotlin_dependency_injection_q3',
          question: 'What is the difference between @Binds and @Provides?',
          options: [
            '@Binds is abstract and maps an interface to its implementation; @Provides is a concrete function that constructs the value',
            '@Binds runs the binding on a background thread',
            '@Provides cannot return interface types',
            'There is no difference between them',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_dependency_injection_q4',
          question: 'Which annotation marks an Activity as a Hilt injection entry point?',
          options: [
            '@AndroidEntryPoint',
            '@Composable',
            '@HiltViewModel',
            '@Inject',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_dependency_injection_q5',
          question: 'How long does an @ActivityScoped instance live?',
          options: [
            'As long as the Activity component that owns it is alive, and it is recreated when the component is destroyed',
            'For the entire process lifetime',
            'For exactly one method call',
            'Only during instrumented tests',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_dependency_injection_q6',
          question: 'How does Koin resolve dependencies at runtime?',
          options: [
            'It uses a service locator that resolves registered modules dynamically instead of generating code at build time',
            'It uses the Android annotation processor to generate a graph',
            'It requires a XML configuration file',
            'It stores resolved dependencies in a database',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_dependency_injection_q7',
          question:
              'Why would you introduce a custom @Qualifier annotation in Hilt?',
          options: [
            'To disambiguate multiple bindings of the same type, such as an authenticated and an anonymous OkHttpClient',
            'To make a binding run on the IO thread',
            'To mark a binding as optional',
            'To disable a binding in release builds',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
          codeHeader: '[ANNOTATION]',
          codeLanguage: 'Kotlin',
          codeLines: [
            '@Qualifier',
            '@Retention(AnnotationRetention.BINARY)',
            'annotation class AuthOkHttp',
          ],
        ),
        SeedQuestion(
          id: 'kotlin_dependency_injection_q8',
          question: 'What does @HiltViewModel enable for a ViewModel?',
          options: [
            'Constructor injection of dependencies plus integration with the hiltViewModel() navigation function',
            'Injection of the Activity reference',
            'Automatic view binding',
            'Persistence of its state to disk',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
          codeHeader: '[CALLSITE]',
          codeLanguage: 'Kotlin',
          codeLines: [
            '@HiltViewModel',
            'class HomeViewModel @Inject constructor(',
            '    private val repo: ArticleRepository,',
            ') : ViewModel()',
          ],
        ),
        SeedQuestion(
          id: 'kotlin_dependency_injection_q9',
          question: 'Which Hilt-provided type lets a @Provides function access the application context safely?',
          options: [
            'Application or a context annotated with @ApplicationContext',
            'Activity derived from the current task',
            'Fragment from the back stack',
            'Service from the manifest',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'kotlin_dependency_injection_q10',
          question: 'What does inversion of control mean for a class that needs a repository?',
          options: [
            'The class does not build the repository itself; the DI framework supplies it',
            'The class instantiates the repository on every method call',
            'The repository is always a static singleton',
            'The class name must contain Repository',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
      ],
    ),
    SeedModule(
      id: 'kotlin_performance',
      title: 'Performance, ProGuard & Build Optimization',
      description: 'Ship a fast app: profile startup and frames, shrink with R8, protect reflection with keep rules, and use baseline profiles and App Startup to cut cold-start time.',
      level: 'ADVANCED',
      learningObjectives: [
        'Profile CPU, memory, and frame timing with Android Studio tools',
        'Configure R8 minification, shrinking, and ProGuard keep rules',
        'Explain baseline profiles and the App Startup library',
        'Recognize jank, recomposition, and memory-leak causes',
        'Use lint and quality gates to keep the build healthy',
      ],
      content: [
        {'type': 'heading', 'text': 'Profile Before You Optimize'},
        {
          'type': 'explanation',
          'text': 'Measure first. The Android Profiler records CPU time by thread, heap allocations, and FrameTimeline data that pinpoints jank frames; Systrace and Perfetto add system-wide traces. Debug.startMethodTracing captures method-level traces within a code region, such as a heavy Application.onCreate. Cold start is dominated by work on the main thread between process launch and first frame, so move anything expensive off that path.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'class App : Application() {\n    override fun onCreate() {\n        super.onCreate()\n        Debug.startMethodTracing("app_startup")\n        // heavy initialization you want to time\n        initializeComponents()\n        Debug.stopMethodTracing()  // writes app_startup.trace\n    }\n}',
        },
        {'type': 'heading', 'text': 'R8, ProGuard, and Keep Rules'},
        {
          'type': 'explanation',
          'text': 'R8 runs in release builds when isMinifyEnabled is true: it removes unreachable code, optimizes bytecode, and obfuscates identifiers. Shrinking is dangerous for reflection-heavy libraries, because Kotlin reflection, kotlinx.serialization, Room, Retrofit converters, and Navigation resolve classes and members by name. proguard-rules.pro keeps those class patterns and attributes; isShrinkResources removes resources that become unreachable after shrinking. If a release-only crash appears after enabling R8, add a targeted -keep rule instead of disabling shrinking.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'android {\n    buildTypes {\n        release {\n            isMinifyEnabled = true\n            isShrinkResources = true\n            proguardFiles(\n                getDefaultProguardFile("proguard-android-optimize.txt"),\n                "proguard-rules.pro",\n            )\n        }\n    }\n}',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': '# kotlinx.serialization models are read via reflection\n-keepclassmembers class com.example.app.model.** { *; }\n-keepattributes RuntimeVisibleAnnotations, *Annotation*\n# retain generic signatures used by Retrofit converters\n-keepattributes Signature',
        },
        {'type': 'heading', 'text': 'Startup, Rendering, and Memory'},
        {
          'type': 'explanation',
          'text': 'Baseline profiles precompile the hot start path ahead of time, cutting startup and scrolling jank on Android 11 and later. The App Startup library consolidates ContentProvider-based initialization into one provider so libraries no longer add a provider each; defer non-critical initialization. At render time, minimize Compose recomposition by hoisting state, reading the smallest state slice, and using derivedStateOf; reuse bitmaps through an LruCache instead of decoding per frame. Long-lived references to an Activity are classic leaks, so register observers with a lifecycle owner and unregister on destroy.',
        },
        {
          'type': 'code',
          'language': 'Kotlin',
          'code': 'private val cache = LruCache<String, Bitmap>(cacheSizeBytes)\n\nfun load(key: String): Bitmap? = cache.get(key)\n\nclass LoggerInitializer : Initializer<Unit> {\n    override fun create(@Suppress("UNUSED_PARAMETER") context: Context): Unit {\n        enableLogging()\n    }\n\n    override fun dependencies(): List<Class<out Initializer<*>>> = emptyList()\n}',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'kotlin_performance_q1',
          question: 'What does R8 do to a release build?',
          options: [
            'It shrinks code by removing unused classes and methods, optimizes bytecode, and obfuscates identifiers',
            'It compiles Kotlin down to C for the native runtime',
            'It adds extra debug logging',
            'It signs the APK automatically',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'kotlin_performance_q2',
          question: 'Which Gradle flag enables code shrinking for a release build type?',
          options: [
            'isMinifyEnabled = true',
            'isDebuggable = true',
            'enableShrinking',
            'debuggable = "proguard"',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
          codeHeader: '[BUILD.GRADLE.KTS]',
          codeLanguage: 'Kotlin',
          codeLines: [
            'buildTypes {',
            '    release {',
            '        isMinifyEnabled = true',
            '        proguardFiles(',
            '            getDefaultProguardFile("proguard-android-optimize.txt"),',
            '            "proguard-rules.pro",',
            '        )',
            '    }',
            '}',
          ],
        ),
        SeedQuestion(
          id: 'kotlin_performance_q3',
          question: 'Why does proguard-rules.pro usually contain keep rules for models and serializers?',
          options: [
            'Because R8 cannot see reflective access, and removed or renamed entities would break Room, JSON, and reflection at runtime',
            'Because keep rules make the APK smaller',
            'Because keep rules accelerate compilation',
            'Because keep rules enable multidex',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
          codeHeader: '[PROGUARD-RULES.PRO]',
          codeLanguage: 'Kotlin',
          codeLines: [
            '-keepclassmembers class com.example.app.model.** { *; }',
            '-keepattributes Signature, *Annotation*',
          ],
        ),
        SeedQuestion(
          id: 'kotlin_performance_q4',
          question: 'What do baseline profiles actually do for an app?',
          options: [
            'They precompile the hot code paths ahead of time so startup and scrolling do not wait for JIT compilation',
            'They record crash stack traces for QA',
            'They disable AOT compilation entirely',
            'They measure network latency only',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_performance_q5',
          question: 'What problem does the AndroidX App Startup library solve?',
          options: [
            'It aggregates many library ContentProviders into a single InitializationProvider instead of one provider per library',
            'It replaces the AndroidManifest entirely',
            'It speeds up the emulator',
            'It splits the APK into multiple dex files',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_performance_q6',
          question: 'Which practice most directly reduces frame jank?',
          options: [
            'Keeping Compose recomposition small and avoiding heavy work on the main thread',
            'Increasing the display refresh rate',
            'Growing the APK size',
            'Disabling the Profiler',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'kotlin_performance_q7',
          question: 'What does isShrinkResources = true remove?',
          options: [
            'Resources that have become unreachable after R8 removed the code that referenced them',
            'Every PNG in the project regardless of use',
            'The manifest and proguard files',
            'All drawable resources in debug builds',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
          codeHeader: '[BUILD.GRADLE.KTS]',
          codeLanguage: 'Kotlin',
          codeLines: [
            'release {',
            '    isMinifyEnabled = true',
            '    isShrinkResources = true',
            '}',
          ],
        ),
        SeedQuestion(
          id: 'kotlin_performance_q8',
          question: 'How do you avoid leaking an Activity through a long-lived observer callback?',
          options: [
            'Register a LifecycleObserver and unregister it in ON_DESTROY through the lifecycle owner',
            'Register the callback on a background thread',
            'Store the Activity reference in a companion object',
            'Make the Activity open for subclassing',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
          codeHeader: '[ACTIVITY]',
          codeLanguage: 'Kotlin',
          codeLines: [
            'val observer = LifecycleEventObserver { _, event ->',
            '    if (event == Lifecycle.Event.ON_DESTROY) unregister()',
            '}',
            'lifecycle.addObserver(observer)',
          ],
        ),
        SeedQuestion(
          id: 'kotlin_performance_q9',
          question: 'What does the Gradle lint check protect against?',
          options: [
            'Bugs, accessibility gaps, unused resources, and performance hazards caught by static analysis at build time',
            'Runtime memory corruption',
            'Server-side SQL injection',
            'API key rotation',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
          codeHeader: '[TERMINAL]',
          codeLanguage: 'Kotlin',
          codeLines: ['./gradlew lintDebug'],
        ),
        SeedQuestion(
          id: 'kotlin_performance_q10',
          question: 'What is a build variant?',
          options: [
            'The combination of one build type and one product flavor, such as freeDebug or proRelease',
            'The Kotlin compiler version',
            'A listed set of dependencies',
            'The screen size of the device',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
      ],
    ),
  ],
  flipcards: [
    SeedFlipcard(
      id: 'kotlin_flip_coroutine',
      term: 'Coroutine',
      definition: 'A lightweight cooperative unit of concurrency that can suspend and resume without blocking the underlying thread. Thousands of coroutines can share a single thread.',
    ),
    SeedFlipcard(
      id: 'kotlin_flip_job',
      term: 'Job',
      definition: 'A handle to a coroutine that represents its lifecycle. Cancelling a Job cancels the coroutine, and a Job can have children that share the structured-concurrency hierarchy.',
    ),
    SeedFlipcard(
      id: 'kotlin_flip_deferred',
      term: 'Deferred',
      definition: 'A Job subtype returned by the async builder that produces a value, retrieved by calling await(), which suspends until the result is ready.',
    ),
    SeedFlipcard(
      id: 'kotlin_flip_stateflow',
      term: 'StateFlow',
      definition: 'A hot Flow that always holds a current value and replays the latest value to new collectors, making it ideal for observable UI state such as ViewModel state.',
    ),
    SeedFlipcard(
      id: 'kotlin_flip_sealed',
      term: 'Sealed Class',
      definition: 'A class whose subclass hierarchy is restricted to a fixed set declared in the same module, enabling exhaustive when expressions verified at compile time.',
    ),
    SeedFlipcard(
      id: 'kotlin_flip_dataclass',
      term: 'Data Class',
      definition: 'A class that automatically generates equals, hashCode, toString, copy, and componentN functions from its primary-constructor properties, useful for value objects.',
    ),
    SeedFlipcard(
      id: 'kotlin_flip_scope',
      term: 'Scope Functions',
      definition: 'The five functions let, run, apply, also, and with that execute a block within the context of an object, differing in how they return values and name the receiver.',
    ),
    SeedFlipcard(
      id: 'kotlin_flip_elvis',
      term: 'Elvis Operator',
      definition: 'The ?: operator that returns the right-hand value when the left-hand nullable expression is null, providing a concise default value.',
    ),
    SeedFlipcard(
      id: 'kotlin_flip_smartcast',
      term: 'Smart Cast',
      definition: 'The compiler automatically casting a value to a more specific type after a type check such as is String, when it can prove the value does not change.',
    ),
    SeedFlipcard(
      id: 'kotlin_flip_structconc',
      term: 'Structured Concurrency',
      definition: 'The principle that every coroutine is bound to a scope; cancelling the scope cancels all children, and failures are propagated predictably through the hierarchy.',
    ),
    SeedFlipcard(
      id: 'kotlin_flip_suspend',
      term: 'suspend Function',
      definition: 'A function that can suspend execution and resume later without blocking, callable only from a coroutine or another suspend function.',
    ),
    SeedFlipcard(
      id: 'kotlin_flip_recompose',
      term: 'Recomposition',
      definition: 'In Jetpack Compose, the process of recomputing a composable function when the state it reads changes, updating only the affected part of the UI.',
    ),
    SeedFlipcard(
      id: 'kotlin_flip_flow',
      term: 'Flow',
      definition: 'A cold asynchronous data stream that emits a sequence of values on demand. Every collector triggers fresh production of the stream, unlike hot streams such as StateFlow, and the operator set spans map, filter, debounce, and flatMapLatest.',
    ),
    SeedFlipcard(
      id: 'kotlin_flip_buildtype',
      term: 'Build Types',
      definition: 'Gradle build types such as debug and release capture per-variant settings like minification, debuggability, and signing. The release type typically enables isMinifyEnabled and proguardFiles.',
    ),
    SeedFlipcard(
      id: 'kotlin_flip_variant',
      term: 'Build Variant',
      definition: 'The combination of one product flavor and one build type, for example freeDebug or proRelease. Each variant owns its own source sets, manifest merging, and test scope.',
    ),
    SeedFlipcard(
      id: 'kotlin_flip_rest',
      term: 'REST',
      definition: 'A stateless API style that models resources behind URLs and HTTP verbs such as GET, POST, PUT, PATCH, and DELETE, exchanging representations such as JSON. Retrofit turns such an API into typed Kotlin calls.',
    ),
    SeedFlipcard(
      id: 'kotlin_flip_retrofit',
      term: 'Retrofit',
      definition: 'A type-safe HTTP client for Android and the JVM that converts an annotated Kotlin interface into executable requests, with native suspend support, JSON converters, and OkHttp under the hood.',
    ),
    SeedFlipcard(
      id: 'kotlin_flip_serialization',
      term: 'Serialization',
      definition: 'Mapping an in-memory object graph to a storable or transmittable form such as JSON. kotlinx.serialization generates this mapping at compile time for @Serializable classes and matches fields through @SerialName.',
    ),
    SeedFlipcard(
      id: 'kotlin_flip_hilt',
      term: 'Hilt',
      definition: 'Googles compile-time dependency injection library built on Dagger for Android, providing components, scopes, @HiltViewModel integration, and the @AndroidEntryPoint lifecycle hookup.',
    ),
    SeedFlipcard(
      id: 'kotlin_flip_koin',
      term: 'Koin',
      definition: 'A lightweight runtime dependency injection library for Kotlin that resolves modules through a service locator. It declares single, factory, and viewModel bindings without code generation, trading compile-time safety for simplicity.',
    ),
    SeedFlipcard(
      id: 'kotlin_flip_lint',
      term: 'Android Lint',
      definition: 'The static analysis tool that runs during the Gradle build and flags bugs, unused and redundant resources, accessibility gaps, and performance hazards from a documented rule set.',
    ),
    SeedFlipcard(
      id: 'kotlin_flip_baseline',
      term: 'Baseline Profiles',
      definition: 'Ahead-of-time compilation profiles of an app hot paths installed with the release build, reducing startup and scrolling jank on Android 11 and later by avoiding runtime JIT warm-up.',
    ),
    SeedFlipcard(
      id: 'kotlin_flip_appstartup',
      term: 'App Startup Library',
      definition: 'An AndroidX library that consolidates ContentProvider-based initialization into one InitializationProvider, so libraries no longer add one provider per library and initialization can be deferred.',
    ),
    SeedFlipcard(
      id: 'kotlin_flip_kotlinnative',
      term: 'Kotlin/Native',
      definition: 'An LLVM-based Kotlin compiler backend that builds Kotlin into standalone native executables and shared libraries without a JVM, powering iOS, desktop, and embedded targets in Kotlin Multiplatform.',
    ),
  ],
);
