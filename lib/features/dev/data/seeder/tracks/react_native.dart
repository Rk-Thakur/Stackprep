import '../seed_models.dart';

/// React Native curriculum track.
///
/// Ten modules: components, hooks, navigation, state management, styling,
/// native modules (migrated from `kotlin_swift_seeder.dart`) plus Flexbox
/// layout, networking & persistence, performance, and the New Architecture.
/// Twenty-four flipcards covering the full interview surface.
final SeedTrack reactNativeTrack = SeedTrack(
  id: 'REACT_NATIVE',
  name: 'React Native',
  description: 'Master cross-platform mobile development with React Native. Covers components, hooks, navigation, state management, styling, and the New Architecture with TurboModules.',
  category: 'Cross-Platform',
  color: 0xFF61DAFB,
  order: 3,
  modules: [
    SeedModule(
      id: 'rn_components',
      title: 'Components & Props',
      description: 'Learn the building blocks of React Native apps: functional and class components, JSX syntax, props passing, component composition, and key concepts like keys and fragments.',
      level: 'BEGINNER',
      learningObjectives: [
        'Differentiate between functional and class components',
        'Understand JSX syntax and how it compiles to React elements',
        'Pass and validate props including children and default values',
        'Use the key prop correctly for list rendering and reconciliation',
        'Compose complex UIs using composition patterns and fragments',
      ],
      content: [
        {'type': 'heading', 'text': 'Functional vs Class Components'},
        {
          'type': 'explanation',
          'text': 'React Native supports two component types. Functional components are JavaScript functions that receive props and return JSX. Class components extend React.Component and use lifecycle methods like componentDidMount and render(). Modern React Native favors functional components because they work with hooks, are simpler to test, and have less boilerplate.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import React from "react";\nimport { Text } from "react-native";\n\n// Functional component\nfunction Greeting({ name }) {\n  return <Text>Hello, {name}!</Text>;\n}\n\n// Class component\nclass GreetingClass extends React.Component {\n  render() {\n    return <Text>Hello, {this.props.name}!</Text>;\n  }\n}',
        },
        {'type': 'heading', 'text': 'JSX and React Elements'},
        {
          'type': 'explanation',
          'text': 'JSX is a syntax extension that looks like HTML but compiles to JavaScript function calls via React.createElement. Each JSX element becomes a lightweight object describing what should appear on screen. React Native maps these elements to native views: View becomes a native container, Text becomes a native text component, and so on.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': '// JSX compiles to createElement calls\nconst element = <Text>Hello</Text>;\n\n// Equivalent to:\nconst element = React.createElement(Text, null, "Hello");',
        },
        {'type': 'heading', 'text': 'Props and Data Flow'},
        {
          'type': 'explanation',
          'text': 'Props (properties) are read-only inputs passed from parent to child. They flow one direction down the component tree. A child must never modify its own props. You can destructure props in the function signature for cleaner code. Props can be any JavaScript value: strings, numbers, objects, arrays, functions, or other React elements.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'function UserProfile({ name, age, avatar }) {\n  return (\n    <View>\n      <Image source={avatar} />\n      <Text>{name}, {age}</Text>\n    </View>\n  );\n}\n\n// Usage\n<UserProfile\n  name="Alice"\n  age={28}\n  avatar={require("./avatar.png")}\n/>',
        },
        {'type': 'heading', 'text': 'Children and Composition'},
        {
          'type': 'explanation',
          'text': 'The special children prop lets you create wrapper components. Any JSX placed between a component opening and closing tag becomes the children prop value. This enables powerful composition patterns like Card, Modal, or Layout wrappers that do not need to know what content they contain.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'function Card({ children, title }) {\n  return (\n    <View style={styles.card}>\n      <Text style={styles.title}>{title}</Text>\n      {children}\n    </View>\n  );\n}\n\n// Usage\n<Card title="Profile">\n  <Text>Name: Alice</Text>\n  <Text>Age: 28</Text>\n</Card>',
        },
        {'type': 'heading', 'text': 'Default Props and Type Checking'},
        {
          'type': 'explanation',
          'text': 'You can set default values for props using ES6 destructuring defaults. For type checking, use the prop-types library or TypeScript. In large codebases, TypeScript is strongly preferred because it catches errors at compile time rather than runtime.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': '// Default values via destructuring\nfunction Badge({ label = "New", color = "blue" }) {\n  return <Text style={{ color }}>{label}</Text>;\n}\n\n// PropTypes for runtime checking\nimport PropTypes from "prop-types";\nBadge.propTypes = {\n  label: PropTypes.string,\n  color: PropTypes.oneOf(["blue", "red", "green"]),\n};',
        },
        {'type': 'heading', 'text': 'The Key Prop'},
        {
          'type': 'explanation',
          'text': 'When rendering lists, each element needs a unique key prop. Keys help React identify which items changed, were added, or were removed during re-renders. Without stable keys, React falls back to index-based comparison which causes bugs when items are reordered or deleted. Keys should be unique IDs from your data, never array indices.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': '// Correct: stable, unique keys from data\n{items.map((item) => (\n  <ListItem key={item.id} title={item.title} />\n))}\n\n// Wrong: index keys cause bugs on reorder\n{items.map((item, index) => (\n  <ListItem key={index} title={item.title} />\n))}',
        },
        {'type': 'heading', 'text': 'Fragments'},
        {
          'type': 'explanation',
          'text': 'React Native components must return a single root element. When you need to group multiple elements without adding an extra wrapper view, use React.Fragment or the short syntax. Fragments do not render any native element, keeping your view hierarchy flat and avoiding unnecessary nesting that can hurt layout performance.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import React from "react";\n\n// Fragment: no extra native view\nfunction Header() {\n  return (\n    <>\n      <Text>Title</Text>\n      <Text>Subtitle</Text>\n    </>\n  );\n}',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'rn_components_q1',
          question: 'What is the primary difference between functional and class components in React Native?',
          options: [
            'Functional components can only return Text elements',
            'Class components automatically optimize performance',
            'Functional components are plain functions that accept props; class components extend React.Component and use lifecycle methods',
            'Class components are required for using React Navigation',
          ],
          correctIndex: 2,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'rn_components_q2',
          question: 'What does JSX compile down to?',
          options: [
            'HTML strings that are parsed by a browser',
            'React.createElement calls or the JSX runtime import',
            'Direct native platform API calls',
            'Virtual DOM diffing algorithms',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'rn_components_q3',
          question:
              'Why should you avoid using array indices as keys in a list?',
          options: [
            'Indices are strings and keys must be numbers',
            'It causes a runtime error on Android',
            'Reordering or deleting items leads to incorrect component reuse and state bugs',
            'React Native does not support index-based keys',
          ],
          correctIndex: 2,
          level: 'INTERMEDIATE',
          codeHeader: '[Key Prop]',
          codeLanguage: 'JavaScript',
          codeLines: [
            '// Bug: index keys on reorderable list',
            '{items.map((item, index) => (',
            '  <ListItem key={index} title={item.title} />',
            '))}',
          ],
        ),
        SeedQuestion(
          id: 'rn_components_q4',
          question: 'What is the purpose of the children prop?',
          options: [
            'To access child processes in Node.js',
            'To pass component state to its parent',
            'To inject JSX content between a component opening and closing tag',
            'To define children of a navigation stack',
          ],
          correctIndex: 2,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'rn_components_q5',
          question: 'Which pattern lets you group multiple elements without adding an extra native view?',
          options: [
            'React.Fragment or the empty tag syntax',
            'A nested View with flex: 1',
            'The React.Children.toArray utility',
            'A higher-order component wrapper',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
          codeHeader: '[Fragment]',
          codeLanguage: 'JavaScript',
          codeLines: [
            'function Header() {',
            '  return (',
            '    <>',
            '      <Text>Title</Text>',
            '      <Text>Subtitle</Text>',
            '    </>',
            '  );',
            '}',
          ],
        ),
        SeedQuestion(
          id: 'rn_components_q6',
          question: 'Given the code below, what will the rendered output be?',
          options: [
            'Hello, undefined!',
            'Hello, Alice!',
            'A runtime error because name is not passed',
            'Hello, {name}!',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
          codeHeader: '[Default Props]',
          codeLanguage: 'JavaScript',
          codeLines: [
            'function Greeting({ name = "Alice" }) {',
            '  return <Text>Hello, {name}!</Text>;',
            '}',
            '',
            '<Greeting />',
          ],
        ),
        SeedQuestion(
          id: 'rn_components_q7',
          question: 'In React reconciliation, what role do keys play when a list re-renders?',
          options: [
            'Keys determine the visual order of elements',
            'Keys help React identify which elements are the same across renders for efficient reuse and reordering',
            'Keys are optional metadata that only affect debugging',
            'Keys control animation timing of list transitions',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_components_q8',
          question:
              'Why are functional components preferred in modern React Native?',
          options: [
            'Class components are deprecated in React 18',
            'Functional components work with hooks, have less boilerplate, and are easier to test',
            'Class components cannot receive props',
            'Functional components render 10x faster on iOS',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'rn_components_q9',
          question: 'What happens if a child component tries to modify its own props?',
          options: [
            'React throws a warning but applies the change',
            'The modification is silently ignored because props are read-only',
            'It causes a native crash on Android',
            'Nothing, props are mutable by default',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_components_q10',
          question: 'Which approach is recommended for prop validation in large React Native codebases?',
          options: [
            'Runtime checks with console.log in useEffect',
            'PropTypes library for runtime type checking',
            'TypeScript for compile-time type safety',
            'Jest snapshot testing for prop correctness',
          ],
          correctIndex: 2,
          level: 'ADVANCED',
        ),
      ],
    ),
    SeedModule(
      id: 'rn_hooks',
      title: 'Hooks & State',
      description: 'Master React hooks for state management and side effects. Learn useState, useEffect, useContext, useMemo, useCallback, useRef, custom hooks, and the rules that govern them.',
      level: 'INTERMEDIATE',
      learningObjectives: [
        'Manage local component state with useState and understand state batching',
        'Handle side effects correctly with useEffect and dependency arrays',
        'Build reusable custom hooks that encapsulate complex logic',
        'Optimize re-renders with useMemo, useCallback, and useRef',
        'Apply the rules of hooks to avoid common pitfalls',
      ],
      content: [
        {'type': 'heading', 'text': 'useState: Local Component State'},
        {
          'type': 'explanation',
          'text': 'useState is the fundamental hook for managing local state in functional components. It returns a state value and a setter function. When you call the setter, React schedules a re-render with the new state. State updates are batched within event handlers for performance. Always use the functional updater form when the new state depends on the previous state.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { useState } from "react";\n\nfunction Counter() {\n  const [count, setCount] = useState(0);\n\n  const increment = () => {\n    setCount((prev) => prev + 1);\n  };\n\n  return (\n    <View>\n      <Text>{count}</Text>\n      <Button title="Add" onPress={increment} />\n    </View>\n  );\n}',
        },
        {'type': 'heading', 'text': 'useEffect: Side Effects'},
        {
          'type': 'explanation',
          'text': 'useEffect lets you perform side effects: data fetching, subscriptions, timers, or modifying the DOM. The effect runs after render and the dependency array controls when it re-runs. An empty array means run once on mount. A return function from useEffect is the cleanup, called before re-runs or on unmount. Forgetting the dependency array is the most common bug.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { useState, useEffect } from "react";\n\nfunction UserProfile({ userId }) {\n  const [user, setUser] = useState(null);\n\n  useEffect(() => {\n    let cancelled = false;\n    fetchUser(userId).then((data) => {\n      if (!cancelled) setUser(data);\n    });\n    return () => { cancelled = true; };\n  }, [userId]);\n\n  if (!user) return <ActivityIndicator />;\n  return <Text>{user.name}</Text>;\n}',
        },
        {'type': 'heading', 'text': 'useContext: Consuming Context'},
        {
          'type': 'explanation',
          'text': 'useContext accepts a context object and returns the current value from the nearest Provider above. When the Provider value changes, all consumers re-render. This is useful for theme, authentication, locale, or any data shared across many components without prop drilling.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { createContext, useContext } from "react";\n\nconst ThemeContext = createContext("light");\n\nfunction ThemedButton() {\n  const theme = useContext(ThemeContext);\n  return (\n    <Button\n      title="Press me"\n      color={theme === "dark" ? "#fff" : "#000"}\n    />\n  );\n}',
        },
        {'type': 'heading', 'text': 'useMemo and useCallback: Memoization'},
        {
          'type': 'explanation',
          'text': 'useMemo caches a computed value between renders. useCallback caches a function reference. Both accept a dependency array. Without memoization, every parent re-render creates new function references and recalculates values, causing unnecessary child re-renders. Do not memoize everything; profile first with React DevTools.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { useMemo, useCallback } from "react";\n\nfunction SearchResults({ query, results }) {\n  const filtered = useMemo(\n    () => results.filter((r) => r.includes(query)),\n    [query, results]\n  );\n\n  const handlePress = useCallback((id) => {\n    navigation.navigate("Detail", { id });\n  }, [navigation]);\n\n  return filtered.map((r) => (\n    <ResultItem key={r} text={r} onPress={handlePress} />\n  ));\n}',
        },
        {'type': 'heading', 'text': 'useRef: Mutable References'},
        {
          'type': 'explanation',
          'text': 'useRef returns a mutable object whose current property persists across renders without triggering a re-render. It stores timer IDs, previous values, or references to native components like TextInput. Do not use useRef for state that should trigger re-renders. The ref is essential for accessing imperative native methods.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { useRef, useEffect } from "react";\nimport { TextInput } from "react-native";\n\nfunction AutoFocusInput() {\n  const inputRef = useRef(null);\n\n  useEffect(() => {\n    inputRef.current?.focus();\n  }, []);\n\n  return <TextInput ref={inputRef} placeholder="Auto-focused" />;\n}',
        },
        {'type': 'heading', 'text': 'Custom Hooks'},
        {
          'type': 'explanation',
          'text': 'Custom hooks are functions that start with "use" and call other hooks internally. They extract reusable stateful logic: data fetching, form handling, storage, animations. A custom hook must follow the rules of hooks. They share logic without sharing state; each calling component gets its own state.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { useState, useEffect } from "react";\n\nfunction useFetch(url) {\n  const [data, setData] = useState(null);\n  const [loading, setLoading] = useState(true);\n  const [error, setError] = useState(null);\n\n  useEffect(() => {\n    setLoading(true);\n    fetch(url)\n      .then((res) => res.json())\n      .then(setData)\n      .catch(setError)\n      .finally(() => setLoading(false));\n  }, [url]);\n\n  return { data, loading, error };\n}',
        },
        {'type': 'heading', 'text': 'Rules of Hooks'},
        {
          'type': 'explanation',
          'text': 'There are two strict rules. First, only call hooks at the top level never inside loops, conditions, or nested functions. This ensures hooks are called in the same order every render so React can track state correctly. Second, only call hooks from React function components or custom hooks. The eslint-plugin-react-hooks package enforces these rules.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': '// WRONG: conditional hook call\nfunction Component({ shouldShow }) {\n  const [visible, setVisible] = useState(false);\n  if (shouldShow) {\n    const [text, setText] = useState(""); // Error!\n  }\n}\n\n// RIGHT: hooks always at top level\nfunction Component({ shouldShow }) {\n  const [visible, setVisible] = useState(false);\n  const [text, setText] = useState("");\n  // Use conditions to control rendering, not hook calls\n}',
        },
        {'type': 'heading', 'text': 'State Batching'},
        {
          'type': 'explanation',
          'text': 'React 18 batches all state updates by default, including those inside promises, setTimeout, and native event handlers. Calling multiple setters in sequence results in a single re-render. If you need to run code after state has been applied and the component re-rendered, use useEffect with the appropriate dependency.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'function Form() {\n  const [name, setName] = useState("");\n  const [email, setEmail] = useState("");\n\n  const handleSubmit = () => {\n    // These are batched into one re-render\n    setName("");\n    setEmail("");\n  };\n}',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'rn_hooks_q1',
          question: 'What does useState return?',
          options: [
            'Just the current state value',
            'The current state value and a function to update it',
            'A promise that resolves to the new state',
            'The state value and a reference to the state node',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'rn_hooks_q2',
          question:
              'When does the cleanup function returned by useEffect execute?',
          options: [
            'Immediately after the effect runs',
            'Only when the component unmounts',
            'Before the effect re-runs due to dependency changes and on unmount',
            'On every render regardless of dependency changes',
          ],
          correctIndex: 2,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_hooks_q3',
          question: 'What is the purpose of the dependency array in useEffect?',
          options: [
            'To specify the initial state values',
            'To list all variables the effect reads so it only re-runs when those change',
            'To define which components should re-render when the effect completes',
            'To set a timeout in milliseconds between effect re-runs',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_hooks_q4',
          question: 'Which rule of hooks is violated in the code below?',
          options: [
            'Hooks must only be called from React functions',
            'Hooks must not be called conditionally',
            'Hooks must be called in the same order every render',
            'Both B and C describe the same rule that is violated',
          ],
          correctIndex: 3,
          level: 'INTERMEDIATE',
          codeHeader: '[Hook Rules]',
          codeLanguage: 'JavaScript',
          codeLines: [
            'function Component({ flag }) {',
            '  const [a, setA] = useState(0);',
            '  if (flag) {',
            '    const [b, setB] = useState("");',
            '  }',
            '}',
          ],
        ),
        SeedQuestion(
          id: 'rn_hooks_q5',
          question: 'What does useCallback return?',
          options: [
            'A memoized value that only recalculates when dependencies change',
            'A memoized function reference that only changes when dependencies change',
            'A cleanup function for the callback',
            'A new function instance on every render',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
          codeHeader: '[useCallback]',
          codeLanguage: 'JavaScript',
          codeLines: [
            'const handleSubmit = useCallback((data) => {',
            '  submitForm(data);',
            '}, [submitForm]);',
            '',
            '// handleSubmit reference is stable across renders',
          ],
        ),
        SeedQuestion(
          id: 'rn_hooks_q6',
          question: 'Why should you use the functional updater form when new state depends on previous state?',
          options: [
            'It is the only form supported in React 18',
            'It avoids stale closure bugs because it always receives the latest state',
            'It forces a synchronous re-render',
            'It bypasses the dependency array check',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_hooks_q7',
          question: 'What is the difference between useMemo and useCallback?',
          options: [
            'useMemo caches a function reference; useCallback caches a computed value',
            'useMemo caches a computed value; useCallback caches a function reference',
            'They are identical and interchangeable',
            'useMemo is for async; useCallback is for sync',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
          codeHeader: '[Memoization]',
          codeLanguage: 'JavaScript',
          codeLines: [
            'const value = useMemo(() => expensive(n), [n]);',
            'const fn = useCallback(() => doSomething(n), [n]);',
            '',
            '// value: memoized result',
            '// fn: memoized function reference',
          ],
        ),
        SeedQuestion(
          id: 'rn_hooks_q8',
          question: 'What problem does useRef solve that useState does not?',
          options: [
            'useRef stores values that trigger a re-render when updated',
            'useRef provides a mutable reference that persists across renders without causing re-renders',
            'useRef is the only way to access native component methods',
            'useRef replaces useEffect for side effects',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
          codeHeader: '[useRef]',
          codeLanguage: 'JavaScript',
          codeLines: [
            'const prevCount = useRef(0);',
            '',
            'useEffect(() => {',
            '  prevCount.current = count;',
            '}, [count]);',
          ],
        ),
        SeedQuestion(
          id: 'rn_hooks_q9',
          question: 'In React 18, when are state updates batched?',
          options: [
            'Only inside React synthetic event handlers',
            'Inside event handlers and setTimeout only',
            'Everywhere by default, including promises, timeouts, and native event handlers',
            'Never, each setter triggers an immediate re-render',
          ],
          correctIndex: 2,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'rn_hooks_q10',
          question: 'What is a key advantage of custom hooks over regular utility functions?',
          options: [
            'Custom hooks run faster due to React optimizations',
            'Custom hooks can call other hooks and maintain their own state per component instance',
            'Custom hooks are automatically memoized',
            'Custom hooks bypass the rules of hooks',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
      ],
    ),
    SeedModule(
      id: 'rn_navigation',
      title: 'Navigation & Routing',
      description: 'Implement robust navigation in React Native apps using React Navigation. Cover stack, tab, and drawer navigators, parameter passing, deep linking, type-safe navigation, and nested patterns.',
      level: 'INTERMEDIATE',
      learningObjectives: [
        'Set up stack, tab, and drawer navigators with React Navigation',
        'Pass and type-check route parameters between screens',
        'Implement deep linking for URL-based navigation',
        'Handle nested navigation and screen-level options dynamically',
        'Manage navigation state programmatically with refs and hooks',
      ],
      content: [
        {'type': 'heading', 'text': 'React Navigation Setup'},
        {
          'type': 'explanation',
          'text': 'React Navigation is the standard navigation library for React Native. It provides stack, tab, drawer, and material top navigators that manage screen transitions, header bars, back gestures, and deep linking. It runs in JavaScript with native driver support for gestures, making it platform-agnostic and highly customizable.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { NavigationContainer } from "@react-navigation/native";\nimport { createNativeStackNavigator } from "@react-navigation/native-stack";\n\nconst Stack = createNativeStackNavigator();\n\nexport default function App() {\n  return (\n    <NavigationContainer>\n      <Stack.Navigator>\n        <Stack.Screen name="Home" component={HomeScreen} />\n        <Stack.Screen name="Details" component={DetailsScreen} />\n      </Stack.Navigator>\n    </NavigationContainer>\n  );\n}',
        },
        {'type': 'heading', 'text': 'Stack Navigator'},
        {
          'type': 'explanation',
          'text': 'A stack navigator maintains a stack of screens. Pushing a screen adds it on top; going back pops it. Each transition has platform-default animations (slide on iOS, fade on Android). The native stack uses the platform native navigation bar for best performance.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'function DetailsScreen({ navigation, route }) {\n  const { itemId, title } = route.params;\n\n  return (\n    <View>\n      <Text>Item ID: {itemId}</Text>\n      <Button\n        title="Go deeper"\n        onPress={() => navigation.push("Details", {\n          itemId: itemId + 1,\n        })}\n      />\n    </View>\n  );\n}',
        },
        {'type': 'heading', 'text': 'Tab and Drawer Navigators'},
        {
          'type': 'explanation',
          'text': 'Tab navigators render a bottom or top tab bar. Drawer navigators render a slide-out menu. Both can be nested inside stack navigators. A common pattern is a stack inside each tab so each tab maintains its own back stack. Tab icons and badges are configured via screen options.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { createBottomTabNavigator } from "@react-navigation/bottom-tabs";\n\nconst Tab = createBottomTabNavigator();\n\nfunction MainTabs() {\n  return (\n    <Tab.Navigator>\n      <Tab.Screen name="Home" component={HomeScreen}\n        options={{\n          tabBarIcon: ({ color }) => <Icon name="home" color={color} />,\n        }}\n      />\n      <Tab.Screen name="Profile" component={ProfileScreen}\n        options={{ tabBarBadge: 3 }}\n      />\n    </Tab.Navigator>\n  );\n}',
        },
        {'type': 'heading', 'text': 'Passing Parameters'},
        {
          'type': 'explanation',
          'text': 'Route parameters are passed as a second argument to navigation.navigate and navigation.push. The receiving screen accesses them via route.params. For TypeScript, define a ParamList type to get compile-time type checking for all screen names and their parameters.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': '// TypeScript param list\nimport { NativeStackScreenProps } from "@react-navigation/native-stack";\n\ntype RootStackParamList = {\n  Home: undefined;\n  Details: { itemId: number; title: string };\n};\n\ntype Props = NativeStackScreenProps<RootStackParamList, "Details">;\n\nfunction DetailsScreen({ route, navigation }: Props) {\n  const { itemId, title } = route.params;\n}',
        },
        {'type': 'heading', 'text': 'Deep Linking'},
        {
          'type': 'explanation',
          'text': 'Deep linking lets users open specific screens from URLs. Configure a linking object with URL prefixes and route mappings. React Navigation parses the URL and navigates to the matching screen with the correct params. This works for universal links on iOS, app links on Android, and custom URL schemes.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'const linking = {\n  prefixes: ["myapp://", "https://myapp.com"],\n  config: {\n    screens: {\n      Home: "home",\n      Details: "item/:itemId",\n    },\n  },\n};\n\n<NavigationContainer linking={linking}>\n  <Stack.Navigator>...</Stack.Navigator>\n</NavigationContainer>',
        },
        {'type': 'heading', 'text': 'useNavigation Hook'},
        {
          'type': 'explanation',
          'text': 'The useNavigation hook lets any component access the navigation object without receiving it via props. This is useful for deep components that need to navigate but are many levels below the screen. You can also use navigation.getParent() to access an enclosing navigator.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { useNavigation } from "@react-navigation/native";\n\nfunction CustomButton() {\n  const navigation = useNavigation();\n\n  return (\n    <Pressable onPress={() => navigation.navigate("Home")}>\n      <Text>Go Home</Text>\n    </Pressable>\n  );\n}',
        },
        {'type': 'heading', 'text': 'Dynamic Screen Options'},
        {
          'type': 'explanation',
          'text': 'Set header titles, buttons, and styles dynamically per screen using the options function which receives route and navigation. This lets you derive header content from route params. Commonly used for detail screens where the header title matches the item name.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': '<Stack.Screen\n  name="Details"\n  component={DetailsScreen}\n  options={({ route }) => ({\n    title: route.params.title,\n    headerStyle: { backgroundColor: "#f4511e" },\n    headerTintColor: "#fff",\n  })}\n/>',
        },
        {'type': 'heading', 'text': 'Nested Navigators'},
        {
          'type': 'explanation',
          'text': 'A tab navigator inside a stack creates nested navigation state. Each nested navigator has its own independent state. To navigate to a screen in an outer navigator, use navigation.getParent(). To reset the entire tree, use CommonActions.reset with a nested state structure.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': '// Auth flow: check login, show app or auth stack\nfunction RootNavigator() {\n  const { user } = useAuth();\n  return (\n    <Stack.Navigator>\n      {user ? (\n        <Stack.Screen name="Main" component={MainTabs} />\n      ) : (\n        <Stack.Screen name="Auth" component={AuthStack} />\n      )}\n    </Stack.Navigator>\n  );\n}',
        },
        {'type': 'heading', 'text': 'Navigation Reset'},
        {
          'type': 'explanation',
          'text': 'After login or logout, you often need to replace the entire navigation history. navigation.reset replaces the current state with a new state. CommonActions.reset provides a builder API. navigation.replace swaps the current screen without adding to the back stack.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { CommonActions } from "@react-navigation/native";\n\n// After logout, reset to login screen\nnavigation.dispatch(\n  CommonActions.reset({\n    index: 0,\n    routes: [{ name: "Login" }],\n  })\n);\n\n// Replace current screen (no back button)\nnavigation.replace("NewScreen");',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'rn_navigation_q1',
          question:
              'What is the role of NavigationContainer in React Navigation?',
          options: [
            'It renders the tab bar UI',
            'It provides the navigation state tree and links the app to the navigation library',
            'It is only needed for deep linking',
            'It replaces the App component as the root',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
          codeHeader: '[Setup]',
          codeLanguage: 'JavaScript',
          codeLines: [
            '<NavigationContainer>',
            '  <Stack.Navigator>',
            '    <Stack.Screen name="Home" component={Home} />',
            '  </Stack.Navigator>',
            '</NavigationContainer>',
          ],
        ),
        SeedQuestion(
          id: 'rn_navigation_q2',
          question: 'What is the difference between navigation.navigate and navigation.push?',
          options: [
            'navigate adds a screen; push replaces the current screen',
            'push always adds a new instance to the stack; navigate may reuse an existing screen if already in the stack',
            'They are identical in behavior',
            'push is for tabs; navigate is for stacks',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_navigation_q3',
          question:
              'How do you configure deep linking for a React Navigation app?',
          options: [
            'Pass a linking prop with prefixes and screen mappings to NavigationContainer',
            'Add URL schemes in package.json',
            'Use the Linking module from react-native to manually parse URLs',
            'Deep linking is only supported with Expo Router',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_navigation_q4',
          question: 'What is the benefit of defining a ParamList type with React Navigation and TypeScript?',
          options: [
            'It automatically generates URL routes for deep linking',
            'It provides compile-time type checking for screen names and their parameters',
            'It reduces bundle size by tree-shaking unused routes',
            'It enables server-side rendering of navigation state',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_navigation_q5',
          question: 'What does navigation.reset do?',
          options: [
            'It clears all cached screens from memory',
            'It replaces the entire navigation state history with a new state',
            'It restarts the JavaScript runtime',
            'It navigates back to the first screen in the stack',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_navigation_q6',
          question: 'Which pattern is recommended for apps with authentication flows?',
          options: [
            'A single stack with conditional rendering of screens',
            'Conditional navigator rendering based on auth state inside NavigationContainer',
            'Using AsyncStorage to persist navigation state',
            'A modal that overlays the main navigator',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'rn_navigation_q7',
          question: 'How do you access the navigation object from a deeply nested component?',
          options: [
            'Import the navigation singleton directly',
            'Use the useNavigation hook',
            'Access it via React.useContext with a custom context',
            'Pass it through global state management',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'rn_navigation_q8',
          question: 'What is the advantage of @react-navigation/native-stack over the JS-based stack?',
          options: [
            'It supports more transition animations',
            'It uses the platform native navigation bar and transitions for better performance',
            'It works without JavaScript enabled',
            'It requires less configuration',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'rn_navigation_q9',
          question: 'How do you dynamically set the header title based on route params?',
          options: [
            'Use useEffect to call navigation.setOptions',
            'Pass an options function to the Screen component that receives route and returns title',
            'Set the title in the linking configuration only',
            'Use a global state manager to store the header title',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
          codeHeader: '[Dynamic Header]',
          codeLanguage: 'JavaScript',
          codeLines: [
            '<Stack.Screen',
            '  name="Details"',
            '  component={DetailsScreen}',
            '  options={({ route }) => ({',
            '    title: route.params.title,',
            '  })}',
            '/>',
          ],
        ),
        SeedQuestion(
          id: 'rn_navigation_q10',
          question: 'In a nested navigator setup, how do you navigate to a screen in an outer navigator?',
          options: [
            'It is not possible to cross navigator boundaries',
            'Use navigation.getParent() to access the outer navigator and then navigate',
            'You must use a global event bus',
            'Call CommonActions.navigate with the full path as a string',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
      ],
    ),
    SeedModule(
      id: 'rn_state_management',
      title: 'State Management',
      description: 'Explore state management solutions for React Native: Context API, Redux Toolkit, Zustand, Jotai, and local-first approaches. Learn when to use each and how to optimize performance.',
      level: 'INTERMEDIATE',
      learningObjectives: [
        'Evaluate Context API for simple global state and understand its re-render trade-offs',
        'Structure Redux Toolkit stores with slices, thunks, and RTK Query',
        'Implement Zustand stores for lightweight, performant global state',
        'Choose the right tool based on app complexity and team needs',
        'Apply selectors and memoization to minimize unnecessary re-renders',
      ],
      content: [
        {'type': 'heading', 'text': 'Context API for Global State'},
        {
          'type': 'explanation',
          'text': 'React Context provides a way to pass data through the component tree without prop drilling. Create a context with createContext, provide a value at the top with a Provider, and consume it with useContext. Context is ideal for theme, locale, and auth data. Every consumer re-renders when the Provider value changes.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { createContext, useContext, useState } from "react";\n\nconst AuthContext = createContext(null);\n\nexport function AuthProvider({ children }) {\n  const [user, setUser] = useState(null);\n  const login = async (creds) => {\n    const response = await api.login(creds);\n    setUser(response.user);\n  };\n  return (\n    <AuthContext.Provider value={{ user, login }}>\n      {children}\n    </AuthContext.Provider>\n  );\n}\n\nexport const useAuth = () => useContext(AuthContext);',
        },
        {'type': 'heading', 'text': 'Redux Toolkit'},
        {
          'type': 'explanation',
          'text': 'Redux Toolkit is the official, recommended way to write Redux logic. It simplifies store setup, reduces boilerplate with createSlice which uses Immer for immutable updates, and provides createAsyncThunk for async operations. RTK Query handles server state caching.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { createSlice, configureStore } from "@reduxjs/toolkit";\n\nconst todosSlice = createSlice({\n  name: "todos",\n  initialState: [],\n  reducers: {\n    addTodo: (state, action) => {\n      state.push({ id: Date.now(), text: action.payload, done: false });\n    },\n    toggleTodo: (state, action) => {\n      const todo = state.find((t) => t.id === action.payload);\n      if (todo) todo.done = !todo.done;\n    },\n  },\n});\n\nconst store = configureStore({ reducer: { todos: todosSlice.reducer } });',
        },
        {'type': 'heading', 'text': 'Zustand: Minimal Global State'},
        {
          'type': 'explanation',
          'text': 'Zustand is a small, fast, and scalable state library. A store is created with a single function call, no Provider needed. Components subscribe to specific state slices using selectors. Zustand re-renders only components that use the changed state. It supports middleware for persistence, devtools, and immer.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { create } from "zustand";\nimport { persist, devtools } from "zustand/middleware";\n\nconst useStore = create(\n  devtools(\n    persist(\n      (set, get) => ({\n        count: 0,\n        increment: () => set((s) => ({ count: s.count + 1 })),\n        decrement: () => set((s) => ({ count: s.count - 1 })),\n      }),\n      { name: "counter-storage" }\n    )\n  )\n);',
        },
        {'type': 'heading', 'text': 'When to Use What'},
        {
          'type': 'explanation',
          'text': 'Context API is best for simple, rarely-changing values like theme, auth, and locale. Redux Toolkit is ideal for complex apps with many interconnected state domains and the need for middleware. Zustand fits apps wanting Redux-like simplicity without boilerplate. For server state, React Query or RTK Query is almost always better than client state tools.',
        },
        {'type': 'heading', 'text': 'Selectors and Performance'},
        {
          'type': 'explanation',
          'text': 'When using Redux or Zustand, always select only the state slices your component needs. In Redux, use createSelector from reselect to derive computed data and memoize results. In Zustand, pass a selector function to the hook. Without selectors, any state change re-renders every connected component.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': '// Zustand: selector for minimal re-renders\nconst count = useStore((s) => s.count);\n\n// Redux: memoized selector with reselect\nimport { createSelector } from "@reduxjs/toolkit";\n\nconst selectTodos = (state) => state.todos;\nconst selectDoneCount = createSelector(\n  [selectTodos],\n  (todos) => todos.filter((t) => t.done).length\n);',
        },
        {'type': 'heading', 'text': 'Redux Middleware and Thunks'},
        {
          'type': 'explanation',
          'text': 'Middleware intercepts every Redux action before it reaches the reducer. Common middleware includes redux-thunk for async actions (default in RTK), redux-logger for debugging, and custom middleware for analytics. createAsyncThunk generates pending/fulfilled/rejected actions automatically.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { createAsyncThunk } from "@reduxjs/toolkit";\n\nconst fetchTodos = createAsyncThunk(\n  "todos/fetchTodos",\n  async (_, { rejectWithValue }) => {\n    try {\n      const response = await fetch("/api/todos");\n      return await response.json();\n    } catch (err) {\n      return rejectWithValue(err.message);\n    }\n  }\n);',
        },
        {'type': 'heading', 'text': 'Persisting State'},
        {
          'type': 'explanation',
          'text': 'React Native apps frequently need to persist state across sessions. Redux has redux-persist, Zustand has built-in persist middleware. For simple cases, react-native-mmkv is significantly faster than AsyncStorage. Always consider what should and should not be persisted for security and performance.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { persist, createJSONStorage } from "zustand/middleware";\nimport AsyncStorage from "@react-native-async-storage/async-storage";\n\nconst useAuthStore = create(\n  persist(\n    (set) => ({\n      token: null,\n      setToken: (token) => set({ token }),\n    }),\n    {\n      storage: createJSONStorage(() => AsyncStorage),\n      partialize: (state) => ({ token: state.token }),\n    }\n  )\n);',
        },
        {'type': 'heading', 'text': 'Atom-Based State with Jotai'},
        {
          'type': 'explanation',
          'text': 'Jotai takes an atomic approach where each piece of state is an independent atom. Components subscribe to specific atoms and re-render only when those atoms change. Derived atoms compute values from other atoms. This model eliminates selector boilerplate and provides fine-grained reactivity.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { atom, useAtom } from "jotai";\n\nconst countAtom = atom(0);\nconst doubleAtom = atom((get) => get(countAtom) * 2);\n\nfunction Counter() {\n  const [count, setCount] = useAtom(countAtom);\n  return (\n    <View>\n      <Text>{count}</Text>\n      <Button title="Inc" onPress={() => setCount((c) => c + 1)} />\n    </View>\n  );\n}',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'rn_state_management_q1',
          question: 'What is a key limitation of using React Context for frequently changing state?',
          options: [
            'Context cannot store primitive values',
            'When Provider value changes, all consumers re-render even if they use only part of the value',
            'Context requires Redux as a dependency',
            'Context values cannot be objects or arrays',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_state_management_q2',
          question:
              'What does Redux Toolkit provide that vanilla Redux does not?',
          options: [
            'A different state management paradigm entirely',
            'Simplified store setup, createSlice with Immer, createAsyncThunk, and RTK Query',
            'Built-in React Navigation integration',
            'Automatic code splitting for reducers',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
          codeHeader: '[createSlice]',
          codeLanguage: 'JavaScript',
          codeLines: [
            'const slice = createSlice({',
            '  name: "todos",',
            '  initialState: [],',
            '  reducers: {',
            '    add: (state, action) => { state.push(action.payload); },',
            '  },',
            '});',
          ],
        ),
        SeedQuestion(
          id: 'rn_state_management_q3',
          question: 'Why does Zustand not need a Provider wrapper?',
          options: [
            'It stores state in localStorage instead of memory',
            'It uses React Context internally which is provided at the root',
            'It creates standalone stores that components import directly',
            'It only works with class components that do not need providers',
          ],
          correctIndex: 2,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_state_management_q4',
          question: 'When should you prefer React Query or RTK Query over client state management?',
          options: [
            'When you need to manage UI-only state like modal open/close',
            'When state represents server data needing caching, refetching, and loading states',
            'When you need real-time WebSocket synchronization',
            'When the state is only used in a single component',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'rn_state_management_q5',
          question: 'What is the purpose of createAsyncThunk in Redux Toolkit?',
          options: [
            'To create synchronous action creators automatically',
            'To handle async operations and dispatch pending/fulfilled/rejected actions',
            'To replace the Redux store entirely',
            'To generate TypeScript types for action payloads',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_state_management_q6',
          question: 'How do Zustand selectors improve performance?',
          options: [
            'They cache the entire store in IndexedDB',
            'They ensure a component only re-renders when the specific selected slice changes',
            'They run selectors on a background thread',
            'They prevent any state updates from occurring',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
          codeHeader: '[Zustand Selectors]',
          codeLanguage: 'JavaScript',
          codeLines: [
            'const count = useStore((s) => s.count);',
            'const name = useStore((s) => s.user.name);',
            '',
            '// Only re-renders when count or name changes',
          ],
        ),
        SeedQuestion(
          id: 'rn_state_management_q7',
          question: 'What is a derived atom in Jotai?',
          options: [
            'An atom that can only be read, never written',
            'An atom whose value is computed from other atoms and updates when dependencies change',
            'An atom stored in persistent storage',
            'An atom that triggers a navigation action',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'rn_state_management_q8',
          question: 'What middleware is included by default in configureStore from Redux Toolkit?',
          options: [
            'redux-saga and redux-thunk',
            'redux-thunk plus a serializable check and an immutability check in development',
            'redux-persist and redux-logger',
            'No middleware is included by default',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'rn_state_management_q9',
          question: 'What are common trade-offs when splitting context for performance?',
          options: [
            'Using too many contexts creates provider nesting complexity',
            'Not memoizing the context value causes unnecessary consumer re-renders',
            'Both A and B are real trade-offs to consider',
            'Context cannot be split; it must always be a single provider',
          ],
          correctIndex: 2,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'rn_state_management_q10',
          question: 'For a React Native app that stores user session tokens, what is the recommended persistence approach?',
          options: [
            'Store tokens in Redux state without persistence',
            'Use react-native-mmkv or AsyncStorage with persist middleware from your state library',
            'Store tokens in a global JavaScript variable',
            'Store tokens only server-side with no client persistence',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
      ],
    ),
    SeedModule(
      id: 'rn_styling',
      title: 'Styling & Layout',
      description: 'Master styling and layout in React Native using StyleSheet, Flexbox, responsive design, platform-specific code, and the Animated and Reanimated APIs for performant animations.',
      level: 'BEGINNER',
      learningObjectives: [
        'Use StyleSheet and the Yoga Flexbox engine for cross-platform layouts',
        'Build responsive designs that adapt to screen sizes and orientations',
        'Apply platform-specific styles using Platform.OS and Platform.select',
        'Create performant animations with React Native Reanimated',
        'Use SafeAreaView and Dimensions for screen-aware layouts',
      ],
      content: [
        {'type': 'heading', 'text': 'StyleSheet and the Yoga Engine'},
        {
          'type': 'explanation',
          'text': 'React Native uses StyleSheet.create to define styles as JavaScript objects. These are sent to the native side where the Yoga layout engine computes positions using Flexbox. StyleSheet.create validates styles at creation time and provides an optimization layer. Layout computation runs on a background thread.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { StyleSheet, View, Text } from "react-native";\n\nfunction Card({ title, body }) {\n  return (\n    <View style={styles.card}>\n      <Text style={styles.title}>{title}</Text>\n      <Text style={styles.body}>{body}</Text>\n    </View>\n  );\n}\n\nconst styles = StyleSheet.create({\n  card: {\n    flex: 1,\n    padding: 16,\n    backgroundColor: "#fff",\n    borderRadius: 8,\n  },\n  title: { fontSize: 18, fontWeight: "bold", marginBottom: 8 },\n  body: { fontSize: 14, color: "#666" },\n});',
        },
        {'type': 'heading', 'text': 'Flexbox in React Native'},
        {
          'type': 'explanation',
          'text': 'Flexbox is the primary layout mechanism. React Native defaults to flexDirection: "column", unlike web CSS which defaults to row. The main axis is vertical by default. Understanding flex, alignSelf, flexWrap, and gap is essential for any non-trivial layout.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'const styles = StyleSheet.create({\n  container: {\n    flex: 1,\n    flexDirection: "row",\n    justifyContent: "space-between",\n    alignItems: "center",\n    padding: 16,\n  },\n  sidebar: { flex: 1, backgroundColor: "#f0f0f0" },\n  content: { flex: 3, padding: 16 },\n});',
        },
        {'type': 'heading', 'text': 'Responsive Design'},
        {
          'type': 'explanation',
          'text': 'React Native has no media queries. Use the Dimensions API or useWindowDimensions hook to get screen size. Design mobile-first with breakpoints for tablets. Percentage-based widths, flex ratios, and dynamic sizing based on Dimensions are the standard approach.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { useWindowDimensions } from "react-native";\n\nfunction ResponsiveLayout({ children }) {\n  const { width } = useWindowDimensions();\n  const isTablet = width >= 768;\n\n  return (\n    <View style={{\n      flexDirection: isTablet ? "row" : "column",\n      padding: isTablet ? 32 : 16,\n    }}>\n      {children}\n    </View>\n  );\n}',
        },
        {'type': 'heading', 'text': 'Platform-Specific Styles'},
        {
          'type': 'explanation',
          'text': 'Platform.OS returns "ios" or "android". Platform.select lets you choose values per platform inline. Platform-specific file extensions like Component.ios.js and Component.android.js let React Native resolve the correct file during the build.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { Platform, StyleSheet } from "react-native";\n\nconst styles = StyleSheet.create({\n  header: {\n    ...Platform.select({\n      ios: {\n        shadowColor: "#000",\n        shadowOffset: { width: 0, height: 2 },\n      },\n      android: { elevation: 4 },\n    }),\n    backgroundColor: "#fff",\n  },\n});',
        },
        {'type': 'heading', 'text': 'SafeAreaView and StatusBar'},
        {
          'type': 'explanation',
          'text': 'SafeAreaView adds padding to avoid the notch, home indicator, and status bar. StatusBar controls the status bar appearance (light/dark content, background color). For fine-grained control, use useSafeAreaInsets to get exact inset values.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { StatusBar } from "react-native";\nimport { useSafeAreaInsets } from "react-native-safe-area-context";\n\nfunction Screen() {\n  const insets = useSafeAreaInsets();\n  return (\n    <View style={{ paddingTop: insets.top, paddingBottom: insets.bottom }}>\n      <StatusBar barStyle="dark-content" backgroundColor="#fff" />\n      <Text>Content respects safe area</Text>\n    </View>\n  );\n}',
        },
        {'type': 'heading', 'text': 'Animated API'},
        {
          'type': 'explanation',
          'text': 'The built-in Animated API provides declarative animation primitives: timing, spring, decay. Animated.Value tracks the animated value and Animated.View applies it to native props. Use useNativeDriver: true to run animations on the native thread, avoiding JS thread jank.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { Animated, useEffect, useRef } from "react-native";\n\nfunction FadeInView({ children }) {\n  const opacity = useRef(new Animated.Value(0)).current;\n\n  useEffect(() => {\n    Animated.timing(opacity, {\n      toValue: 1,\n      duration: 500,\n      useNativeDriver: true,\n    }).start();\n  }, []);\n\n  return <Animated.View style={{ opacity }}>{children}</Animated.View>;\n}',
        },
        {'type': 'heading', 'text': 'React Native Reanimated'},
        {
          'type': 'explanation',
          'text': 'Reanimated runs animations on a separate UI thread, independent of JavaScript. Worklets run on the UI thread, enabling complex gesture-driven animations. Shared element transitions, layout animations, and entering/exiting animations are declarative and performant.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import Animated, {\n  useSharedValue,\n  useAnimatedStyle,\n  withTiming,\n} from "react-native-reanimated";\n\nfunction Box() {\n  const offset = useSharedValue(0);\n\n  const animatedStyle = useAnimatedStyle(() => ({\n    transform: [{ translateX: offset.value }],\n  }));\n\n  return (\n    <Animated.View\n      style={[styles.box, animatedStyle]}\n      onTouchEnd={() => {\n        offset.value = withTiming(offset.value === 0 ? 150 : 0);\n      }}\n    />\n  );\n}',
        },
        {'type': 'heading', 'text': 'Dynamic and Responsive Styles'},
        {
          'type': 'explanation',
          'text': 'Combine Dimensions, Platform, and conditional logic for responsive styles. Avoid hardcoded pixel values; use relative units or scale values derived from screen dimensions. Test on multiple device sizes to catch layout issues early.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { Dimensions } from "react-native";\n\nconst { width: SCREEN_WIDTH } = Dimensions.get("window");\nconst scale = SCREEN_WIDTH / 375; // based on iPhone 12 width\n\nexport function normalize(size) {\n  return Math.round(size * scale);\n}\n\nconst styles = StyleSheet.create({\n  title: { fontSize: normalize(20) },\n  padding: { padding: normalize(16) },\n});',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'rn_styling_q1',
          question: 'What is the default flexDirection in React Native?',
          options: ['row', 'column', 'row-reverse', 'column-reverse'],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'rn_styling_q2',
          question:
              'Why should you use useNativeDriver: true in the Animated API?',
          options: [
            'It enables physics-based spring animations',
            'It runs the animation on the native UI thread, preventing JS thread jank',
            'It reduces the JavaScript bundle size',
            'It allows animations to run in background mode',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_styling_q3',
          question:
              'What is the benefit of StyleSheet.create over plain objects?',
          options: [
            'It enables CSS syntax instead of camelCase',
            'It validates styles at creation time and provides an optimization layer for the bridge',
            'It automatically adds vendor prefixes',
            'It converts styles to CSS for web targets',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'rn_styling_q4',
          question:
              'How do you handle platform-specific styles in React Native?',
          options: [
            'Use window.navigator.platform to detect the OS',
            'Use Platform.select or platform-specific file extensions',
            'Write separate CSS files for each platform',
            'Use media queries like in web development',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'rn_styling_q5',
          question: 'What does SafeAreaView do?',
          options: [
            'It encrypts sensitive data on screen',
            'It adds padding to avoid the notch, status bar, and home indicator',
            'It creates a view that persists across transitions',
            'It provides a sandbox for untrusted content',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'rn_styling_q6',
          question: 'Why is Reanimated preferred over the built-in Animated API for complex animations?',
          options: [
            'Reanimated supports more easing functions',
            'Reanimated runs animations on a separate UI thread, independent of the JS thread',
            'Reanimated uses CSS transitions under the hood',
            'Reanimated requires less code for all animation types',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_styling_q7',
          question:
              'Given the Flexbox layout below, what will the arrangement be?',
          options: [
            'Three boxes stacked vertically, each full width',
            'Three boxes horizontally, each taking equal space',
            'Three boxes horizontally with the first taking double space',
            'Three boxes in a two-row grid',
          ],
          correctIndex: 2,
          level: 'BEGINNER',
          codeHeader: '[Flexbox]',
          codeLanguage: 'JavaScript',
          codeLines: [
            '<View style={{ flexDirection: "row" }}>',
            '  <View style={{ flex: 2 }} />',
            '  <View style={{ flex: 1 }} />',
            '  <View style={{ flex: 1 }} />',
            '</View>',
          ],
        ),
        SeedQuestion(
          id: 'rn_styling_q8',
          question: 'What is the advantage of useWindowDimensions over Dimensions.get?',
          options: [
            'useWindowDimensions is faster because it caches the result',
            'useWindowDimensions automatically re-renders when screen dimensions change from rotation or split screen',
            'useWindowDimensions works on web but not on native',
            'There is no difference; they are aliases',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
          codeHeader: '[Dimensions]',
          codeLanguage: 'JavaScript',
          codeLines: [
            'const { width, height } = useWindowDimensions();',
            '',
            '// Re-renders automatically on rotation',
            'const isLandscape = width > height;',
          ],
        ),
        SeedQuestion(
          id: 'rn_styling_q9',
          question: 'What is a worklet in React Native Reanimated?',
          options: [
            'A background thread managed by the OS scheduler',
            'A function that runs on the Reanimated UI thread, separate from the JavaScript thread',
            'A hook that defers rendering until the next frame',
            'A style object that is evaluated lazily',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'rn_styling_q10',
          question: 'Why should you wrap your root layout in SafeAreaView or use useSafeAreaInsets?',
          options: [
            'To enable dark mode detection',
            'To prevent content from being hidden behind the notch, status bar, and home indicator',
            'To automatically apply safe area padding on web browsers',
            'To register the app with Apple and Google for push notifications',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
      ],
    ),
    SeedModule(
      id: 'rn_native_modules',
      title: 'Native Modules & the Bridge',
      description: 'Understand how React Native communicates with native iOS and Android code. Learn about the Bridge, TurboModules, Fabric, CodeGen, and how to write and link native modules.',
      level: 'ADVANCED',
      learningObjectives: [
        'Explain the role of the Bridge in classic React Native architecture',
        'Understand TurboModules and how they replace old native modules',
        'Write and link a custom native module for iOS and Android',
        'Describe how Fabric improves the rendering pipeline',
        'Use CodeGen for type-safe native module interfaces',
      ],
      content: [
        {'type': 'heading', 'text': 'The Classic Bridge Architecture'},
        {
          'type': 'explanation',
          'text': 'In the classic architecture, the Bridge is an asynchronous, serialized JSON message channel between the JavaScript thread and the native thread. All communication goes through the bridge: props, events, native module calls. The bridge is inherently asynchronous, creating a performance ceiling for high-frequency updates.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': '// On the JS side, calling a native module goes through the bridge\nimport { NativeModules } from "react-native";\nconst { CalendarModule } = NativeModules;\n\n// This call is serialized, sent over the bridge, and executed on native\nCalendarModule.createEvent("Meeting", "9am").then((result) => {\n  console.log("Event created with id:", result.id);\n});',
        },
        {'type': 'heading', 'text': 'Writing a Native Module (iOS)'},
        {
          'type': 'explanation',
          'text': 'To create a native module on iOS, write an Objective-C or Swift class extending NSObject, annotated with RCT_EXPORT_MODULE(). Methods exported with RCT_EXPORT_METHOD become callable from JavaScript. Each method receives a resolve/reject promise pair or a callback.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': '// iOS native module in Objective-C\n// CalendarModule.m\n#import "React/RCTBridgeModule.h"\n\n@interface CalendarModule : NSObject <RCTBridgeModule>\n@end\n\n@implementation CalendarModule\nRCT_EXPORT_MODULE();\n\nRCT_EXPORT_METHOD(createEvent:(NSString *)name\n                  date:(NSString *)date\n                  resolver:(RCTPromiseResolveBlock)resolve\n                  rejecter:(RCTPromiseRejectBlock)reject)\n{\n  NSString *eventId = @"event-123";\n  resolve(eventId);\n}\n@end',
        },
        {'type': 'heading', 'text': 'Writing a Native Module (Android)'},
        {
          'type': 'explanation',
          'text': 'On Android, a native module is a Java or Kotlin class extending ReactContextBaseJavaModule. Methods annotated with @ReactMethod are exported to JavaScript. The module is registered in a ReactPackage which is added to the ReactNativeHost in the Application class.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': '// Android native module in Kotlin\n// CalendarModule.kt\nimport com.facebook.react.bridge.ReactApplicationContext\nimport com.facebook.react.bridge.ReactContextBaseJavaModule\nimport com.facebook.react.bridge.ReactMethod\nimport com.facebook.react.bridge.Promise\n\nclass CalendarModule(reactContext: ReactApplicationContext) :\n  ReactContextBaseJavaModule(reactContext) {\n\n  override fun getName() = "CalendarModule"\n\n  @ReactMethod\n  fun createEvent(name: String, date: String, promise: Promise) {\n    val eventId = "event-123"\n    promise.resolve(eventId)\n  }\n}',
        },
        {'type': 'heading', 'text': 'TurboModules'},
        {
          'type': 'explanation',
          'text': 'TurboModules replace the old NativeModules system. They are lazily loaded (only instantiated when first called), use JavaScriptInterface instead of serialized JSON for communication, and support CodeGen for type safety. They can be synchronous or asynchronous and are defined by a JS spec.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': '// TurboModule spec (NativeCalendar.ts)\nimport type { TurboModule } from "react-native";\nimport { TurboModuleRegistry } from "react-native";\n\nexport interface Spec extends TurboModule {\n  createEvent(name: string, date: string): Promise<string>;\n  getEvents(): Promise<Array<{ id: string; name: string }>>;\n}\n\nexport default TurboModuleRegistry.getEnforcing<Spec>("CalendarModule");',
        },
        {'type': 'heading', 'text': 'Fabric: The New Renderer'},
        {
          'type': 'explanation',
          'text': 'Fabric replaces the old renderer with a C++ core supporting synchronous layout and rendering. It uses C++ Shadow Nodes for layout computation, enables concurrent rendering, and shares C++ code between iOS and Android. The render pipeline: JS creates elements, Fabric creates Shadow Nodes, Yoga computes layout, platform renderer commits to screen.',
        },
        {
          'type': 'explanation',
          'text': 'Because layout computation happens in C++ and can be synchronous, scroll and gesture callbacks can read layout information without waiting for an async bridge round-trip. This is critical for performant scrolling and gesture-driven animations.',
        },
        {'type': 'heading', 'text': 'CodeGen'},
        {
          'type': 'explanation',
          'text': 'CodeGen generates type-safe native code from JavaScript/TypeScript specs. Write a spec file extending TurboModuleRegistry for modules or defining component props, and CodeGen generates Objective-C++ headers, C++ interfaces, and Kotlin abstract classes. This ensures native implementations match the JS interface at compile time.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': '// Component spec for Fabric\nimport type { HostComponent, ViewProps } from "react-native";\nimport codegenNativeComponent from "react-native/Libraries/Utilities/codegenNativeComponent";\n\ntype MyComponentProps = ViewProps & {\n  title: string;\n  count: number;\n  onCustomEvent: (event: { value: string }) => void;\n};\n\nexport default codegenNativeComponent<MyComponentProps>(\n  "MyComponent"\n) as HostComponent<MyComponentProps>;',
        },
        {'type': 'heading', 'text': 'Event Emissions from Native'},
        {
          'type': 'explanation',
          'text': 'Native modules can send events to JavaScript using RCTEventEmitter on iOS and DeviceEventManager on Android. Events are useful for push notification taps, sensor data, and Bluetooth communication. On the JS side, addListener and removeListeners methods are required for proper cleanup.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { NativeEventEmitter, NativeModules } from "react-native";\n\nconst { CalendarModule } = NativeModules;\nconst emitter = new NativeEventEmitter(CalendarModule);\n\nuseEffect(() => {\n  const subscription = emitter.addListener(\n    "EventCreated",\n    (event) => console.log("Native event:", event)\n  );\n  return () => subscription.remove();\n}, []);',
        },
        {'type': 'heading', 'text': 'When to Write Native Modules'},
        {
          'type': 'explanation',
          'text': 'Write native modules when no existing library covers your use case. Common scenarios include platform-specific APIs (HealthKit, Google Fit, Bluetooth LE), hardware access (NFC, USB), or performance-critical operations (image processing, cryptography). Before writing a module, search npm and GitHub thoroughly.',
        },
        {'type': 'heading', 'text': 'Bridge Performance Bottleneck'},
        {
          'type': 'explanation',
          'text': 'The old Bridge serializes every message to JSON, creating garbage collection pressure and limiting throughput. High-frequency updates like scroll events, gesture tracking, and real-time animations suffer. The New Architecture mitigates this with JSI for direct C++ function calls, allowing synchronous access to native objects.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': '// JSI allows direct C++ access without JSON serialization\n// TurboModules use JSI for synchronous calls\n\nimport TurboModuleRegistry from "react-native/Libraries/TurboModule/TurboRegistry";\nconst module = TurboModuleRegistry.getEnforcing("NativeMath");\n// Synchronous call, no bridge serialization\nconst result = module.add(2, 3); // returns 5 directly',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'rn_native_modules_q1',
          question:
              'What is the primary role of the Bridge in classic React Native?',
          options: [
            'It renders components directly to the screen',
            'It is an asynchronous, serialized JSON channel between JavaScript and native threads',
            'It manages the app lifecycle and memory',
            'It handles network requests on behalf of JavaScript',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
          codeHeader: '[Bridge]',
          codeLanguage: 'JavaScript',
          codeLines: [
            '// JS calls native through the bridge',
            'CalendarModule.createEvent("Meeting", "9am")',
            '  .then((result) => {',
            '    console.log(result.id);',
            '  });',
          ],
        ),
        SeedQuestion(
          id: 'rn_native_modules_q2',
          question:
              'What is a key advantage of TurboModules over old NativeModules?',
          options: [
            'TurboModules only work on iOS',
            'TurboModules are lazily loaded, use JSI for direct C++ calls, and support CodeGen',
            'TurboModules automatically write native code for you',
            'TurboModules remove the need for any native platform code',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'rn_native_modules_q3',
          question: 'What does CodeGen generate from a TurboModule spec?',
          options: [
            'JavaScript runtime code for the app',
            'Type-safe native interfaces: Objective-C++ headers, C++ interfaces, and Kotlin abstract classes',
            'Unit tests for the native module',
            'Bundle optimization configuration',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'rn_native_modules_q4',
          question: 'How does Fabric improve over the old renderer?',
          options: [
            'Fabric removes the need for Yoga layout',
            'Fabric uses C++ Shadow Nodes for layout, supports synchronous rendering, and enables concurrent features',
            'Fabric is a replacement for React Navigation',
            'Fabric only works with Expo-managed projects',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'rn_native_modules_q5',
          question:
              'Why is the serialized JSON bridge a performance bottleneck?',
          options: [
            'JSON parsing is too fast and creates race conditions',
            'JSON serialization creates GC pressure and limits throughput for high-frequency updates',
            'JSON cannot represent nested objects',
            'JSON messages are encrypted which adds latency',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'rn_native_modules_q6',
          question: 'When writing an iOS native module with RCT_EXPORT_METHOD, what must you provide for async results?',
          options: [
            'A global JavaScript callback function',
            'A resolve/reject promise pair or a callback parameter',
            'A React state setter function',
            'A WebSocket connection for streaming',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
          codeHeader: '[iOS Module]',
          codeLanguage: 'JavaScript',
          codeLines: [
            'RCT_EXPORT_METHOD(createEvent:',
            '  (NSString *)name',
            '  resolver:(RCTPromiseResolveBlock)resolve',
            '  rejecter:(RCTPromiseRejectBlock)reject)',
            '{ resolve(eventId); }',
          ],
        ),
        SeedQuestion(
          id: 'rn_native_modules_q7',
          question: 'How do you register an Android native module so React Native can find it?',
          options: [
            'Add it to AndroidManifest.xml as a service',
            'Create a ReactPackage class and add it to ReactNativeHost packages list',
            'Place the Kotlin file in the assets directory',
            'Import it in the main Activity with a static import',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_native_modules_q8',
          question: 'How does JSI improve upon the classic Bridge?',
          options: [
            'JSI compresses JSON messages for faster transfer',
            'JSI allows direct C++ function calls from JavaScript without JSON serialization',
            'JSI replaces the JavaScript engine with a native one',
            'JSI only works on iOS and not Android',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'rn_native_modules_q9',
          question:
              'What should you check before writing a custom native module?',
          options: [
            'Whether the Android emulator supports native code',
            'Whether an existing community library on npm already covers the use case',
            'Whether your app uses class components',
            'Whether the New Architecture is disabled',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_native_modules_q10',
          question: 'What is the Fabric rendering pipeline?',
          options: [
            'JS creates elements, Bridge serializes to JSON, Yoga computes on JS thread, native renders',
            'JS creates React elements, Fabric creates C++ Shadow Nodes, Yoga computes layout in C++, platform renderer commits',
            'React elements are compiled to native Swift/Kotlin at build time',
            'The pipeline is identical to the old architecture but uses less memory',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
      ],
    ),
    SeedModule(
      id: 'rn_flexbox_layout',
      title: 'Flexbox & Layout',
      description: 'Go deep on the Yoga layout engine that powers every React Native screen. Master the flex shorthand, alignment and distribution, wrapping, gaps, and absolute positioning to build pixel-perfect, interview-ready layouts.',
      level: 'INTERMEDIATE',
      learningObjectives: [
        'Explain how the Yoga engine maps Flexbox concepts onto native Android and iOS layout',
        'Decode the flex shorthand into flexGrow, flexShrink, and flexBasis',
        'Distribute and align children along the main and cross axes',
        'Wrap items and use gap for consistent, native spacing',
        'Position children absolutely and control stacking order with zIndex',
      ],
      content: [
        {'type': 'heading', 'text': 'The Yoga Engine and Flexbox Defaults'},
        {
          'type': 'explanation',
          'text': 'React Native renders layout with Yoga, a cross-platform Flexbox engine implemented in C++ that ships on Android, iOS, and web. Layout is computed natively, off the JavaScript thread, from the style tree React builds. Unlike web CSS, Yoga defaults to flexDirection: column, so the main axis runs vertically, and alignItems defaults to stretch. There are no media queries: screen dimensions come from useWindowDimensions or Dimensions.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { View } from "react-native";\n\n// Main axis is vertical by default (flexDirection: "column")\nfunction Stack() {\n  return (\n    <View style={{ flexDirection: "column" }}>\n      <View style={{ height: 80, backgroundColor: "tomato" }} />\n      <View style={{ height: 80, backgroundColor: "steelblue" }} />\n      <View style={{ height: 80, backgroundColor: "seagreen" }} />\n    </View>\n  );\n}',
        },
        {'type': 'heading', 'text': 'The flex Shorthand: Grow, Shrink, Basis'},
        {
          'type': 'explanation',
          'text': 'flex: 1 means flexGrow: 1, flexShrink: 1, flexBasis: 0. With a basis of 0, every child starts at zero size on the main axis and then grows in proportion to its flex value, so flex: 2 claims twice the free space of flex: 1. Override the individual longhand properties when you want a child to keep its natural size but still grow to fill leftovers.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { View } from "react-native";\n\n// flex: 2 takes twice the space of flex: 1\nfunction Split() {\n  return (\n    <View style={{ flexDirection: "row", height: 120 }}>\n      <View style={{ flex: 2, backgroundColor: "tomato" }} />\n      <View style={{ flex: 1, backgroundColor: "steelblue" }} />\n      <View style={{ flex: 1, backgroundColor: "seagreen" }} />\n    </View>\n  );\n}',
        },
        {'type': 'heading', 'text': 'Aligning and Distributing Children'},
        {
          'type': 'explanation',
          'text': 'justifyContent distributes children along the main axis (flex-start, flex-end, center, space-between, space-around, space-evenly). alignItems and the per-child alignSelf control the cross axis; the default alignItems: stretch makes children fill the cross axis unless they have an explicit size. alignContent only has an effect when flexWrap wraps children onto multiple lines.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { View } from "react-native";\n\nfunction Toolbar() {\n  return (\n    <View\n      style={{\n        flexDirection: "row",\n        justifyContent: "space-between",\n        alignItems: "center",\n        height: 120,\n      }}\n    >\n      <View style={{ width: 40, height: 40, backgroundColor: "tomato" }} />\n      <View style={{ width: 40, height: 40, backgroundColor: "steelblue" }} />\n      <View style={{ width: 40, height: 40, backgroundColor: "seagreen" }} />\n    </View>\n  );\n}',
        },
        {'type': 'heading', 'text': 'Wrapping, Gap, and Absolute Positioning'},
        {
          'type': 'explanation',
          'text': 'Set flexWrap: "wrap" so children flow onto new lines instead of shrinking onto one. gap (and rowGap / columnGap) adds consistent spacing between children on both axes, applied natively by Yoga, replacing margin-based spacing hacks. For overlays, position: "absolute" removes a child from the flex flow and anchors it to the nearest padded parent using top, right, bottom, and left; zIndex (iOS) and elevation (Android) control stacking order between siblings.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { View, Text, StyleSheet } from "react-native";\n\nfunction Grid() {\n  return (\n    <View style={{ flexDirection: "row", flexWrap: "wrap", gap: 12 }}>\n      <View style={styles.tile} />\n      <View style={styles.tile} />\n      <View style={styles.tile} />\n      <View style={styles.tile} />\n    </View>\n  );\n}\n\nconst styles = StyleSheet.create({\n  tile: { width: "48%", height: 80, backgroundColor: "#61DAFB" },\n});',
        },
        {'type': 'heading', 'text': 'A Complete Positioning Example'},
        {
          'type': 'explanation',
          'text': 'Combine absolute positioning with alignment to build a badge overlaying a card. The card centers its label with justifyContent and alignItems, while the badge sits in the top-right corner via position: "absolute". This is the standard pattern for notification counts, drag handles, and close buttons.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { View, Text, StyleSheet } from "react-native";\n\nfunction Card() {\n  return (\n    <View style={styles.card}>\n      <Text>Grocery list</Text>\n      <View style={styles.badge}>\n        <Text style={styles.badgeText}>5</Text>\n      </View>\n    </View>\n  );\n}\n\nconst styles = StyleSheet.create({\n  card: {\n    width: 240,\n    height: 120,\n    justifyContent: "center",\n    alignItems: "center",\n  },\n  badge: {\n    position: "absolute",\n    top: 8,\n    right: 8,\n    width: 24,\n    height: 24,\n    borderRadius: 12,\n    backgroundColor: "tomato",\n  },\n  badgeText: { color: "#fff", textAlign: "center" },\n});',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'rn_flexbox_layout_q1',
          question: 'What is the default flexDirection in the Yoga engine used by React Native?',
          options: ['row', 'column', 'column-reverse', 'inherit'],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'rn_flexbox_layout_q2',
          question: 'What does flex: 1 expand to in Yoga?',
          options: [
            'flexGrow: 1, flexShrink: 0, flexBasis: auto',
            'flexGrow: 1, flexShrink: 1, flexBasis: 0',
            'flexGrow: 0, flexShrink: 1, flexBasis: auto',
            'flexGrow: 1, flexShrink: 1, flexBasis: "100%"',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
          codeHeader: '[Flex Shorthand]',
          codeLanguage: 'JavaScript',
          codeLines: [
            'flex: 1;',
            '// expands to',
            'flexGrow: 1;',
            'flexShrink: 1;',
            'flexBasis: 0;',
          ],
        ),
        SeedQuestion(
          id: 'rn_flexbox_layout_q3',
          question: 'In the layout below, which child claims the most horizontal space?',
          options: [
            'The first child',
            'The second child',
            'The third child',
            'All children have equal width',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
          codeHeader: '[Flex Distribution]',
          codeLanguage: 'JavaScript',
          codeLines: [
            '<View style={{ flexDirection: "row" }}>',
            '  <View style={{ flex: 1 }} />',
            '  <View style={{ flex: 3 }} />',
            '  <View style={{ flex: 2 }} />',
            '</View>',
          ],
        ),
        SeedQuestion(
          id: 'rn_flexbox_layout_q4',
          question: 'A row of three children needs equal spacing between them with no space at the outer edges. Which justifyContent value achieves this?',
          options: ['flex-start', 'space-between', 'space-around', 'center'],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_flexbox_layout_q5',
          question: 'What is the default value of alignItems in React Native?',
          options: ['flex-start', 'center', 'stretch', 'baseline'],
          correctIndex: 2,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'rn_flexbox_layout_q6',
          question: 'Which property lets children flow onto multiple lines instead of shrinking to fit one row?',
          options: [
            'flexGrow',
            'flexWrap: "wrap"',
            'alignItems: "stretch"',
            'position: "absolute"',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_flexbox_layout_q7',
          question: 'Two absolutely positioned siblings overlap. Which view renders on top?',
          options: [
            'The tomato view',
            'The steelblue view',
            'The last child always wins regardless of zIndex',
            'Rendering order is undefined on Android',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
          codeHeader: '[zIndex]',
          codeLanguage: 'JavaScript',
          codeLines: [
            '<View>',
            '  <View style={{ position: "absolute", zIndex: 1, backgroundColor: "tomato" }} />',
            '  <View style={{ position: "absolute", zIndex: 2, backgroundColor: "steelblue" }} />',
            '</View>',
          ],
        ),
        SeedQuestion(
          id: 'rn_flexbox_layout_q8',
          question: 'Why is gap generally preferred over per-child margin hacks in a wrapping flex row?',
          options: [
            'gap is applied natively by Yoga and stays uniform even when items wrap, while margins add unwanted space at the edges',
            'margin is unsupported for children of a flex container',
            'gap doubles the effective spacing compared to margin',
            'margin is ignored whenever flexWrap is active',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'rn_flexbox_layout_q9',
          question: 'Given the code below, what stretch does the child receive on the cross axis?',
          options: [
            'The child has no height, so it collapses to 0',
            'It grows to 200 because alignItems: stretch makes the child fill the cross axis',
            'It depends on the child content height',
            'It is fixed at 50 percent of the container',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
          codeHeader: '[Cross Axis]',
          codeLanguage: 'JavaScript',
          codeLines: [
            '<View style={{ flexDirection: "row", alignItems: "stretch", height: 200 }}>',
            '  <View style={{ backgroundColor: "tomato" }} />',
            '</View>',
          ],
        ),
        SeedQuestion(
          id: 'rn_flexbox_layout_q10',
          question: 'Which statement about alignContent is correct?',
          options: [
            'It distributes space on the cross axis but only has an effect when children wrap onto multiple lines',
            'It is identical to alignSelf applied to the parent',
            'It only works with flexDirection: column',
            'It controls spacing along the main axis',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
      ],
    ),
    SeedModule(
      id: 'rn_networking_persistence',
      title: 'Networking & Persistence',
      description: 'Learn how real React Native apps talk to servers and save data. Cover fetch and axios, request cancellation and timeouts, AsyncStorage, secure Keychain and Keystore storage, and offline-aware networking with NetInfo.',
      level: 'INTERMEDIATE',
      learningObjectives: [
        'Make HTTP requests with fetch and axios and parse JSON responses',
        'Cancel and timeout in-flight requests with AbortController',
        'Persist key-value data with AsyncStorage and respect its limits',
        'Store secrets on-device with Keychain and Keystore backed libraries',
        'React to connectivity changes with NetInfo and handle offline mode',
      ],
      content: [
        {'type': 'heading', 'text': 'fetch and axios'},
        {
          'type': 'explanation',
          'text': 'React Native ships a global fetch implementation that matches the browser API and integrates with promises and async/await. Axios adds conveniences like default timeouts, interceptors, and clean JSON serialization. Whichever you choose, do not let an in-flight request settle state on an unmounted screen, or you risk updating state for a screen the user already left.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import axios from "axios";\n\nconst api = axios.create({\n  baseURL: "https://api.example.com",\n  timeout: 10000,\n});\n\nasync function loadProfile(userId) {\n  const { data } = await api.get("/users/" + userId);\n  return data;\n}\n\n// The same call with fetch:\nasync function loadProfileFetch(userId) {\n  const res = await fetch("https://api.example.com/users/" + userId);\n  if (!res.ok) throw new Error("HTTP " + res.status);\n  return res.json();\n}',
        },
        {'type': 'heading', 'text': 'Cancellation and Timeouts'},
        {
          'type': 'explanation',
          'text': 'fetch has no built-in timeout, so enforce one with AbortController. Aborting the controller both enforces a deadline and lets the user cancel a slow request. The promise rejects with an error whose name is AbortError, which your catch block should distinguish from real network failures. Axios supports the same signal plus a timeout option.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'const controller = new AbortController();\nconst timer = setTimeout(() => controller.abort(), 8000);\n\ntry {\n  const res = await fetch(url, { signal: controller.signal });\n  const data = await res.json();\n} catch (err) {\n  if (err.name === "AbortError") {\n    console.log("Request timed out or was cancelled");\n  } else {\n    console.log("Network error", err.message);\n  }\n} finally {\n  clearTimeout(timer);\n}',
        },
        {'type': 'heading', 'text': 'AsyncStorage'},
        {
          'type': 'explanation',
          'text': 'AsyncStorage is the community-maintained, asynchronous, unencrypted key-value store for React Native. It serializes strings only, so objects need JSON.stringify. It is backed by SQLite on Android and a file-based implementation on iOS, and is fast enough for caches, drafts, and settings, but not for large binary blobs. Persist only non-sensitive data; tokens and passwords belong in encrypted storage.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import AsyncStorage from "@react-native-async-storage/async-storage";\n\nconst KEY = "onboarding_done";\n\nasync function persistFlag() {\n  await AsyncStorage.setItem(KEY, JSON.stringify(true));\n}\n\nasync function readFlag() {\n  const raw = await AsyncStorage.getItem(KEY);\n  return raw != null ? JSON.parse(raw) : false;\n}',
        },
        {'type': 'heading', 'text': 'Secure Storage'},
        {
          'type': 'explanation',
          'text': 'For access tokens, refresh tokens, and other secrets, use libraries that back onto the iOS Keychain and the Android Keystore, such as react-native-keychain or a Keystore-backed mmkv build. These encrypt data at rest and, on iOS, survive app reinstallation. Never store credentials in plain AsyncStorage or in Redux persistence, where they are trivially readable.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import * as Keychain from "react-native-keychain";\n\nasync function saveToken(token) {\n  await Keychain.setGenericPassword("session", token, {\n    service: "com.example.session",\n  });\n}\n\nasync function getToken() {\n  const creds = await Keychain.getGenericPassword({\n    service: "com.example.session",\n  });\n  return creds ? creds.password : null;\n}',
        },
        {'type': 'heading', 'text': 'Offline Awareness with NetInfo'},
        {
          'type': 'explanation',
          'text': 'NetInfo reports the current network state: the connection type (wifi, cellular, none) and whether the internet is reachable. The useNetInfo hook re-renders when connectivity changes, so you can show an offline banner, queue mutations, and halt premature API calls. Combine it with AsyncStorage for cache-first reads that keep the UI usable offline.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { Text, StyleSheet } from "react-native";\nimport { useNetInfo } from "@react-native-community/netinfo";\n\nfunction StatusBanner() {\n  const netInfo = useNetInfo();\n\n  if (netInfo.isInternetReachable === false) {\n    return <Text style={styles.offline}>You are offline</Text>;\n  }\n  return null;\n}\n\nconst styles = StyleSheet.create({\n  offline: { padding: 8, color: "#fff", backgroundColor: "#d32f2f" },\n});',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'rn_networking_persistence_q1',
          question: 'Which HTTP API does React Native provide out of the box?',
          options: [
            'XMLHttpRequest only, through a WebView',
            'A global fetch that follows the browser API',
            'Native URLSession exposed directly via NativeModules',
            'Axios and no other option',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'rn_networking_persistence_q2',
          question: 'What must you do before storing a JavaScript object in AsyncStorage?',
          options: [
            'Convert it to a string with JSON.stringify',
            'Base64-encode it',
            'Nothing; AsyncStorage accepts objects directly',
            'Split it across multiple keys',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'rn_networking_persistence_q3',
          question: 'Why should an unmounted screen avoid calling setState from a fetch response?',
          options: [
            'setState throws immediately when the component unmounts',
            'It updates state for a screen the user already left, wasting work and risking memory leak warnings on native',
            'React Native forbids promises after unmount',
            'fetch results are always cached, so the call is redundant',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_networking_persistence_q4',
          question: 'What happens when the AbortController signal in the code below fires?',
          options: [
            'fetch resolves with an empty response',
            'fetch rejects with an error whose name is AbortError',
            'fetch retries the request automatically',
            'fetch returns a 408 status code',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
          codeHeader: '[AbortController]',
          codeLanguage: 'JavaScript',
          codeLines: [
            'const controller = new AbortController();',
            'fetch(url, { signal: controller.signal })',
            '  .then((res) => res.json())',
            '  .catch((err) => {',
            '    if (err.name === "AbortError") {',
            '      console.log("Request cancelled");',
            '    }',
            '  });',
          ],
        ),
        SeedQuestion(
          id: 'rn_networking_persistence_q5',
          question: 'Why is storing an access token in plain AsyncStorage considered insecure?',
          options: [
            'AsyncStorage is unencrypted and stored in files or an SQLite database readable within the app sandbox',
            'AsyncStorage can only hold numbers, not strings',
            'AsyncStorage synchronously blocks the UI thread on writes',
            'AsyncStorage automatically deletes data after 24 hours',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_networking_persistence_q6',
          question: 'What does the axios response interceptor below achieve?',
          options: [
            'It logs every request to the server',
            'It transparently refreshes the session and retries a request that failed with 401',
            'It blocks all responses with status 401',
            'It converts network errors into 401 responses',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
          codeHeader: '[Axios Interceptor]',
          codeLanguage: 'JavaScript',
          codeLines: [
            'api.interceptors.response.use(',
            '  (response) => response,',
            '  async (error) => {',
            '    if (error.response && error.response.status === 401) {',
            '      await refreshSession();',
            '      const config = error.config;',
            '      const { data } = await api.request(config);',
            '      return data;',
            '    }',
            '    return Promise.reject(error);',
            '  }',
            ');',
          ],
        ),
        SeedQuestion(
          id: 'rn_networking_persistence_q7',
          question: 'What does useNetInfo from the NetInfo library provide?',
          options: [
            'The current network state, including connection type and internet reachability',
            'The device IP address and DNS records',
            'A WebSocket connection manager',
            'The current cellular carrier plan',
          ],
          correctIndex: 0,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'rn_networking_persistence_q8',
          question: 'What pattern does the function below implement?',
          options: [
            'Server-authoritative cache invalidation',
            'Cache-first read with a network fill',
            'Polling with exponential backoff',
            'Feature-flag based configuration loading',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
          codeHeader: '[Cache First]',
          codeLanguage: 'JavaScript',
          codeLines: [
            'async function getCachedUsers() {',
            '  const cached = await AsyncStorage.getItem("users");',
            '  if (cached) return JSON.parse(cached);',
            '',
            '  const res = await fetch("https://api.example.com/users");',
            '  const users = await res.json();',
            '  await AsyncStorage.setItem("users", JSON.stringify(users));',
            '  return users;',
            '}',
          ],
        ),
        SeedQuestion(
          id: 'rn_networking_persistence_q9',
          question: 'Which statement about react-native-mmkv is accurate?',
          options: [
            'It stores values synchronously using memory-mapped files, making it faster than AsyncStorage',
            'It is the official Facebook-maintained AsyncStorage successor',
            'It works only on Android',
            'It cannot persist values across app restarts',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'rn_networking_persistence_q10',
          question: 'For a temporary network failure on a 5xx error, which retry strategy is most defensible in a mobile app?',
          options: [
            'Retry immediately up to five times with no delay',
            'Retry a bounded number of times with exponential backoff and jitter',
            'Never retry; always surface the error',
            'Retry for up to an hour regardless of connectivity',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
      ],
    ),
    SeedModule(
      id: 'rn_performance',
      title: 'Performance Optimization',
      description: 'Make React Native screens fast: understand the frame budget, avoid wasted re-renders with memoization, tune virtualized lists, defer non-urgent work with InteractionManager, and profile with the right tools.',
      level: 'ADVANCED',
      learningObjectives: [
        'Explain the 60 fps frame budget and where JavaScript work destroys it',
        'Prevent wasted renders with React.memo, useMemo, and useCallback',
        'Tune FlatList virtualization for long lists with windowing props',
        'Defer non-critical work until animations settle with InteractionManager',
        'Profile JavaScript work to find real bottlenecks before optimizing',
      ],
      content: [
        {'type': 'heading', 'text': 'The 60 fps Frame Budget'},
        {
          'type': 'explanation',
          'text': 'A 60 fps UI has roughly 16.6 ms per frame. React spreads work across the JavaScript thread, which runs React and your application logic, and the native UI thread, which draws the screen. If the JS thread is busy in a long synchronous operation, frames are delayed and the interface visibly stutters. Keep heavy work off the render path, let animations run on the JS thread with a native driver, and defer anything non-urgent.',
        },
        {'type': 'heading', 'text': 'Avoiding Wasted Re-renders'},
        {
          'type': 'explanation',
          'text': 'React re-renders a child whenever its parent re-renders, even if the child props look identical. React.memo bails out when props are shallow-equal, but only if the props are stable: memoizing callbacks with useCallback and computed values with useMemo is what keeps those props stable. An inline arrow function or inline object literal in a prop breaks memoization by creating a fresh reference every render.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import React, { memo, useCallback, useState } from "react";\nimport { View, Text, Pressable } from "react-native";\n\nconst Row = memo(({ item, onPress }) => (\n  <Pressable onPress={() => onPress(item.id)}>\n    <Text>{item.title}</Text>\n  </Pressable>\n));\n\nfunction List() {\n  const [page, setPage] = useState(1);\n\n  const handlePress = useCallback((id) => {\n    console.log("open", id);\n  }, []);\n\n  return items.slice(0, page * 20).map((item) => (\n    <Row key={item.id} item={item} onPress={handlePress} />\n  ));\n}',
        },
        {'type': 'heading', 'text': 'Virtualized Lists and FlatList'},
        {
          'type': 'explanation',
          'text': 'FlatList renders only the cells inside a sliding window around the viewport and recycles rows as you scroll. Tune it with keyExtractor for stable keys, initialNumToRender for the first batch, maxToRenderPerBatch to bound work per frame, windowSize to control how many viewport heights stay mounted, and getItemLayout so fixed-height rows skip on-measurement entirely. Rows should be memoized and their renderItem callback stable at the parent.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { FlatList, Platform } from "react-native";\n\nconst ITEM_HEIGHT = 72;\n\n<FlatList\n  data={items}\n  keyExtractor={(item) => item.id}\n  initialNumToRender={10}\n  maxToRenderPerBatch={10}\n  windowSize={7}\n  removeClippedSubviews={Platform.OS === "android"}\n  getItemLayout={(_, index) => ({\n    length: ITEM_HEIGHT,\n    offset: ITEM_HEIGHT * index,\n    index,\n  })}\n  renderItem={renderItem}\n/>',
        },
        {'type': 'heading', 'text': 'Deferring Work with InteractionManager'},
        {
          'type': 'explanation',
          'text': 'InteractionManager schedules callbacks to run after ongoing animations and interactions finish. createInteractionHandle() and clearInteractionHandle() keep work deferred while a gesture or transition is active. Use it for non-essential bootstrap work, such as preloading secondary screens or applying a heavy local transform, so the first animation stays at 60 fps.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': 'import { useEffect } from "react";\nimport { InteractionManager } from "react-native";\n\nfunction Comments() {\n  useEffect(() => {\n    const handle = InteractionManager.createInteractionHandle();\n\n    InteractionManager.runAfterInteractions(async () => {\n      const comments = await loadComments();\n      setComments(comments);\n      InteractionManager.clearInteractionHandle(handle);\n    });\n\n    return () => InteractionManager.clearInteractionHandle(handle);\n  }, []);\n}',
        },
        {'type': 'heading', 'text': 'Profiling and Measuring'},
        {
          'type': 'explanation',
          'text': 'Use the React DevTools Profiler to see which components render and why, and Dev Settings > Show Perf Monitor or Flipper for a live frame overview. The why-did-you-render library in development pinpoints re-render storms. Always measure before and after an optimization on a low-end device: fixing the wrong hotspot is a common waste of time.',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'rn_performance_q1',
          question: 'At 60 fps, roughly how many milliseconds are available to produce each frame?',
          options: ['10 ms', '16.6 ms', '25 ms', '60 ms'],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'rn_performance_q2',
          question: 'Why does an inline arrow function passed as onPress defeat React.memo?',
          options: [
            'The inline arrow creates a new function reference each render, so the shallow prop comparison always fails and the memoized row still re-renders',
            'React.memo ignores function props entirely',
            'Inline arrows execute more slowly at call time',
            'Only callback props wrapped in useCallback are executed',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
          codeHeader: '[Memo Pitfall]',
          codeLanguage: 'JavaScript',
          codeLines: [
            'function List() {',
            '  return items.map((item) => (',
            '    <Row key={item.id} item={item} onPress={(id) => open(id)} />',
            '  ));',
            '}',
          ],
        ),
        SeedQuestion(
          id: 'rn_performance_q3',
          question: 'What does useMemo actually do?',
          options: [
            'It prevents a component from re-rendering',
            'It caches the result of a computation until a dependency changes',
            'It runs computations on a background thread',
            'It memoizes the rendered output for reuse',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_performance_q4',
          question: 'Which FlatList setup is best for a long list of fixed-height rows?',
          options: [
            'windowSize: 0 and removeClippedSubviews: false',
            'getItemLayout plus a stable keyExtractor and a memoized row',
            'initialNumToRender: 0 and maxToRenderPerBatch: 1000',
            'disableVirtualization: true',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
          codeHeader: '[FlatList]',
          codeLanguage: 'JavaScript',
          codeLines: [
            '<FlatList',
            '  data={items}',
            '  keyExtractor={(item) => item.id}',
            '  initialNumToRender={10}',
            '  maxToRenderPerBatch={10}',
            '  windowSize={7}',
            '  getItemLayout={(_, index) => ({',
            '    length: ITEM_HEIGHT,',
            '    offset: ITEM_HEIGHT * index,',
            '    index,',
            '  })}',
            '/>',
          ],
        ),
        SeedQuestion(
          id: 'rn_performance_q5',
          question: 'What is jank?',
          options: [
            'A network error during bundle download',
            'Visibly stuttering UI caused by frames that exceed the 16.6 ms budget',
            'The Android-only crash reporter',
            'A state update that exceeds 500 MB of memory',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_performance_q6',
          question: 'When a screen is transitioning, what does InteractionManager.runAfterInteractions accomplish?',
          options: [
            'It guarantees the animation completes instantly',
            'It defers the callback until ongoing animations and interactions finish, keeping the transition smooth',
            'It pauses all JavaScript execution during the animation',
            'It runs the callback on the native thread',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'rn_performance_q7',
          question: 'Which development tool reports dropped frames or JS thread utilization?',
          options: [
            'The Metro bundler stats screen',
            'Perf Monitor in Dev Settings, or the Flipper Performance plugin',
            'The Android logcat only',
            'Xcode Instruments on iOS only',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'rn_performance_q8',
          question: 'Why can many small native-module calls inside a tight loop be slow?',
          options: [
            'Each call crosses the JavaScript/native boundary, and the legacy bridge adds JSON serialization overhead per call',
            'Native modules are limited to one call per second',
            'React will not execute native code from a loop',
            'The calls are deliberately delayed to avoid starving the UI thread',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'rn_performance_q9',
          question: 'Which is the safest way to update a single row in a memoized list rendered from a state array?',
          options: [
            'Mutate the item object in place so its reference stays the same',
            'Create a new updated item object and replace it in a new array so only affected rows get new references',
            'Call setState on every row',
            'Inject raw HTML into the Text component',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_performance_q10',
          question: 'A screen stutters because the JS thread is blocked by a long synchronous operation. Which change most directly relieves the symptom?',
          options: [
            'Moving the operation into a setInterval',
            'Splitting the operation into chunks that yield to the JS thread between items, or deferring it with InteractionManager',
            'Increasing windowSize on the FlatList',
            'Switching the JavaScript engine from Hermes to V8',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
      ],
    ),
    SeedModule(
      id: 'rn_new_architecture',
      title: 'The New Architecture',
      description: 'Understand the post-bridge future of React Native: Fabric, TurboModules, JSI, and Codegen. See how they remove the JSON bridge, hand JavaScript direct C++ access, and become the default as of React Native 0.76.',
      level: 'ADVANCED',
      learningObjectives: [
        'Describe the four pillars: Fabric, TurboModules, JSI, and Codegen',
        'Explain how JSI replaces serialized bridge messages with direct C++ calls',
        'Understand how Codegen keeps JavaScript and native interfaces in sync',
        'Enumerate the benefits of the bridgeless runtime',
        'Outline a migration path for an existing app',
      ],
      content: [
        {'type': 'heading', 'text': 'The Four Pillars'},
        {
          'type': 'explanation',
          'text': 'The New Architecture replaces the classic asynchronous bridge with four coordinated pieces. JSI is the JavaScript-to-C++ interface that removes serialization, Fabric is the new renderer backed by a C++ shadow tree, TurboModules replace legacy native modules with lazily initialized spec-driven modules, and Codegen generates native glue from JavaScript or TypeScript specs. It is the default starting with React Native 0.76 and Expo SDK 52.',
        },
        {'type': 'heading', 'text': 'JSI: The JavaScript Interface'},
        {
          'type': 'explanation',
          'text': 'JSI is a C++ abstraction between the JavaScript engine (Hermes by default) and native code. It lets host objects be shared with JavaScript so native and JS can call each other directly, synchronously, without JSON serialization. A TurboModule exposed over JSI reads like a plain synchronous function call, which eliminates the round-trip latency and garbage collection pressure of the legacy bridge.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': '// Legacy bridge: asynchronous JSON round trip\nNativeModules.Calendar.createEvent("talk")\n  .then((id) => console.log(id));\n\n// JSI-backed TurboModule: direct and synchronous\nconst id = NativeCalendar.createEvent("talk");\nconsole.log(id);',
        },
        {'type': 'heading', 'text': 'Fabric: The New Renderer'},
        {
          'type': 'explanation',
          'text': 'Fabric keeps a C++ shadow tree that mirrors the React element tree. Layout is computed in C++ with Yoga, so a screen can measure synchronously and gestures can read positions without a bridge round-trip. Fabric also enables React 18 concurrent features. A compatibility layer lets components written for the old renderer keep rendering during migration.',
        },
        {'type': 'heading', 'text': 'TurboModules and Codegen'},
        {
          'type': 'explanation',
          'text': 'A TurboModule is defined once as a TypeScript spec. Codegen reads the spec at build time and generates the typed bindings business side and native side: C++ interfaces, Objective-C++ headers, and Kotlin abstract classes. The generated surface means a typo in a method name or argument type fails the build instead of failing at runtime. Modules are lazily initialized on first use.',
        },
        {
          'type': 'code',
          'language': 'JavaScript',
          'code': '// NativeBattery.ts\nimport type { TurboModule } from "react-native";\nimport { TurboModuleRegistry } from "react-native";\n\nexport interface Spec extends TurboModule {\n  getBatteryLevel(): number;\n  isCharging(): boolean;\n}\n\nexport default TurboModuleRegistry.getEnforcing<Spec>("NativeBattery");',
        },
        {'type': 'heading', 'text': 'Bridgeless Runtime'},
        {
          'type': 'explanation',
          'text': 'In bridgeless mode the legacy RCTBridge is not initialized at all. JavaScript talks to native over JSI and events travel over C++, which trims startup time and removes the JSON bottleneck. Libraries that still rely on bridge APIs are kept running through the interop layer, so an app does not have to upgrade every dependency at once to enable the New Architecture.',
        },
      ],
      questions: [
        SeedQuestion(
          id: 'rn_new_architecture_q1',
          question: 'What is the set of Fabric, TurboModules, JSI, and Codegen collectively called?',
          options: [
            'The Tailwind renderer',
            'The New Architecture (bridgeless runtime)',
            'The Hermes revival',
            'The interop layer',
          ],
          correctIndex: 1,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'rn_new_architecture_q2',
          question: 'What does JSI stand for and what is its core purpose?',
          options: [
            'JavaScript Interface: a C++ API that lets JavaScript and native objects call each other directly without JSON serialization',
            'JavaScript Instance: a singleton for the Android runtime',
            'Java Swift Interop: a bridge for cross-language calls',
            'Just Inline Source: an in-memory bundler',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_new_architecture_q3',
          question: 'Which benefit does running layout in the Fabric C++ shadow tree provide?',
          options: [
            'Layout can be read synchronously, so gestures can query positions without waiting on the JS bridge',
            'Layout no longer uses Yoga',
            'Layout is computed only on the web thread',
            'Layout is precompiled into the binary at build time',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'rn_new_architecture_q4',
          question:
              'Why does Codegen matter for teams building native modules?',
          options: [
            'It writes the native implementation for you',
            'It generates type-safe bindings from a JS or TS spec, so interface mismatches fail the build',
            'It removes the need for native build tools',
            'It converts native code to JavaScript automatically',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'rn_new_architecture_q5',
          question:
              'How are TurboModules loaded compared to legacy NativeModules?',
          options: [
            'All modules are loaded at startup',
            'Lazily, on first use, through the TurboModuleRegistry',
            'They are downloaded from a CDN on demand',
            'They load only when the app enters the background',
          ],
          correctIndex: 1,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_new_architecture_q6',
          question: 'Which JavaScript engine is the default for the New Architecture on Android?',
          options: ['V8', 'JavaScriptCore', 'Hermes', 'QuickJS'],
          correctIndex: 2,
          level: 'BEGINNER',
        ),
        SeedQuestion(
          id: 'rn_new_architecture_q7',
          question: 'What is the interop layer in the context of the New Architecture?',
          options: [
            'A compatibility shim that lets components and modules written for the legacy bridge keep working under Fabric and bridgeless mode',
            'A layer that converts TypeScript to Objective-C',
            'A network protocol for native debugging',
            'A tool that compiles C++ to Java bytecode',
          ],
          correctIndex: 0,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'rn_new_architecture_q8',
          question: 'Which statement best describes bridgeless mode?',
          options: [
            'The app no longer initializes the legacy RCTBridge; JS-to-native communication happens over JSI',
            'The app runs with no native code at all',
            'JavaScript runs without a runtime',
            'Only iOS supports bridgeless apps',
          ],
          correctIndex: 0,
          level: 'INTERMEDIATE',
        ),
        SeedQuestion(
          id: 'rn_new_architecture_q9',
          question: 'A TurboModule spec declares a method returning Promise<string>. What must the native implementation do?',
          options: [
            'Return the string synchronously from C++',
            'Resolve the promise when the work completes, matching the signature Codegen expects',
            'Throw on every call',
            'Deliver the string as an event payload',
          ],
          correctIndex: 1,
          level: 'ADVANCED',
        ),
        SeedQuestion(
          id: 'rn_new_architecture_q10',
          question: 'Starting with which React Native version is the New Architecture enabled by default?',
          options: ['0.66', '0.71', '0.76', '1.0'],
          correctIndex: 2,
          level: 'BEGINNER',
        ),
      ],
    ),
  ],
  flipcards: [
    SeedFlipcard(
      id: 'rn_fc_component',
      term: 'Component',
      definition: 'A reusable building block in React Native that encapsulates UI and logic. Components are JavaScript functions that accept props and return JSX describing what should appear on screen.',
    ),
    SeedFlipcard(
      id: 'rn_fc_props',
      term: 'Props',
      definition: 'Short for properties. Read-only inputs passed from a parent component to a child. Props flow one direction down the component tree and must never be modified by the receiving component.',
    ),
    SeedFlipcard(
      id: 'rn_fc_state',
      term: 'State',
      definition: 'Internal data managed by a component that triggers re-renders when updated. Initialized with useState in functional components. Each component instance maintains its own independent state.',
    ),
    SeedFlipcard(
      id: 'rn_fc_jsx',
      term: 'JSX',
      definition: 'A syntax extension for JavaScript that looks like HTML and compiles to React.createElement calls. Allows embedding JavaScript expressions inside curly braces for dynamic UI.',
    ),
    SeedFlipcard(
      id: 'rn_fc_key',
      term: 'Key',
      definition: 'A special prop assigned to list items to help React identify which elements changed, were added, or were removed. Keys should be stable, unique IDs from your data, not array indices.',
    ),
    SeedFlipcard(
      id: 'rn_fc_virtual_dom',
      term: 'Virtual DOM',
      definition: 'A lightweight JavaScript representation of the native UI tree. React diffs the new virtual tree against the previous one (reconciliation) and applies only the minimal set of native updates needed.',
    ),
    SeedFlipcard(
      id: 'rn_fc_hook',
      term: 'Hook',
      definition: 'A function that lets functional components use state and lifecycle features. Must be called at the top level of a component or custom hook, never inside conditions or loops.',
    ),
    SeedFlipcard(
      id: 'rn_fc_useeffect',
      term: 'useEffect',
      definition: 'A hook that runs side effects after render: data fetching, subscriptions, timers. The dependency array controls re-run frequency. The return function handles cleanup on unmount or before re-runs.',
    ),
    SeedFlipcard(
      id: 'rn_fc_react_navigation',
      term: 'React Navigation',
      definition: 'The standard navigation library for React Native providing stack, tab, and drawer navigators. Manages screen transitions, header bars, gestures, deep linking, and navigation state.',
    ),
    SeedFlipcard(
      id: 'rn_fc_redux',
      term: 'Redux',
      definition: 'A predictable state container for JavaScript apps. Redux Toolkit provides createSlice, createAsyncThunk, and RTK Query for simplified, type-safe state management with middleware support.',
    ),
    SeedFlipcard(
      id: 'rn_fc_flexbox',
      term: 'Flexbox',
      definition: 'The layout algorithm used by React Native via the Yoga engine. Arranges children along a main and cross axis. React Native defaults to flexDirection: column, unlike web CSS which defaults to row.',
    ),
    SeedFlipcard(
      id: 'rn_fc_turbomodule',
      term: 'TurboModule',
      definition: 'The replacement for old NativeModules in the New Architecture. TurboModules are lazily loaded, use JSI for direct C++ calls instead of the JSON bridge, and support CodeGen for type-safe native interfaces.',
    ),
    SeedFlipcard(
      id: 'rn_fc_fabric',
      term: 'Fabric',
      definition: 'The New Architecture renderer. Fabric keeps a C++ shadow tree that mirrors the React element tree, computes layout with Yoga in C++, and commits the minimal native view diff. It enables synchronous measurement and React 18 concurrent features.',
    ),
    SeedFlipcard(
      id: 'rn_fc_jsi',
      term: 'JSI (JavaScript Interface)',
      definition: 'A C++ API between the JavaScript engine and native code. It exposes native host objects to JavaScript and allows direct, synchronous calls, removing the JSON serialization overhead of the classic bridge.',
    ),
    SeedFlipcard(
      id: 'rn_fc_bridgeless',
      term: 'Bridgeless mode',
      definition: 'A New Architecture mode where the legacy RCTBridge is not initialized. JavaScript talks to native over JSI and events travel over C++, reducing startup time and removing the JSON bridge bottleneck.',
    ),
    SeedFlipcard(
      id: 'rn_fc_codegen',
      term: 'Codegen',
      definition: 'The New Architecture build step that reads JavaScript and TypeScript specs for TurboModules and Fabric components and generates type-safe native glue (C++, Objective-C++, Kotlin) so interface mismatches fail at build time.',
    ),
    SeedFlipcard(
      id: 'rn_fc_yoga',
      term: 'Yoga',
      definition: 'The cross-platform Flexbox layout engine implemented in C++ that powers React Native layouts. Layout runs natively rather than in web CSS, and its defaults differ, notably flexDirection: column.',
    ),
    SeedFlipcard(
      id: 'rn_fc_flatlist',
      term: 'FlatList',
      definition: 'The virtualized list component in React Native. It renders only the cells inside a sliding window, recycling rows, and is tuned with keyExtractor, initialNumToRender, maxToRenderPerBatch, windowSize, and getItemLayout.',
    ),
    SeedFlipcard(
      id: 'rn_fc_interaction_manager',
      term: 'InteractionManager',
      definition: 'An API that schedules callbacks to run only after ongoing animations and interactions finish. Use createInteractionHandle to hold off non-essential work during a transition for a smooth 60 fps.',
    ),
    SeedFlipcard(
      id: 'rn_fc_async_storage',
      term: 'AsyncStorage',
      definition: 'A community-maintained, asynchronous, unencrypted key-value store for React Native. It stores strings only (so objects are JSON.stringified) and is backed by SQLite on Android; secrets do not belong in it.',
    ),
    SeedFlipcard(
      id: 'rn_fc_memoization',
      term: 'Memoization (React)',
      definition: 'Caching values or function references between renders so re-renders skip expensive work. React.memo bails out on shallow-equal props, useMemo caches a computed value, and useCallback stabilizes a function reference.',
    ),
    SeedFlipcard(
      id: 'rn_fc_reconciliation',
      term: 'Reconciliation',
      definition: 'The React algorithm that diffs the new element tree against the previous one and computes the minimal set of updates. Keys on list items let React identify moved, added, or removed elements efficiently.',
    ),
    SeedFlipcard(
      id: 'rn_fc_batching',
      term: 'State batching',
      definition: 'Grouping multiple state updates into a single re-render. React 18 batches updates everywhere by default, including event handlers, promises, timeouts, and native events, avoiding intermediate renders and layout thrash.',
    ),
    SeedFlipcard(
      id: 'rn_fc_app_state',
      term: 'AppState',
      definition: 'A React Native API that reports whether the app is active, inactive, or backgrounded. Listen with AppState.addEventListener to pause timers, persist progress, or refresh data when the app returns to the foreground.',
    ),
  ],
);
